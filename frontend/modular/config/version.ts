export const garmetixModularVersion = {
  version: '6.8.5',
  stage: 'Stage GST-4: HSN/SAC Master',
  label: 'Version6 Stage GST-4: HSN/SAC Master',
  summary: 'Fourth stage of the GST & Taxes module (see Stage GST-1/2/3 for schema/foundation, provider setup, and GSTIN verification). Adds the local HSN/SAC master - the first-class, API-independent directory that daily billing relies on instead of a live API call. Backend: new GstHsnEndpoints.cs (its own /api/gst/hsn route group) with full CRUD, GET / with hsnCode/description/codeType/isActive filters plus pagination and a per-row Product mapping count (grouped-count query against Products.HSNCode, not N+1), POST /search for autocomplete (goods/services filter, top 20 matches), POST /bulk-status for bulk activate/deactivate, and CSV import/export/template. Deliberate scope adaptation, disclosed rather than silent: bulk import accepts CSV, not .xlsx - this codebase has no Excel-reading library (only a write-only SimpleXlsxBuilder used for GST return exports), and hand-rolling an OpenXML parser for arbitrary uploaded workbooks is a real correctness risk for a data-load path; CSV serves the same "bulk-load a HSN directory" need with far less risk. New page apps/books/pages/gst-tax-hsn.vue: filterable/paginated table with checkbox multi-select for bulk activate/deactivate, a create/edit modal (code/type/description/UQC/CGST/SGST/IGST/Cess/active), Import CSV (added postForm to books-api.ts, mirroring main-api.ts\'s existing multipart-upload helper since Books never needed one before), and Download Template. Also wired a lightweight HSN autocomplete into the Inventory app\'s existing Products page (apps/inventory/pages/products.vue) - a debounced search-as-you-type dropdown under the existing plain-text HSN input (calling the new /gst/hsn/search endpoint), plus a "Missing HSN" inline warning when the field is empty; the existing field/form itself was left otherwise untouched. Registered route /gst-tax-hsn, added to the Books "GST & Taxes" menu and dashboard quick actions. Validated: dotnet build (0 errors, including a self-inflicted CA2024 async-EndOfStream warning caught and fixed before commit), full backend test suite (72 passed, 0 regressions), clean books-web and inventory-web production builds (new/changed routes confirmed via compiled-bundle grep for books-web), validate-structure.mjs, and a dev-server console-error check on /gst-tax-hsn (zero errors, correctly redirects to Login - Books remains login-gated with no test credentials in this environment; inventory-web has no configured dev-server launch entry in this environment, so the Products-page autocomplete was verified via a clean production build only, not an interactive dev-server pass).'
} as const
