export const garmetixModularVersion = {
  version: '6.2.2',
  stage: 'Stage 14Q.4 Vendor Type Distinction',
  label: 'Version6 Stage 14Q.4 Vendor Type Distinction',
  summary: 'Added a nullable VendorType column to the Vendor entity so tailoring/alteration vendors can be genuinely distinguished from purchase vendors, reusing a VendorType enum that already existed (with a Tailoring literal) but had never been wired to anything - a prior attempt to do exactly this had broken the build and left behind a regression-guard script forbidding item.VendorType. Added EF migration 20260710120000_AddVendorType.cs and updated the model snapshot by hand, matching this repo\'s existing hand-written-migration convention (confirmed migrations apply automatically via db.Database.MigrateAsync() on API startup, so no manual production DB step is needed). Wired TailoringEndpoints.cs to filter/create tailoring vendors by the real enum value instead of the old hardcoded-label workaround, excluded VendorType.Tailoring from the general GET /api/vendors list by default, and added an optional vendor-type field to the Purchase Vendors page. Removed the now-obsolete forbid/require assertions checking for the old workaround text in legacy/scripts/validation/stage8e-package7-compile-hotfix-static-checks.py. Backward-compatibility limitation: pre-migration vendor rows have VendorType == null and can\'t be retroactively classified, so they remain visible in both vendor pickers - only vendors created after this change are cleanly separated by type. Verified via a clean dotnet build (0 errors) and a clean main-web production build.'
} as const
