export const garmetixModularVersion = {
  version: '6.9.1',
  stage: 'Final Accounts app-switcher fix - cross-app visibility for the 9th app',
  label: 'Version6: Final Accounts visible in every app switcher',
  summary: 'Amit reported that Final Accounts could not be selected from the app switcher inside any other app, even though every other app was selectable from inside Final Accounts. Root cause matched the exact bug already fixed once before for Inventory: each modular app hardcodes its own runtimeConfig.public.appUrls object in its own nuxt.config.ts, and buildAppTargetLinks only shows an app in the switcher if the current app declared that specific NUXT_PUBLIC_GARMETIX_*_URL env key. The Final Accounts app itself declared its own key (so it could see everyone), but none of the other 8 apps (main/pos/hr/ai-sense/books/crm/admin/inventory) declared a key for Final Accounts - so it was invisible everywhere except from inside itself. Added NUXT_PUBLIC_GARMETIX_FINAL_ACCOUNTS_URL to all 8 apps appUrls objects, and closed two matching gaps in the SRP deploy script that would have silently kept the fix from ever reaching production: build_app() bakes every app URL into every other apps build via explicit env vars at build time, and patch_static_runtime_config() re-patches those same baked-in values for any app skipped by a selective --apps= deploy - neither list included Final Accounts yet. Verified locally before redeploying: rebuilt main-web with the new env var set and grepped the compiled bundle to confirm NUXT_PUBLIC_GARMETIX_FINAL_ACCOUNTS_URL now bakes in correctly, matching the exact verification method used for the original Inventory fix.'
} as const
