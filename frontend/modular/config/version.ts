export const garmetixModularVersion = {
  version: '6.9.16',
  stage: 'Swalekha PersonalFin_10 - Investments III (Shares/Stocks + Other Assets)',
  label: 'Version6: Swalekha tracks Shares/Stocks with realized+unrealized P&L, plus PPF/EPF/NPS/Gold',
  summary: 'New Share/Stock holding tracker (symbol/company/exchange/demat/broker), optionally linked to one of the Owner\'s own Accounts Hub accounts, using the exact same integration pattern PersonalFin_09 established for Mutual Funds: Buy debits the linked account and grows the position, Sell credits it back and reduces TotalInvested at average cost per share while adding the realized gain/loss to a running RealizedPnL - deleting any transaction reverses its exact effect (units, invested amount, realized P&L, account balance) since Sells record precisely how much they removed rather than re-deriving a ratio later. CurrentPrice is manually updated and drives CurrentValue/UnrealizedPnL/UnrealizedPnLPercent. New /investments/shares list (create/edit, Update Price, aggregate totals) and a detail page (P&L panel, Record Transaction form, paginated transaction history). Also added Other Assets - simple PPF/EPF/NPS/Gold value snapshots (/investments/other-assets), deliberately scoped as CurrentValue-only entries with no transaction history, matching the design\'s explicit "simple asset entries" framing for these holding types. Investments hub gained Shares and Other Assets cards. Live-verified via direct API checks with exact math: Buy 10 shares at Rs 100 (account debited 2150->1150), Sell 4 at Rs 150 (avg-cost-removed Rs 400, realized P&L Rs 200, quantity 10->6, invested 1000->600, account credited to 1750), then a price update to Rs 120 correctly computed Current Value Rs 720 and Unrealized P&L Rs 120 (20%) - all confirmed via UI-driven creation plus direct API-level transaction/reversal checks.'
} as const
