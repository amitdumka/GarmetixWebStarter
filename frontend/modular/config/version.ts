export const garmetixModularVersion = {
  version: '6.8.2',
  stage: 'Stage GST-3: GSTIN Verification',
  label: 'Version6 Stage GST-3: GSTIN Verification',
  summary: 'Third stage of the GST & Taxes module (see Stage GST-1/GST-2 for schema/foundation and provider setup). Builds the actual GSTIN verification flow on top of the Stage GST-1/2 multi-provider registry: local cache first (unless force-refresh), then enabled providers with the GSTIN_LOOKUP feature in priority order, falling back to the next provider only if FallbackEnabled, ending in an honest local-format/state-code-only answer if nothing is configured or everything fails. New GstinResolutionService.cs implements the first real adapter (GenericRestProvider - actual HTTP GET against {baseUrl}/gstin/{gstin} with API_KEY/AUTH_TOKEN credential injection and flexible multi-path JSON field parsing); every other provider type still returns an honest "arrives in a later stage" message rather than faking a live call, matching Stage GST-2\'s test-tool precedent. Every attempt (success or failure) is logged to GstApiCallLogs, and every successful live lookup is cached to GstinVerificationCaches so later lookups of the same GSTIN never need a live call. New endpoints: POST /api/gst/gstin/verify, GET /api/gst/gstin/cache (search/list), GET /api/gst/gstin/cache/{gstin}, POST /api/gst/gstin/cache/manual (manual entry when no provider has it). New page apps/books/pages/gst-tax-gstin-verify.vue: GSTIN input + Verify + Force Refresh, a result panel (legal/trade name, status, taxpayer type, state, address, source, last verified), Copy Details (clipboard), a recently-verified-GSTIN cache table, and Update Vendor/Update Customer buttons that search the existing vendor/customer masters (via the same shared backend API every app already calls) and push the verified GST fields onto the selected record - deliberately left the existing "Check GSTIN" buttons already on the Vendor Master (main app) and Customer Master (CRM app, via CustomerForm.vue) untouched, since they already work against the older single-provider /api/gstin module and replacing working data-entry functionality carried more regression risk than value for this stage; the new dedicated GST & Taxes page is additive, not a replacement. Registered route /gst-tax-gstin-verify, added to the Books "GST & Taxes" menu and dashboard quick actions. Validated: dotnet build (0 errors), full backend test suite (72 passed, 0 regressions), clean books-web production build (new route confirmed via compiled-bundle grep), validate-structure.mjs, books-accounting-contract-check.mjs, and a dev-server console-error check (zero errors, correctly redirects to Login - Books remains login-gated with no test credentials in this environment, so the live verify/apply-to-vendor/apply-to-customer flow was not exercised interactively). Versioning note: from this stage on, patch numbers bump within a minor version (6.8.1 -> 6.8.2 -> ... -> 6.8.70) before the minor version itself increments (then 6.9.1), per Amit\'s explicit instruction, replacing the previous per-stage minor-version-bump habit.'
} as const
