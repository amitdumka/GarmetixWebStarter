export const garmetixModularVersion = {
  version: '6.9.9',
  stage: 'Swalekha PersonalFin_05 - Travel Expense Sheets',
  label: 'Version6: Swalekha Trips (Travel Expense Sheets)',
  summary: 'PersonalFin_05 of the isolated Swalekha (Personal & Personal Finance) module. New SwalekhaTrip model - a thin metadata wrapper around a dedicated SwalekhaExpenseSheet (SheetType "Travel") created automatically on trip creation. Expense entries are added via the existing PersonalFin_04 SwalekhaExpenseEndpoints entry routes against the trip\'s SheetId, not duplicated - the money already lives in a Travel-typed sheet from the moment it\'s entered, so "roll up into the main Expense ledger under Travel" needs no data copy on close: the existing expenses/summary endpoint\'s bySheetType grouping already aggregates every trip\'s spend under Travel, while per-trip drill-down stays available via each trip\'s own SheetId. Closing a trip (SwalekhaTripEndpoints.cs, POST .../close) just finalizes it - IsClosed/ClosedAt on the trip, IsActive=false on its linked sheet - with a matching reopen action. New/updated/deleted trip metadata (name, budget) stays in sync with the linked sheet. Frontend: new /trips (trip table with budget-vs-spent, an open/closed badge, close/reopen/edit/delete actions, create/edit slideover) and /trips/[id] (trip header, an add-expense form that\'s hidden once closed, a closed-trip explanatory banner, paginated entry table reusing the same expense-entry endpoints) pages, with dashboard nav updated.'
} as const
