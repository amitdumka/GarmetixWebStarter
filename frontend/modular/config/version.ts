export const garmetixModularVersion = {
  version: '6.8.7',
  stage: 'Stage GST-6: GST Audit Engine',
  label: 'Version6 Stage GST-6: GST Audit Engine',
  summary: 'Sixth stage of the GST & Taxes module (see Stage GST-1 through GST-5 for schema/foundation, provider setup, GSTIN verification, HSN/SAC master, and rate master). Adds the configurable audit engine (spec section 11) against the 17-rule catalog seeded in Stage GST-1. Backend: new GstAuditEngineService.cs runs Product checks (HSN missing/invalid, GST rate missing, UQC missing, and a genuine GST_RATE_MISMATCH check reusing the Stage GST-5 rate resolver), Vendor/Customer checks (GSTIN format invalid, GSTIN inactive, and a GSTIN_STATE_MISMATCH check for Customer specifically since only Customer has a separate declared State field to compare against - Vendor has no such field, confirmed by reading the entity, not assumed), and - going further than originally scoped for this stage after a dedicated schema research pass confirmed the exact Invoice/InvoiceItem/PurchaseInvoice field names - real Sale/Purchase invoice checks: CGST_SGST_NOT_EQUAL, INTRASTATE_IGST_USED, INTERSTATE_CGST_SGST_USED (all against the header InterState flag and CGST/SGST/IGST amounts), B2B_GSTIN_REQUIRED (Sale only), SALE_LINE_TAX_MISMATCH/PURCHASE_LINE_TAX_MISMATCH (recomputes expected tax from each line stored MRP/DiscountAmount/TaxPercentage and compares against the stored TaxAmount, since those are genuinely separate persisted columns, not computed - a later manual edit to one without the other is exactly the drift this check exists to catch), GST_RATE_MISMATCH/GARMENT_THRESHOLD_RATE_MISMATCH per line via the same rate resolver used for Products, and INVOICE_TOTAL_TAX_MISMATCH (header tax total vs sum of line taxes). A run is capped at 2000 lines per module to keep an unscoped "audit everything" click from running away - narrow the date range for a full historical sweep. New GstAuditEndpoints.cs (/api/gst/audit): GET/PUT rules (enable/strict-mode toggle, Admin-gated), POST /run (Edit-gated), GET /findings (status/severity/module filters), and ignore/mark-fixed/accept actions. New page apps/books/pages/gst-tax-audit.vue: run panel (module/date range), findings list with per-finding actions, and a rules management table. Registered route /gst-tax-audit, added to Books menu + dashboard quick actions. Validated: dotnet build (0 errors on first attempt - the schema research paid off), full backend test suite (72 passed, 0 regressions), clean books-web production build (new route confirmed via compiled-bundle grep), validate-structure.mjs, and a dev-server console-error check (zero errors, redirects to Login).'
} as const
