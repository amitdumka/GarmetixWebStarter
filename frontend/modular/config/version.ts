export const garmetixModularVersion = {
  version: '6.9.15',
  stage: 'Swalekha PersonalFin_09 - Investments II (Mutual Funds + SIP tracker)',
  label: 'Version6: Swalekha tracks Mutual Funds/SIPs with real XIRR returns',
  summary: 'New Mutual Fund folio tracker - scheme/AMC/folio/investment mode (Lumpsum or SIP), optionally linked to one of the Owner\'s own Accounts Hub accounts. Purchase/SIP-installment transactions debit the linked account and grow CurrentUnits/TotalInvested; Redemption credits it back and reduces TotalInvested at the fund\'s average cost per unit (a standard approximation for a personal tracker, not full FIFO/LIFO lot accounting) - deleting any transaction reverses its exact effect on units, invested amount, and the account balance, since Redemptions record precisely how much they removed rather than re-deriving a ratio later. CurrentNav is manually updated and drives CurrentValue/AbsoluteReturn/ReturnPercent plus a genuine XIRR calculation (Newton-Raphson with a bisection fallback) over the fund\'s full dated cash-flow history - shipped with 5 unit tests including a hand-verified 20% case, given the financial-correctness stakes of getting this wrong. SIP funds also get a DueThisMonth flag (mirroring PersonalFin_04\'s Recurring Bill pattern). New /investments/mutual-funds list (create/edit, Update NAV, aggregate totals) and a detail page (returns/XIRR panel, Record Transaction form, paginated transaction history) plus a Mutual Funds card on the Investments hub. Live-verified end to end through the actual UI and direct API checks: a real Lumpsum purchase (Rs 1,000 at NAV 100 = 10 units) followed by a NAV update to 130 correctly computed Current Value Rs 1,300, Absolute Return Rs 300, and XIRR ~30% (hand-verified against the exact one-year-round-trip math); a partial redemption and its exact reversal both checked at the API level; a SIP fund\'s DueThisMonth flag correctly flipped from true to false after recording an installment.'
} as const
