export const garmetixModularVersion = {
  version: '6.4.1',
  stage: 'Stage 14U.1: fix - explicit New Return button was missing',
  label: 'Version6 Stage 14U.1: fix - explicit New Return button was missing',
  summary: 'Amit reported right after the Stage 14U deploy that Add purchase return was not visible or did not exist, in neither the old nor new sense. Verified the actual rendering was not broken this time (grepped the live compiled bundle directly for the search-section text, confirmed it was genuinely present and serving, ruling out another crash like Stage 14T.1) - this was a real UX-clarity gap, not a bug: the page has always required searching for the original invoice first, then clicking Return Items on the matching row, with no distinctly-labeled Add or New Return button anywhere, so it did not read as an Add affordance to someone scanning for one. Added a prominent New Return button next to Settlement QA and Refresh in the page header that scrolls to (and tries to focus) the invoice-search section, and renamed that section from the plain Find Purchase Invoice To Return to New Return - Find Purchase Invoice with an explanatory subtitle. No functional change - the underlying search-then-select flow is unchanged, this only makes the existing entry point discoverable. Validated via a clean main-web production build and a compiled-bundle grep confirming the new button text and no dangling references.'
} as const
