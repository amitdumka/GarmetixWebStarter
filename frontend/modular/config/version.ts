export const garmetixModularVersion = {
  version: '6.9.12',
  stage: 'Swalekha PersonalFin_06 - multi-owner data isolation',
  label: 'Version6: Swalekha now scopes data per-Owner login',
  summary: 'Amit caught a real design gap: Swalekha had no concept of "which Owner" - if a company has two Owner logins, both would have shared the exact same accounts/contacts/expenses/etc, since nothing filtered by user. New SwalekhaOwnedEntity base class (Guid OwnerId) that all 9 existing Swalekha entities now extend instead of plain BaseEntity. A new scoped SwalekhaOwnerContext, populated per-request by SwalekhaOwnerMiddleware from the JWT\'s NameIdentifier claim (same established pattern as AuditActorContext/AuditActorMiddleware), is injected into SwalekhaDbContext, which (a) auto-stamps OwnerId on every insert and (b) applies a combined "not deleted AND OwnerId matches" global query filter to every SwalekhaOwnedEntity type - so no individual endpoint can forget to scope a query and accidentally leak one owner\'s financial data to another. The one deliberate exception (for PersonalFin_07\'s family-transfer sync) is that the auto-stamp only fires when OwnerId is still the default Guid.Empty, so a handler can explicitly set a different (recipient) OwnerId when posting a cross-owner synced entry. SwalekhaSchemaRepairService extended with an OwnerId column + index on all 9 tables. Verified live with two real Owner logins: creating data as Owner 2 is completely invisible to Owner 1 and vice versa, confirmed via direct API calls against the already-running local backend.'
} as const
