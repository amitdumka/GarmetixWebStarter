export const garmetixModularVersion = {
  version: '6.9.3',
  stage: 'Final Accounts report fix, part 2 - GroupBy hit the same translation gap',
  label: 'Version6: Final Accounts reports fixed for real (ValueTuple projection)',
  summary: 'The prior fix moved the accountIds.Contains() filter inside ReportLines(), which fixed that one operator - but Amit immediately hit the same underlying problem one level deeper: GroupBy(item => item.Line.AccountId), used by General Ledger, Trial Balance, Profit and Loss and closing-balance loading, failed with the identical class of error. The real root cause was broader than the first fix addressed: ReportLines() projected the joined entry and line into a private positional record (ReportLineQueryRow), and EF Core cannot reliably flatten member access through a custom records constructor back to the underlying columns for ANY downstream operator that needs a real column reference - not just Contains, but GroupBy key selectors too, and potentially Select/OrderBy compositions elsewhere in the same file. Rather than patch each operator as it surfaces in production one at a time, replaced the record entirely with a named ValueTuple return type (IQueryable<(FinalAccountsJournalEntry Entry, FinalAccountsJournalLine Line)>) - the officially-supported EF Core pattern for exactly this reusable-queryable-helper scenario, which correctly flattens through Where, GroupBy, OrderBy and Select alike. Tuple literal syntax is not legal inside an expression tree (CS8143), so the projection uses the explicit new ValueTuple<T1,T2>(entry, line) constructor form, which is. Every one of the ~40 existing item.Entry.X / item.Line.X call sites across the file needed no changes, since named-tuple element access uses identical C# syntax to record property access. Audited every other private record type in the FinalAccounts module (CloseBalanceRow, JournalTotalRow, LedgerSourceRow) and confirmed none of them share this risk - they are all terminal Select-then-materialize projections, not further composed with Where/GroupBy/Contains before execution. Validated: dotnet build (0 errors), full backend test suite (219 passed, 3 pre-existing Postgres-only skipped, zero regressions). This environment still has no live Postgres to run an end-to-end translation check against (the 3 skipped tests are gated on GARMETIX_TEST_POSTGRES, unset here) - deployed and asked Amit to confirm the specific report he originally hit now loads.'
} as const
