export const garmetixModularVersion = {
  version: '6.0.65',
  stage: 'Stage 14N BooksMasterTable Props Fix',
  label: 'Version6 Stage 14N BooksMasterTable Props Fix',
  summary: 'Books: fixed a critical bug in the shared BooksMasterTable component that silently broke every data table across the whole Books app (Parties, Ledgers, Accounting, Vouchers, Cash Details, Audit, Financial Year Locks, GST Returns/Reports/Production, Petty Cash, Vendor Payments/Settlements) - the component declared its columns/rows/emptyText props via a bare defineProps<{...}>() referencing an imported ApiRecord type, which failed to generate a runtime props declaration in this build, so Vue treated columns/rows as unrecognized attributes (visible as literal columns="[object Object],..." on the rendered DOM) instead of props, leaving every table permanently empty regardless of how much real data the API returned. Fixed by switching to reactive-destructure-with-defaults (const { columns = [], rows = [], emptyText = \'...\' } = defineProps<{...}>()), matching the working BooksPlaceholder.vue pattern - verified the fix compiles to the identical structural pattern as that known-good component. This explains the "no parties/ledgers listed despite data existing" reports from earlier this session - it was never a backend/data/scope issue.'
} as const
