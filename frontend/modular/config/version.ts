export const garmetixModularVersion = {
  version: '6.9.36',
  stage: 'ai-sense: renamed the old stock-reports stub to stock-reports-preview',
  label: 'Renamed AI Sense\'s pre-existing broken stock-reports stub to stock-reports-preview, now that Inventory owns the real /stock-reports',
  summary: 'Amit asked to rename the old AI Sense stock-reports route (rather than delete it) since it will be rebuilt properly later in more depth, now that Inventory owns the real /stock-reports. Renamed route id stock-reports to ai-stock-reports-preview, path /stock-reports to /stock-reports-preview, label to "Stock Reports (Preview)"; moved the underlying file apps/ai-sense/pages/stock-reports.vue to stock-reports-preview.vue via git mv (page title updated to match) since Nuxt file-based routing requires the file path and route path to agree. Updated every reference to the old path: the ai-sense sidebar entry, the "Stock Risk" quick-link button on the ai-sense dashboard home page, and the file-existence check in validate-structure.mjs. Left the stub\'s actual (broken) internals untouched, as agreed - that is deliberately deferred to the future in-depth rebuild. Verified no other reference to the old id/path remained anywhere in frontend/modular via a full-repo grep (the only other stock-reports hits are the shared backend API endpoint path inventory/stock-reports/summary, unrelated to this frontend route). Validated: clean ai-sense-web production build (new route /stock-reports-preview prerenders, old /stock-reports route is gone), validate-structure.mjs.'
} as const
