export const garmetixModularVersion = {
  version: '6.8.9',
  stage: 'Stage GST-8: Purchase GST Review',
  label: 'Version6 Stage GST-8: Purchase GST Review',
  summary: 'Eighth stage of the GST & Taxes module (see Stage GST-1 through GST-7 for schema/foundation, provider setup, GSTIN verification, HSN/SAC master, rate master, audit engine, and Sale GST Review), implemented autonomously via the scheduled GST continuation task. Adds "Purchase GST Review": the same configurable-rate-driven review Sale GST Review built for Sale invoices, mirrored for Purchase invoices, plus Input Tax Credit (ITC) eligibility warnings. Research confirmed there is no existing hardcoded-threshold "Purchase Review" tool to merge with (unlike Sale Review, which had SaleReviewEndpoints.cs) - this is the first Purchase GST review surface in the codebase, and Purchase-import (OCR) batches are covered automatically once posted since posting writes into the same PurchaseInvoices/PurchaseInvoiceItems tables this reads from (unposted import drafts are intentionally out of scope, not yet real financial invoices). Backend: new GstPurchaseReviewEndpoints.cs (/api/gst/purchase-review, GET only, read-only) resolves each Purchase invoice lines GST rate live against the configurable Rate Master (Stage GST-5) exactly like the Sale side, cross-links each invoice to its count of open GST Audit Engine findings (Stage GST-6, ModuleArea=Purchase via the PurchaseInvoiceId field/filter already added in Stage GST-7), and computes a per-invoice ITC eligibility status - "Not Eligible" when the purchase invoice itself carries no vendor GSTIN, "At Risk" when the vendor masters own GST registration status is on record as something other than Active, "Unverified" when a GSTIN is present but has never been confirmed via the GST & Taxes GSTIN Verification tool (Stage GST-3), "Eligible" otherwise - explicitly documented as a heuristic since this codebase has no formal ITC ledger table. Capped at 2000 lines per request, same as Sale GST Review. New page apps/books/pages/gst-tax-purchase-review.vue: filters (date range, store, vendor, search, only-issues), a rate-mismatch/not-configured/open-findings summary row plus a dedicated ITC summary row (not eligible/at risk/unverified counts), an invoice table (stored vs resolved tax, open findings link to /gst-tax-audit, ITC status badge + note), and a line table (HSN/category, stored vs resolved %, tax diff, matched rule, Garment Threshold badge) with pagination - deliberately a single section, unlike Sale GST Reviews two-section layout, since there is no legacy apparel-threshold Purchase tool to port alongside it. Registered route /gst-tax-purchase-review, added to Books GST & Taxes sidebar menu and dashboard quick actions. Validated: dotnet build (0 errors), full backend test suite (72 passed, 3 pre-existing Postgres-only skipped, 0 regressions), clean books-web production build (new route confirmed via compiled-bundle grep), validate-structure.mjs, and a dev-server console-error check on /gst-tax-purchase-review (zero errors, redirects to Login).'
} as const
