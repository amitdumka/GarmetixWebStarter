export const garmetixModularVersion = {
  version: '6.8.12',
  stage: 'Salary payments now flow into Day Book and day-closing cash reconciliation',
  label: 'Version6: Salary payments now flow into Day Book and day-closing cash reconciliation',
  summary: 'Amit reported that salary payments were not showing up in the Day Book, were not part of the accounting system, and were breaking day closing. Investigation confirmed the accounting journal side was already correct - every salary-payment save/update/delete path already calls AccountingPostingService.PostSalaryPaymentAsync/RemoveSalaryPaymentPostingAsync, so Trial Balance and Ledger Statement (which read every JournalLine unconditionally) already included salary payments correctly. The real gaps were in the two day-to-day report/reconciliation surfaces: Day Book only ever built rows from Sales, Purchases, Vouchers, CashVouchers, Customer Receipts and Vendor Payments - SalaryPayments were invisible unless the operator explicitly toggled Show Journal Entries, and even then just showed as a generic Journal line, not a real Salary Payment row. Fixed DayBookEndpoints.cs to query SalaryPayments directly and add them as a first-class row (with a real Salary Payment detail view, a payments/salary type filter, and Open Source deep-linking into the HR app - day-book.vue needed a new hr entry in its app-url and source-path mapping, since it previously only knew about main/pos/books). Separately, and more materially: the day-closing "book closing cash" figure - the system-calculated expected cash used both to compare against the physical count and, when the operator relies on the default use-book-cash-if-no-count option, as the actual persisted DayEnd.ClosingBalance/PettyCashSheet.CashInHand - never subtracted cash-mode salary payments at all, in either of the two independent places this calculation is duplicated (StoreDayEndpoints.CalculateBookSummaryAsync, used by POS Day Close, and the separate PettyCashEndpoints.CalculateAsync used by the Books Petty Cash Sheet page). Any day cash salary was paid out, the system would silently overstate expected closing cash by that amount. Fixed both calculations to subtract cash SalaryPayments, added a SalaryPayments figure to both DTOs for transparency (surfaced as a new Salary paid in cash today field on the POS Day Close page, and via the existing calculation-notes list on the Books Petty Cash page, both already rendered spots), and fixed StoreDayEndpoints.ApplyPettyCashDraft (which recomputes cashInHand from scratch whenever the operator manually edits/submits the closing preview) to keep subtracting the carried-over SalaryPayments figure instead of silently dropping it. Validated: dotnet build, full backend test suite (72 passed, 3 pre-existing Postgres-only skipped, zero regressions), clean books-web and pos-web production builds, validate-structure.mjs, and dev-server console-error checks on /day-book and /day-close (both zero errors, correctly redirect to Login).'
} as const
