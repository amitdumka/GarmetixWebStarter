export const garmetixModularVersion = {
  version: '6.9.38',
  stage: 'Sale: Vyapar Sale Invoice Import ported to modular main app',
  label: 'New Sale pages: Vyapar Sale Import wizard, Imported Invoices, Import Batches (final summary + undo + clear history)',
  summary: 'Ports the legacy /billing/vyapar-import, /billing/vyapar-imported and /billing/vyapar-import-batches pages into the modular main (Back Office) app under Sales - all three against the pre-existing VyaparSaleImportEndpoints.cs (8 endpoints, zero backend changes). Confirmed via research this is structurally simpler than the already-ported Purchase Invoice Import feature: Vyapar exports structured Excel (Sale Report + Item Details sheets), so there is no OCR/proof-file/draft-persistence lifecycle - it is a stateless preview-then-confirm flow, not a durable batch-editing workflow. vyapar-import.vue: a 4-step wizard (upload -> review barcode matches with a filterable/paginated/searchable line table, per-line Override Barcode input and Import/Create checkboxes, a bulk barcode-mapping-file upload that fills overrides across matching lines client-side -> map non-cash payment sources to bank accounts -> confirm with useVyaparInvoiceNumbers/createMissingProductsAndStock/allowStockBridgeForInsufficientStock flags via a mandatory final-approval modal, two scoped submit buttons for "Fully Matched" vs "Ready" invoice sets). vyapar-imported.vue: paginated read-only imported-invoice list with date/search filters and bill/paid/balance totals. vyapar-import-batches.vue: a Final Summary reconciliation dashboard (payment-mode + GST tables, issues/notices, closeout checklist), a paginated batch list with a guarded batch-level Undo (reverses stock/accounting for every active invoice in the batch), and a Clear Cancelled History action - both destructive actions gated behind a confirmation modal with an explicit understanding checkbox, matching this project\'s established guarded-write pattern. Added pageSizeOptions/paginateRows helpers to main-api.ts (the main app had never needed client-side pagination before this stage). Three new routes under the existing Sales sidebar group. Validated: clean main-web production build (all three new routes prerender with real content), validate-structure.mjs, and a dev-server console-error check on all three routes (zero errors, correctly redirect to Login - no local test credentials in this environment).'
} as const
