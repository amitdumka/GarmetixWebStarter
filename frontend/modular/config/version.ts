export const garmetixModularVersion = {
  version: '6.0.58',
  stage: 'Stage 14F.7 MCP Server Layer',
  label: 'Version6 Stage 14F.7 MCP Server Layer',
  summary: 'Assistant: added an MCP (Model Context Protocol) server layer exposing the same read-only tool catalog (get_store_sales_summary, compare_store_performance, get_low_stock_items, get_outstanding_dues, get_business_snapshot, get_today_snapshot) used by the in-app chat assistant, reachable at POST /api/mcp for external MCP clients (Claude Desktop, claude.ai). Purely additive - AssistantMcpTools forwards every call to the existing AssistantToolCatalog.ExecuteAsync with no business logic duplicated, and reuses the standard JWT bearer auth pipeline so tenant scoping matches the rest of the API. Gated behind a new Assistant:McpEnabled flag, independent of and defaulting off alongside the existing Assistant:Enabled chat flag. Also fixed the Git-Bash dotnet publish doubled-drive-letter deploy bug for real this time (cygpath -w), superseding an earlier insufficient fix, and closed out the CRM Digital Bills pagination/clipping/deleted-invoice fixes plus the Main app Sale Invoices page rebuild from Stage 14J (all committed, deploy still held pending the 3-iteration gate).'
} as const
