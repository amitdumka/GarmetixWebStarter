export const garmetixModularVersion = {
  version: '6.9.13',
  stage: 'Swalekha PersonalFin_07 - Owner Profile + Family Connections + Transaction Sync',
  label: 'Version6: Swalekha profiles, family links, and one-entry family transfers',
  summary: 'Built on PersonalFin_06\'s per-Owner data isolation: a new Owner Profile (PAN/Aadhar/Passport/address/spouse name+contact/a linked bank account picked from the Owner\'s own Accounts Hub), auto-provisioned on first access from the shared GarmetixDbContext\'s Employee/EmployeeDetail record when the logged-in Owner\'s AppUser.EmployeeId resolves to an EmployeeCategory.Owner row (PAN/Aadhar/Mobile/spouse name carry over automatically, still fully editable afterward - Passport has no HRM equivalent and stays Owner-entered only). A new Family Members list (name/relationship/mobile/DOB/notes), each optionally linked to another real Owner login on the platform via a picker sourced from live Owner-type AppUsers. The requested "one entry syncs both sides" transfer: linking is deliberately mutual-consent, not one-directional - a transfer only goes through once both Owners have independently added each other back with a Linked Owner Login set, checked fresh at transfer time (not just when the link was first created, since either side could remove it later). A confirmed transfer debits the sender\'s chosen account and credits the recipient\'s own linked-account-or-best-guess account in one atomic transaction, entirely server-side - the recipient never has to do anything for their balance to update. Live-verified end to end with three real logins (a real Employee-linked Owner auto-provisioning correctly, a second Owner confirming the reciprocal link, a real cross-owner transfer moving money and updating both sides\' account balances), plus the negative path (an unconfirmed link is correctly rejected with a clear message before any money moves).'
} as const
