export const garmetixModularVersion = {
  version: '6.9.23',
  stage: 'Communication & Mail CM-06 - Templates',
  label: 'Communication & Mail: safe HTML email templates with versioning, approval, preview and test-send',
  summary: 'CM-06 of the Garmetix Communication & Mail module (branch Mail-Com, not yet merged). EmailTemplateRenderer renders {{token}} merge fields with two independent safety layers - every token value is HTML-escaped before substitution (so merge data can never inject markup), and the final rendered HTML is passed through the new HtmlSanitizer dependency (an allow-list sanitizer, not a hand-rolled regex blacklist) to strip scripts/event handlers even from administrator-authored template HTML. EmailTemplateSeedService seeds the 14 initial system templates named in the spec (sale invoice, receipt, credit note, purchase inward, vendor payment, payment reminder, payslip, attendance notice, password reset, invitation, daily summary, low stock, system alert, internal-message notification) with real usable content, not placeholders. EmailTemplateEndpoints.cs adds template CRUD, draft/approve/restore versioning (approving a version publishes it as current; restoring an older approved version needs no new draft), safe preview, and an immediate test-send bypassing the queue. New Templates page in the Communication & Mail app: list, version history with Load/Approve/Restore, an inline editor that creates new draft versions, live preview, and test-send. Validated: dotnet build 0 errors/0 new warnings (HtmlSanitizer 9.0.892 pulled in with a disclosed known-moderate advisory on its pinned transitive AngleSharp 0.17.1 dependency - overriding it breaks the sanitizer\'s own API surface, so the advisory is accepted rather than silently overridden), backend suite 338 passed/0 failed/11 skipped (8 new template-renderer unit tests covering escaping/sanitization/token-substitution, zero regressions), clean communication-web build (all 5 routes prerender with real content), validate-structure.mjs passing.'
} as const
