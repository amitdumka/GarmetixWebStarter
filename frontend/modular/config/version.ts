export const garmetixModularVersion = {
  version: '6.8.6',
  stage: 'Stage GST-5: GST Rate Master',
  label: 'Version6 Stage GST-5: GST Rate Master',
  summary: 'Fifth stage of the GST & Taxes module (see Stage GST-1 through GST-4 for schema/foundation, provider setup, GSTIN verification, and HSN/SAC master). Adds the date-effective GST Rate Master and its resolution service - the piece that turns the garment 5%/18% threshold rule seeded in Stage GST-1 into something actually usable. Backend: new GstRateResolutionService.cs implements the resolve algorithm from spec section 9 - match HSN first, then product category, then filter candidates by the rule\'s price threshold (garment slab), pick the highest-priority match, split into CGST/SGST (intra-state) or IGST (inter-state) unless the rule has explicit split rates, fall back to the HSN master\'s own default rate, and finally an honest "nothing configured" result rather than guessing. Uses the invoice date, never "today" - a rate change must never retroactively alter an already-posted historical line. New GstRateEndpoints.cs (/api/gst/rates): full CRUD plus POST /resolve. New page apps/books/pages/gst-tax-rates.vue: filterable rule table, create/edit modal (HSN/category/rate/CGST-SGST-IGST-Cess/price threshold/effective dates/priority), and a live "Resolve A Rate" test panel that calls the resolver exactly the way Sale/Purchase entry will. Product Master integration: added a "Resolve" button next to the existing Tax dropdown in the Inventory app\'s products.vue - since Product.taxId references a fixed Tax-slab master rather than a free-form percentage, the resolved rate is shown as a suggestion ("Resolved 5% (rule) - matches X") with an Apply link that selects the matching slab if one exists, rather than silently overwriting the field. Scope note, consistent with staged delivery rather than jamming everything into one stage: wiring "GST Validate" into the live Sale/Purchase invoice entry screens (spec sections 7.4/7.5) is deliberately deferred to Stage GST-7/8 (Sale/Purchase GST Review), which are the stages already scoped for exactly that line-level integration - this stage delivers the rate master and resolver those stages will consume, not a second, redundant wiring pass into revenue-critical live billing screens. Validated: dotnet build (0 errors, only pre-existing unrelated warnings), full backend test suite (72 passed, 0 regressions), clean books-web and inventory-web production builds (new route confirmed via compiled-bundle grep for books-web), validate-structure.mjs, and a dev-server console-error check on /gst-tax-rates (zero errors, correctly redirects to Login).'
} as const
