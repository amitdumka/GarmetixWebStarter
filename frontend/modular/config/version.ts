export const garmetixModularVersion = {
  version: '6.9.25',
  stage: 'Communication & Mail CM-08 - Business Integration',
  label: 'Communication & Mail: real, additive "send email" actions wired into Sales, Purchase, Payroll, Inventory and Administration',
  summary: 'CM-08 of the Garmetix Communication & Mail module (branch Mail-Com, not yet merged) - the first stage that actually calls EmailEnqueueService from outside the module. Every integration point is a brand-new endpoint added after an existing business transaction has already committed (never inside CreateSaleCoreAsync/CreateVendorPaymentVoucherAsync/GeneratePayslipsAsync/CreateUserAsync) - "provider failure must never roll back posting" holds by construction, not by wrapping existing code in try/catch. BusinessNotificationService centralizes the render+enqueue call (loads the published system template, renders via EmailTemplateRenderer, enqueues via EmailEnqueueService); a missing recipient email or unpublished template is a soft BusinessEmailResult skip, never an exception. EmailEnqueueService/EmailEnqueueRequest gained attachment support (a real gap found while researching this stage - the EmailAttachment table existed since CM-02 but nothing wrote to it) via a new CommunicationAttachmentStorageService.SaveEmailAttachmentAsync, and a genuine bug in EmailQueueItemProcessor was fixed along the way (it was reading EmailAttachment.StoredRelativePath as if it were already absolute, never resolving it through the storage root - would have thrown/silently sent attachment-less email on first real send). Sales (POST /sales/{id}/send-email, next to the existing send-whatsapp/pdf routes) and Payroll (POST /payslips/{id}/send-email) both attach a real PDF - Sales required extracting the existing invoice-PDF-model-building code out of DownloadInvoicePdfAsync into a shared BuildInvoicePdfModelAsync helper (pure refactor, no behavior change, confirmed by the unchanged passing test count) so the emailed PDF is byte-identical to the downloadable one; Payroll needed no refactor since PayrollService.GetPrintablePayslipAsync/PayrollPdfDocument.BuildPayslip were already standalone reusable calls. Purchase (POST /purchase/payments/{id}/send-email) and Inventory (POST /inventory/stock-reports/low-stock-alert/send-email, built on the same Risk-bucket calculation the Stock Reports screen already shows) are email-only - no dedicated vendor-payment-receipt or reorder-level PDF exists in this codebase, so a first pass stays text-only there rather than inventing new PDF documents. Administration (POST /access/users/{id}/send-invitation-email) reuses the exact same PasswordResetToken table and PasswordResetTokenService the real forgot-password flow already uses - an invitation is functionally "please set your password," so it completes through the same /reset-password verification path instead of a second token system; sent through the new queue, the legacy PasswordResetEmailService/IEmailSender stays completely untouched. Disclosed scope: these are backend "send email" actions only - no new frontend buttons were added to the 5 existing business pages to trigger them (a UI-wiring follow-up, not silently dropped) and Accounting\'s daily-summary/system-alert templates remain seeded-but-unwired since no natural "the day just closed" event exists yet to hang them on. Validated: dotnet build 0 errors/0 new warnings, backend suite 338 passed/0 failed/11 skipped (the unchanged pass count is itself the regression check for the BillingEndpoints.cs extract-method refactor). No merge, push, or deploy performed.'
} as const
