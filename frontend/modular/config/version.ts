export const garmetixModularVersion = {
  version: '6.9.11',
  stage: 'Swalekha - real Dashboard Layout shell (top bar + sidebar navigation)',
  label: 'Version6: Swalekha adopts the same Dashboard Layout as the main Garmetix apps',
  summary: 'Amit flagged a design issue right after the first live click-through: Swalekha\'s app.vue was a flat top bar with page-level nav buttons, not the same Dashboard Layout (top bar + collapsible sidebar + navigation system + Dashboard as the first/default page) every other Garmetix app uses. Rebuilt app.vue using the same UDashboardGroup/UDashboardSidebar/UDashboardPanel/UDashboardNavbar structure as packages/shared-ui/components/ModularAppShell.vue, reusing its shared garmetix-dashboard-* CSS classes for visual consistency - but with Swalekha\'s own 7-item nav (Dashboard/Accounts Hub/Contacts/Expenses/Income/Recurring Bills/Trips), no app-switcher dropdown, no cross-app search, and no link to or from any business app anywhere in it - the isolation guarantee from PersonalFin_01 is unchanged, only the visual shell now matches. pages/index.vue was rebuilt into a genuine Dashboard (net worth, owed-to-you/you-owe, spent, income, bills-due-this-month summary cards pulled from the real APIs, each linking into its section) now that page-level "go to X" buttons are redundant with the sidebar. Verified live: full sidebar nav renders correctly with real data, clicking a nav item navigates and updates the active state and page title correctly, zero console errors - confirmed via the accessibility tree at both a narrow (correctly collapses to an on-demand slide-over, matching the main app\'s own responsive behavior) and a 1280px desktop viewport (fixed, always-visible sidebar).'
} as const
