export const garmetixModularVersion = {
  version: '6.6.0',
  stage: 'Stage 14W: New Goods Return page (vendor-first, multi-invoice)',
  label: 'Version6 Stage 14W: New Goods Return page (vendor-first, multi-invoice)',
  summary: 'Amit asked for a brand new, separate Purchase Goods Return entry page (not a change to the just-rebuilt purchase-return/index.vue) that works like New Inward: pick a vendor first, then scan a barcode or search a product name to add return items, with rate/quantity/discount pulled from that vendor purchase history rather than one pre-selected invoice, plus Add/Edit/View/Delete including hard delete with full reversal, and refined freight handling (vendor-borne freight adds to the debit note at freight amount plus 5 percent GST; in-house freight books at the plain actual amount with no GST, as an Expense voucher). Confirmed the multi-invoice-spanning design point with Amit before building anything, since a single Goods Return can now legitimately consolidate items originally purchased on different invoices from the same vendor into one return and one debit note. Backend: added PurchaseReturn.FreightTaxAmount (migration plus DatabaseSchemaRepairService ALTER TABLE), a new vendor-scoped GET purchase/vendors/{vendorId}/returnable-items search endpoint (barcode exact match or product name contains, across every non-cancelled invoice for that vendor in the current store, sorted oldest invoice first), and a new POST purchase/vendors/{vendorId}/goods-return endpoint that consolidates lines from different source invoices into one PurchaseReturn header while each PurchaseReturnItem keeps its own correct PurchaseInvoiceId, recomputes status for every distinct invoice touched, and posts the freight GST split through an extended AccountingPostingService.PostPurchaseReturnAsync. Generalized GetReturnedQuantityLookupAsync into an invoice-set variant, fixed LoadPurchaseReturnAsync row-number and OriginalInvoiceNumbers lookups to span multiple source invoices, and generalized ReversePurchaseReturnAsync to recompute every affected invoice status instead of only the header single invoice - this keeps existing single-invoice returns on the untouched index page working exactly as before while correctly supporting the new multi-invoice case, and the existing POST purchase/returns/{id}/reverse endpoint is reused as-is for this new page Delete (hard delete) and Edit (reverse then reopen the form pre-filled for resubmission). Frontend: new page apps/main/pages/purchase-return/goods-return.vue at route /purchase-return/goods-return (registered in routes.ts and the Purchase sidebar group), with its own compact register plus View/Edit/Delete, and one small addition to the existing purchase-return/index.vue - a New Goods Return header button linking to the new page - with no other line of that file touched, honoring the explicit instruction that the current page stay as it is. Validated via dotnet build plus the full backend test suite (72 passed, 3 pre-existing Postgres-only tests skipped, zero regressions), a clean main-web production build confirming /purchase-return/goods-return prerenders with the same real app-shell byte size as the known-good /purchase-return page, validate-structure.mjs, a compiled-bundle grep confirming the new page content lands in its own route chunk, and a dev-server pass confirming the new route correctly redirects to login with zero console errors (Purchase is login-gated with no test credentials in this environment, consistent with every prior Purchase Return stage this session).'
} as const
