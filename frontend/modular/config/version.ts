export const garmetixModularVersion = {
  version: '6.9.8',
  stage: 'Swalekha PersonalFin_04 - Expense & Income',
  label: 'Version6: Swalekha Expense Sheets, Income and Recurring Bills',
  summary: 'PersonalFin_04 of the isolated Swalekha (Personal & Personal Finance) module. New SwalekhaExpenseSheet (a named grouping by free-text SheetType - Personal/House/Medical/Gifts/Hidden/custom), SwalekhaExpenseEntry (each individually flaggable IsHidden so a sensitive line can be excluded from default summaries regardless of sheet), SwalekhaIncomeEntry, and SwalekhaRecurringBill (a lightweight due-day reminder with a manual mark-paid stamp, not a full automation engine) domain models. All standalone from Accounts Hub in v1 - a deliberate, disclosed scope decision to avoid double-entry risk and keep this stage\'s build tractable; a future stage can add an optional payment-account link if reconciling expense/income against Accounts Hub balances is wanted. New SwalekhaExpenseEndpoints.cs (sheet + entry CRUD, a hidden/visible-aware paginated entry list, and a cross-sheet summary grouped by sheet type and category), SwalekhaIncomeEndpoints.cs, and SwalekhaRecurringBillEndpoints.cs (incl. mark-paid). SwalekhaSchemaRepairService extended with the four new tables. Frontend: new /expenses (summary cards, sheet table, create/edit) and /expenses/[id] (add-entry form with a hidden toggle, show-hidden switch, paginated entry table), /income (inline add form, paginated list), and /recurring-bills (bill table with a due-this-month badge and mark-paid action) pages, with dashboard nav updated.'
} as const
