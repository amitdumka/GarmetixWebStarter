export const garmetixModularVersion = {
  version: '6.9.5',
  stage: 'Swalekha PersonalFin_01 - isolated Owner-only module foundation',
  label: 'Version6: Swalekha (Personal & Personal Finance) foundation stage',
  summary: 'PersonalFin_01 of the new Swalekha module (design approved 2026-07-17, see docs/personal-finance-module-design.md). A fully isolated, Owner-userType-only module with its own swalekha_db PostgreSQL database (SwalekhaDbContext, own connection string, same shared API process), a new GarmetixPolicies.SwalekhaOwner backend policy (registered directly in Program.cs bypassing the shared Admin/Owner-permissive matrix, same pattern as the SuperAdmin-only factory-reset fix), and a brand-new frontend/modular/apps/swalekha Nuxt app with its own minimal top bar (not the shared ModularAppShell switcher) at port 3109 / path /swalekha. Deliberately excluded from the app switcher and every app.ts/appUrls registry; the only shared-code touchpoint is one small Owner-role-conditional link added to ModularAppShell.vue\'s profile dropdown (gated on a new one-off swalekhaUrl runtime config key, not the appUrls object the switcher itself reads). Deploy wiring (build_app, nginx location, --apps= allow-list) added to srp-whole-site-deploy.sh but not executed - no deploy performed this stage. This stage proves the isolation model end-to-end (separate DB, Owner-only auth, invisible switcher, one small entry point) before any real personal-finance feature is built in PersonalFin_02 onward.'
} as const
