export const garmetixModularVersion = {
  version: '6.9.42',
  stage: 'Sale/Accounting: Vyapar Cancelled-ignore fix, real PhonePe POS/EDC accounts, new EDC/POS Settlement Reconciliation page',
  label: 'Vyapar-cancelled invoices are now fully ignored (not just excluded), added real PhonePePos/PhonePeSpeaker accounts, and a new Books page reconciles delayed EDC/UPI bank settlements against sale receipts, net of processing charges',
  summary: 'Three closely-related fixes/additions from Amit\'s live feedback on the Vyapar import work. (1) A Vyapar[Cancelled] invoice never represents a real sale, so it is now skipped outright during preview parsing rather than surfaced with a warning in the review/return-adjustment bucket - confirmed against the real annual file, where the one genuine "Sale[Cancelled]" row (which happens to have zero Item Details lines, so it was already invisible either way) is fully ignored, and the change is verified isolated from the separate Sale Return/Credit Note detection path. (2) Created two real POS/EDC Machine bank accounts via the AccountType.PosMachine plumbing from the prior stage: PhonePePos (accepts UPI, Debit Card and Credit Card) and PhonePeSpeaker (UPI only, a PhonePe Smart Speaker device with no card reader) - both live, each with its own auto-created ledger under a new "PhonePe" Bank entry; the Vyapar import account dropdown now also shows each account\'s Branch/description hint inline so an operator can tell them apart. (3) New EDC / POS Settlement Reconciliation page (Books > Accounting): Card/UPI sale receipts post to a POS/EDC account\'s own clearing ledger on the sale date, but the aggregator\'s actual bank credit lands later as one lump sum, net of its processing charge - this page lists outstanding (unsettled) receipts for a chosen POS/EDC account as a manual checklist, lets the operator enter the settlement date/real bank account/net amount actually received, computes the charge (fee) live as gross-minus-net, and posts a balanced 3-line journal (Debit real bank net + Debit charges expense fee = Credit POS/EDC clearing gross) via a new AccountingPostingService.PostEdcSettlementAsync, reusing InvoicePayment\'s existing-but-previously-unused SettlementStatus field plus a new EdcSettlementBatchId link column and EdcSettlementBatch table. Includes a guarded Reverse action (explicit "REVERSE SETTLEMENT" confirmation) that unlinks the receipts and hard-deletes the journal, matching this project\'s established reversal-safety pattern. Validated: dotnet build (0 errors), full backend test suite (367 passed, 0 regressions), clean main-web and books-web production builds, validate-structure.mjs.'
} as const
