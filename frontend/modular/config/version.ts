export const garmetixModularVersion = {
  version: '6.9.39',
  stage: 'Sale: Invoice Books Adjustment (BookDate for GST filing period)',
  label: 'New Sale page: Invoice Books Adjustment - correct a Sale invoice\'s GST filing period without touching the invoice itself',
  summary: 'Adds a BookDate to every Sale invoice, separate from its real Sale Date (OnDate): BookDate is auto-set equal to OnDate at creation (GarmetixDbContext.StampInvoiceBookDates, a SaveChanges hook - no invoice entry form or create DTO was touched anywhere) and can only be changed afterward from the new, single-purpose Invoice Books Adjustment page. GST Returns (GSTR-1, GSTR-3B, HSN summary, invoice register - GstReturnEndpoints.cs) now filter and date Sale invoices by BookDate instead of OnDate, so an invoice created 4-Jun-2026 whose BookDate is moved to 19-Jul-2026 files under the July 2026 return; payments, accounting postings, Day Book and every other module keep using the real OnDate unchanged, exactly as scoped. Backend: Invoice.BookDate/BookDateUpdatedAt/BookDateUpdatedBy (migration 20260723090000_AddInvoiceBookDate.cs + idempotent DatabaseSchemaRepairService entry, backfilling existing rows from OnDate), a new dedicated GarmetixPolicies.InvoiceBookAdjustment policy (Owner/SuperAdmin/Admin via the standard bypass, plus Accountant only - matching the exact "owner, admin, accountant level access" ask), and InvoiceBookAdjustmentEndpoints.cs (GET a month-wise invoice list by Sale Date with each invoice\'s current BookDate, PUT to update only the BookDate field with a changed-by/changed-at audit stamp - no other invoice field is editable from this endpoint). Frontend: apps/main/pages/billing/invoice-book-adjustment.vue - month/year/store/search filters, a paginated register showing Sale Date alongside an inline-editable Book Date input per row with an Adjusted badge and last-changed-by/at, under the existing Sales sidebar group. Validated: dotnet build (0 errors), full backend test suite (367 passed, 0 regressions), clean main-web production build (new route prerenders with real content), validate-structure.mjs, and a dev-server console-error check (zero errors, correctly redirects to Login - no local test credentials in this environment).'
} as const
