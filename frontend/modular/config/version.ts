export const garmetixModularVersion = {
  version: '6.8.11',
  stage: 'Stage GST-10: E-Invoice/E-Way Bill placeholders',
  label: 'Version6 Stage GST-10: E-Invoice/E-Way Bill placeholders',
  summary: 'Tenth and final stage of the GST & Taxes module (see Stage GST-1 through GST-9 for schema/foundation, provider setup, GSTIN verification, HSN/SAC master, rate master, audit engine, Sale/Purchase GST Review, and ITC Register). Per the todo item, this is explicitly a placeholder stage: "gst_einvoice_irn_records/gst_ewaybill_records tables (not yet created), provider-ready structure, not configured messaging when no provider is set". Research confirmed Stage GST-1 already built the provider registry provider-ready for this - GstTaxCatalog.ProviderTypes already lists OfficialEInvoiceNIC/OfficialEWayBill and FeatureCodes already lists the full EINVOICE_*/EWAYBILL_* set - but no adapter that calls the real government NIC e-invoice/e-way bill portals exists (a genuine integration with signed-payload formats and portal-specific auth is a substantially larger effort than a placeholder stage; building one on invented assumptions about those APIs would be worse than not building it). Schema: two new tables (GstEinvoiceIrnRecords, GstEwaybillRecords - the latter with nullable InvoiceId/PurchaseInvoiceId mirroring GstAuditFinding\'s existing dual-link pattern, since e-way bills apply to both Sale and Purchase). Added the migration + hand-updated model snapshot + the DatabaseSchemaRepairService idempotent CREATE TABLE IF NOT EXISTS repair (the mechanism that actually matters on the SRP FreshBaseline host). New GstEinvoiceEwaybillEndpoints.cs (/api/gst/einvoice, /api/gst/ewaybill): GET list (B2B Sale invoices for e-invoice; Sale+Purchase invoices for e-way bill, both with Generated/NotConfigured status) and POST generate, which checks the provider registry for an enabled provider with the right feature and returns an honest message either way - "no provider configured" or "a provider is configured but live generation against the government portal is not implemented yet" - exactly mirroring the precedent Stage GST-2/3 already set for every provider type without a live adapter. Deliberately does not implement Cancel - cancelling only makes sense once live generation exists, so a cancel endpoint today would be untestable dead code. Wired the Stage GST-1 dashboard\'s two remaining explicit 0 placeholders (EInvoicePendingCount/EWayBillPendingCount) to real current-month counts (B2B invoices without a Generated IRN; invoices over the disclosed Rs 50,000 default e-way bill threshold without a Generated e-way bill). New page apps/books/pages/gst-tax-einvoice-ewaybill.vue: two sections (E-Invoice, E-Way Bill) each with filters, summary cards, a status table, and a Generate button per row that surfaces the honest not-configured/not-implemented message. Registered route /gst-tax-einvoice-ewaybill, added to Books GST & Taxes sidebar menu and dashboard quick actions. Validated: dotnet build (0 errors, incl. after the schema/dashboard changes), full backend test suite (72 passed, 3 pre-existing Postgres-only skipped, 0 regressions), clean books-web production build (new route confirmed via compiled-bundle grep), validate-structure.mjs, and a dev-server console-error check on /gst-tax-einvoice-ewaybill (zero errors, redirects to Login). This closes out Stage GST-1 through GST-10 - the GST & Taxes module is now code-complete and awaiting a deploy decision.'
} as const
