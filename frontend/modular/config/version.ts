export const garmetixModularVersion = {
  version: '6.9.0',
  stage: 'Final Accounts module deploy readiness - SRP schema repair + 9th app deploy wiring',
  label: 'Version6: Final Accounts module (balancesheet merge) deploy-ready',
  summary: 'Codex merged the "balancesheet" branch into version6 (PR #5, 22 commits): a complete new Final Accounts module - balance sheet/P&L/general ledger/CA adjustment workspace/period close/projection engine/Tally exchange package - living in its own dedicated "final_accounts" Postgres schema, gated off by default per workspace (FinalAccountsOptions.DefaultEnabled), with zero changes to any existing Sales/Purchase/Payroll/Billing endpoint. Before deploying, verified the module builds clean (dotnet build, 0 errors) and the full backend test suite passes (219 passed, 0 failed, 3 pre-existing Postgres-only skipped - up from 72, confirming every new FinalAccounts test file passes). Root-caused that the 9 new EF migrations would NOT apply automatically on the SRP host: this project applies schema via DatabaseSchemaRepairService.cs idempotent SQL on every API startup, not literal EF migration replay (confirmed the SRP host\'s __EFMigrationsHistory only recognizes InitialCreate, meaning ~80 prior migrations were never really tracked either - this is how schema has always shipped here, not a new gap). Generated an EF-verified idempotent DDL script (dotnet ef migrations script 20260623123000_InitialCreate 20260714150000_AddFinalAccountsExchangePackage --idempotent) and mechanically transformed it into the project\'s native IF NOT EXISTS repair style as a new RepairFinalAccountsStorageAsync method, wired into the existing RepairKnownSchemaDriftAsync orchestrator - creates all 31 final_accounts.* tables (fa_module_settings through fa_exchange_exceptions) with their indexes/foreign keys, with zero manual production database access. Added the missing final-accounts app (@garmetix/final-accounts-web, already registered in apps.ts by Codex\'s merge but never wired into the deploy script) to all 6 established per-app reference points in srp-whole-site-deploy.sh: usage/--apps= docs, base-path/URL vars, plan-print, nginx rewrite regex + location block, and the build_app call sequence.'
} as const
