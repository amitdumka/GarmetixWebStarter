export const garmetixModularVersion = {
  version: '6.9.24',
  stage: 'Communication & Mail CM-07 - Internal Inbox',
  label: 'Communication & Mail: internal conversations - compose, reply/reply-all, folders, read state, attachments, preferences',
  summary: 'CM-07 of the Garmetix Communication & Mail module (branch Mail-Com, not yet merged). CommunicationRecipientResolver resolves a compose/reply request\'s individual user ids and/or roles into a concrete, deduplicated recipient set at send time (role-based "broadcast" resolution is scoped through WorkspaceScope to the sender\'s own company/store and gated behind the CommunicationBroadcast policy; this codebase has no Department entity, so the master prompt\'s "departments" audience is scoped down to Role, the closest real concept here - documented as a deliberate scope adaptation). CommunicationEndpoints.cs adds compose/draft/send, reply and reply-all (reply-all targets every distinct sender+recipient the conversation has ever had), Inbox/Sent/Drafts/Archive/Trash with per-recipient read state (opening a conversation auto-marks the caller\'s own unread rows read), search, pagination, an unread-count badge endpoint, and per-user notification preferences. CommunicationAttachmentStorageService follows the PurchaseInvoiceImportService/SwalekhaDocumentEndpoints local-disk pattern (configurable storage root, per-company/date/owner subfolders, random stored file names, SHA-256 checksums, extension allow-list, 15MB cap) with an auth-gated download endpoint that re-checks the caller is the message\'s sender or a resolved recipient on every request, never trusting the URL alone. New Mailbox page (apps/communication/pages/mailbox.vue): folder tabs, search/unread-only filter, pagination, a compose modal (with a role-broadcast field shown only to Admin/PowerUser sessions), and a conversation slideover with inline reply/reply-all and attachment download. Scope simplification, disclosed rather than silently done: the spec lists inbox/sent/drafts/archive/trash/compose/conversation as separate routes - this stage ships them as one comprehensive Mailbox page with folder tabs and a slideover instead of seven separate URL routes, to keep the surface manageable; the functional requirements (drafts, replies, archive/trash, read state, search, pagination, attachments) are all covered. Validated: dotnet build 0 errors/0 new warnings, backend suite 338 passed/0 failed/11 skipped (zero regressions - no new endpoint-integration tests added this stage, consistent with this repo\'s established pattern of not integration-testing minimal-API endpoint handlers directly), clean communication-web build (all 6 routes prerender with real content), validate-structure.mjs passing.'
} as const
