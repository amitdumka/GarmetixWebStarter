export const garmetixModularVersion = {
  version: '6.9.43',
  stage: 'Sale: Vyapar Import confirm silently hangs on large batches - fixed',
  label: 'Vyapar import "Approve & Import" appeared to do nothing for large batches (390-553 invoices in one request) - now chunks the confirm call with visible progress, and two silent-failure paths now show a real error',
  summary: 'Amit reported clicking "Approve & Import" on a real Vyapar import (390+ invoices) did nothing - no error, no result, no visible feedback. Root-caused three compounding issues in apps/main/pages/billing/vyapar-import.vue, none of which had been exercised by this feature\'s prior testing since no real large file was available until now. (1) The whole invoice set was posted to POST /api/sale-import/vyapar/confirm in one HTTP request, wrapped server-side in a single all-or-nothing DB transaction across every invoice (stock movements, payments, accounting postings) - for hundreds of invoices this can genuinely take minutes with the button just showing a spinner and no progress text, which reads as "stuck." Confirmed the backend already supports repeated confirm calls sharing one ImportBatchId to group them, so the frontend now splits large batches into 40-invoice chunks and posts them sequentially, aggregating results and showing "Importing X of Y (chunk N of M)..." progress live in the modal - a failure partway through only loses the in-flight chunk, not everything already committed. (2) confirmImport() had a silent early return (`if (!preview.value || !selectedStore.value) return`) with zero user feedback if selectedStore failed to resolve - now shows a specific error explaining what to do instead of silently no-op\'ing. (3) The error alert only rendered at the top of the page, invisible behind the still-open approval modal on failure - now also shown inside the modal itself. Validated: clean main-web production build, validate-structure.mjs, compiled-bundle grep confirming the new strings landed correctly (this exact file has a history of two prior dangling-reference/syntax hotfixes, so this was checked deliberately). Could not click-test the actual large-batch import end to end - browser file-upload tooling was unavailable in this environment - so this is a careful, high-confidence code fix based on root-causing the exact reported symptom, not a live-reproduced-and-confirmed-fixed bug.'
} as const
