export const garmetixModularVersion = {
  version: '6.9.22',
  stage: 'Communication & Mail CM-05 - Provider Administration',
  label: 'Communication & Mail: multi-provider email registry, encrypted credentials, and a new isolated Communication & Mail app',
  summary: 'CM-05 of the Garmetix Communication & Mail module (branch Mail-Com, not yet merged). Adds a new dedicated Nuxt app (frontend/modular/apps/communication, registered in apps.ts/routes.ts/ModularAppShell.vue like Books or Inventory, not excluded from the switcher like Swalekha) with a Providers page: full CRUD for Brevo API and SMTP email providers (with Brevo/GoDaddy/Microsoft 365/Gmail/Custom/local-Postfix presets), masked/encrypted credential storage with retain-or-rotate semantics, enable/disable/set-default, connection test, and send-test-email - all built on the CM-01-04 domain/queue/provider-client foundation from earlier stages this same pass. Backend: EmailProviderEndpoints.cs mirrors the GST module\'s provider-admin pattern exactly. New GarmetixPolicies.Communication/CommunicationBroadcast/CommunicationTemplates/CommunicationProviders/CommunicationQueue/CommunicationSuppression policies wired into AccessPermissionMatrix (base Communication access for every staff role; the five admin-tier policies for Admin/PowerUser only). Every existing app\'s nuxt.config.ts gained the NUXT_PUBLIC_GARMETIX_COMMUNICATION_URL appUrls key so the switcher actually offers the new app (avoiding the historical Inventory-switcher-omission bug). Validated: dotnet build 0 errors/0 new warnings, backend suite 330 passed/0 failed, clean communication-web build (all 4 routes prerender with real content) and a books-web rebuild confirming the appUrls edits caused no regression.'
} as const
