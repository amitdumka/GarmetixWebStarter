export const garmetixModularVersion = {
  version: '6.7.0',
  stage: 'Stage GST-1: GST & Taxes module foundation',
  label: 'Version6 Stage GST-1: GST & Taxes module foundation',
  summary: 'Started the new GST & Taxes module (multi-provider GST API architecture, encrypted credentials, HSN/rate master, GST audit engine, GSTIN cache, CA export) per Amit\'s full module spec. This first stage lays the foundation: 11 new backend tables (gst_api_providers/features/credentials, gst_api_call_logs, gstin_verification_cache, gst_hsn_master, gst_tax_rate_rules, gst_state_codes, gst_uqc_codes, gst_audit_rules, gst_audit_findings), a first-ever ASP.NET Core Data Protection based credential-encryption service (GstCredentialProtector), a GET /api/gst/dashboard endpoint plus a read-only GET /api/gst/providers, a new Gst authorization policy (also closing a pre-existing gap where the "GST" module string was never actually wired into AccessPermissionMatrix.CanAccessPolicy), and startup seed data (Indian GST state codes, common UQC codes, the 17-rule audit catalog, a default garment 5%/18% price-threshold rate rule, and a "Local Master" provider so the module works with zero external API setup). Confirmed via research that this app already has /api/gst-returns (GSTR-1/3B/HSN summary/CA review-send) and /api/gstin (single env-configured GSTIN lookup) - this stage builds the genuinely new multi-provider/audit/master-data layer and links out to what already exists rather than duplicating it. Frontend: renamed the Books "GST" sidebar group to "GST & Taxes" and added a new GST & Taxes Dashboard page (apps/books/pages/gst-tax-dashboard.vue) as its landing page, with empty/zeroed cards for metrics that later stages (Sale/Purchase GST Review, Returns wiring) will populate. Validated: dotnet build (0 errors), full backend test suite (72 passed, 3 pre-existing Postgres-only skipped, zero regressions), clean books-web production build. Remaining stages (API Setup UI, GSTIN verification UI, HSN/Rate Master UI, Audit engine + UI, Sale/Purchase GST Review, Returns/CA export UI, E-Invoice/E-Way Bill placeholders) are tracked in .claude/todo.md and will land as separate stages, matching this project\'s established staged-delivery pattern.'
} as const
