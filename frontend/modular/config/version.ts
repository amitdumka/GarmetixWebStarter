export const garmetixModularVersion = {
  version: '6.9.19',
  stage: 'Swalekha PersonalFin_13 - Personal Organizer (Diary/Notes/Calendar)',
  label: 'Version6: Swalekha adds a Journal, Personal Notes, and a Calendar that auto-surfaces finance due-dates',
  summary: 'Starts Pillar B - the module\'s second major half, alongside Pillar A\'s Personal Finance stages. New Diary/Journal (dated entries, one per day by convention, already private by construction via PersonalFin_06\'s Owner-only isolation - no separate visibility flag needed) and Personal Notes (topic-based, Folder + freeform Tags, pin/search) - both simple CRUD, deliberately distinct: dated timeline vs. topic-organized. New Calendar &amp; Appointments: manually-added SwalekhaAppointment rows plus - the ask\'s explicit "auto-populated finance due-dates" - a merged GET /api/swalekha/calendar endpoint that computes and surfaces due-dates live from Pillar A for the requested month (active Recurring Bills projected via DueDayOfMonth, open FD/RD maturities, active Insurance policies\' NextPremiumDueDate) without duplicating any of that data into a stored table, matching the computed-not-stored pattern PersonalFin_04/12 already established for DueThisMonth/NextPremiumDueDate. New /journal, /notes and /calendar pages (the Calendar as a month-navigable agenda list, color-coded by event type). Live-verified end to end through the actual UI and direct API checks: created a real journal entry and a real pinned/tagged/foldered note (search by tag correctly matched), added a real appointment via the UI, and created a real Recurring Bill via the API - the calendar correctly merged both the manually-added appointment and the bill\'s due-day-projected date onto the same day, sorted correctly, with the right type badge and amount on each.'
} as const
