export const garmetixModularVersion = {
  version: '6.2.5',
  stage: 'Stage 14Q.6 Purchase Import: correction-safety, cleanup-history, multi-file UI',
  label: 'Version6 Stage 14Q.6 Purchase Import: correction-safety, cleanup-history, multi-file UI',
  summary: 'Closed the remaining deferred Purchase Import frontend gaps - all three backend endpoints already existed and were fully implemented, this is purely new frontend wiring. (1) import-acceptance.vue: the row-level "request correction" action now opens a Correction Safety Check panel first (GET correction-safety - shows canDeleteDraft/canRejectDraft/directUndoAvailable/safe actions/warnings) before the operator proceeds to the existing request-correction call, instead of jumping straight to a window.prompt. (2) import-acceptance.vue: added a Cleanup History panel (older-than-days + status checkboxes + delete-files toggle, confirm dialog, POST cleanup-history) for bulk-deleting old unposted failed/rejected/needs-review/ready-to-post import drafts. (3) import.vue: added a Files button/modal next to View Proof that lists every stored file for a batch (GET batches/{id}/files) with a per-file Download button (GET batches/{id}/files/{fileId}/download via openBlob) - previously only the single original proof file was reachable from the UI. Also confirmed two other items from the same deferred list were already resolved before this stage: .gitattributes already has a blanket text=auto rule (added 2026-07-07), and the factory-reset SuperAdmin route-level gating was fixed in the prior v6.2.4 stage. Zero backend changes. Verified via a clean main-web production build (both routes prerender with real content) and validate-structure.mjs.'
} as const
