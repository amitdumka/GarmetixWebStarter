export const garmetixModularVersion = {
  version: '6.2.6',
  stage: 'Stage 14R Books: Ledger/Parties pagination + dedicated Banking page',
  label: 'Version6 Stage 14R Books: Ledger/Parties pagination + dedicated Banking page',
  summary: 'Two Books UX changes requested directly by Amit. (1) Added client-side pagination (page-size selector + prev/next, resets on filter change) to the Ledger table (ledgers.vue), the Party Register and Internal Party Masters tables (parties.vue) - all three already had a search-box filter but no pagination, matching the existing unpaginated-backend-endpoint constraint (ledgers/parties/customers/vendors have no page/pageSize support server-side, same as every other MapCrud<T> list endpoint in this codebase). Added a shared pageSizeOptions/paginateRows helper to books-api.ts. (2) Split every banking-related tab (Bank Accounts, Bank Transactions, Bank Reconciliation, Cheque Log, Vendor Bank Accounts, Account Details/net-banking logins) out of accounting.vue into a new dedicated /banking page with its own "Banking" sidebar menu item (routes.ts + ModularAppShell.vue localMenus), mirroring the existing Ledgers/Parties/Vouchers pattern - accounting.vue now only shows the read-only Trial Balance. Bank Transactions, Bank Reconciliation and Cheque Log (the three tabs Amit specifically called out) also gained the same pagination pattern as banking.vue is a single shared-table page like the old accounting.vue was. Fixed four Books validation scripts (books-accounting-contract-check.mjs, books-parity-baseline.mjs, books-ledger-sync-readiness.mjs, books-voucher-ledger-parity.mjs) whose marker checks pointed at accounting.vue for content that actually lives in ledgers.vue (a pre-existing drift from an earlier, unrelated ledger-split stage, only surfaced now) or needed a new banking.vue entry. Zero backend changes. Verified via clean books-web production build (all routes incl. /banking prerender with real content), all four Books validation scripts passing, validate-structure.mjs, and a dev-server check confirming /banking correctly redirects to login with no console errors and "banking" baked into the compiled bundle (Books has no test credentials in this environment, consistent with prior stages).'
} as const
