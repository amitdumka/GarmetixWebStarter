export const garmetixModularVersion = {
  version: '6.8.10',
  stage: 'Stage GST-9: ITC Register',
  label: 'Version6 Stage GST-9: ITC Register',
  summary: 'Ninth stage of the GST & Taxes module (see Stage GST-1 through GST-8 for schema/foundation, provider setup, GSTIN verification, HSN/SAC master, rate master, audit engine, and Sale/Purchase GST Review). Research first: the todo item read "GSTR-1/3B/HSN summary/ITC register CA Excel export" - confirmed the pre-existing GST Returns module (/api/gst-returns, GstReturnEndpoints.cs/GstReturnExportService.cs) already fully covers GSTR-1/GSTR-3B/HSN summary/tax-summary/invoice-register with CSV and CA-ready Excel export, so building those again would duplicate working infrastructure. The one genuine gap found: the existing GSTR-3B builder\'s "4 ITC" section is entirely manual entry - its own Excel export literally notes "Manual/separate GST module export. Not linked to Billing/Purchase yet." This stage closes that gap with a real ITC Register auto-computed from posted Purchase invoices. Backend: extracted the Stage GST-8 ITC eligibility heuristic out of GstPurchaseReviewEndpoints.cs into a new shared GstItcEligibilityService.cs (Eligible/Unverified/At Risk/Not Eligible, same rules, same wording) so Purchase GST Review and the new register can never disagree - refactored GstPurchaseReviewEndpoints.cs to call the shared service instead of its own private copy. New GstItcRegisterEndpoints.cs (/api/gst/itc-register, read-only, GarmetixPolicies.Gst): GET (paginated JSON), GET /csv, and GET /excel - the Excel export reuses the existing SimpleXlsxBuilder/XlsxSheet OOXML writer from GstReturnExportService.cs (building on existing infrastructure, not duplicating an Excel writer) for a Summary sheet plus a full ITC Register sheet. Each row is a Purchase invoice with taxable value/CGST/SGST/IGST/tax (same NetAmount/CGST-SGST-IGST-fallback pattern as the existing GST Returns invoice-register report, for consistency), its ITC status, and an "eligible ITC amount" (full tax when Eligible/Unverified, zero when At Risk/Not Eligible). Capped at 2000 rows per request. New page apps/books/pages/gst-tax-itc-register.vue: filters (date range, store, vendor, search), a totals summary row plus a status-breakdown row (Eligible/Unverified/At Risk/Not Eligible counts), a paginated register table, and Export CSV / CA Excel Export buttons wired through the existing shared download() helper in books-api.ts (auth token + Content-Disposition filename handling, reused rather than reimplemented). Explicitly does not touch or replace the existing manual GSTR-3B ITC block - the new register is documented, on the page itself, as a source to cross-check those manually-entered numbers against. Registered route /gst-tax-itc-register, added to Books GST & Taxes sidebar menu and dashboard quick actions. Validated: dotnet build (0 errors, incl. after the GstPurchaseReviewEndpoints refactor), full backend test suite (72 passed, 3 pre-existing Postgres-only skipped, 0 regressions), clean books-web production build (new route confirmed via compiled-bundle grep), validate-structure.mjs, and a dev-server console-error check on /gst-tax-itc-register (zero errors, redirects to Login).'
} as const
