export const garmetixModularVersion = {
  version: '6.9.41',
  stage: 'Sale: Vyapar Import payment mapping - real-data validation pass',
  label: 'Vyapar Sale Import payment-kind bifurcation validated against two real production Vyapar exports; fixed a real label-drift bug and a Cancelled-invoice gap the data surfaced',
  summary: 'Amit supplied two real Vyapar Sale Report exports from his live business (a quarter and a full financial year, 225 and 475 invoices). Uploaded both directly to the live SRP preview endpoint to close out the disclosed "no sample file" gap from the prior stage. Found and fixed two real issues neither the code review nor the test suite had caught: (1) GuessPaymentKindLabel independently re-guessed the payment kind from source-name text, which drifted from the already-resolved PaymentMode on real data - "SBI Aadwika Fashion AMY" (121 real invoices, the business\'s second-most-used payment source) is correctly resolved to PaymentMode.UPI by the existing GuessPaymentMode keyword list ("amy"), but the independent text-only label guesser did not know that keyword and showed "Other". Redesigned GuessPaymentKindLabel to derive from the already-resolved PaymentMode instead of re-guessing it, so this class of drift is now structurally impossible - confirmed fixed against the real file (now correctly shows "UPI"). (2) The annual file surfaced a real "Sale[Cancelled]" Vyapar Transaction Type Amit\'s account had actually produced - added an explicit PaymentStatus == "Cancelled" check (the same row also carries this in the existing PaymentStatus column) folded into the same auto-import exclusion as Sale Return/Credit Note. Also deliberately verified NOT changing anything: 6 real invoices whose free-text Description mentions "credit note"/"sale return" (payment partially adjusted against an earlier return, not a return themselves) correctly stayed in the normal import buckets - including one, "No Return on any product as Spl discount given", that would have been a false positive under a naive Description-text scan, confirming the decision to key detection off Vyapar\'s structured Transaction Type column only, not free text, was correct. Neither real file contained an actual Transaction-Type-marked Sale Return row, so that specific exclusion path remains logically validated but not live-fire-tested - disclosed honestly. Did not call the confirm/import endpoint against either real file - that would post real invoices into production and needs explicit approval first. Validated: dotnet build (0 errors), full backend test suite (367 passed, 0 regressions), and two live preview uploads against the real SRP API (200 OK, correct invoice/line/payment-source counts, zero errors).'
} as const
