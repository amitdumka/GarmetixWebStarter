export const garmetixModularVersion = {
  version: '6.8.13',
  stage: 'Payslip View/Edit/Delete, without cascading to salary payments',
  label: 'Version6: Payslip View/Edit/Delete, without cascading to salary payments',
  summary: 'Amit reported that a deduction added during salary payment entry does not carry through to the payslip, so the due amount keeps showing an outstanding balance that should have been absorbed by the deduction, and asked for payslip View/Edit/Delete so a wrong calculation can be corrected directly, with delete explicitly not cascading to any salary payment already recorded. Root cause: the payslip Deductions/NetSalary fields (which the due calculation always reads live) are set once by Generate Payslips from the salary structure baseline, and there was no way to edit them afterward - the only place a deduction figure could be adjusted was the Salary Payment entry forms own Deductions field, which is a snapshot saved only on that one payment row and never written back to the payslip, so the due figure computed from the untouched payslip stayed wrong. The fix Amit asked for is the direct one: give the payslip its own Edit so the actual deduction fields can be corrected at the source, which the due calculation already reads live. Added GET/PUT/DELETE /api/payroll/payslips/{id} (PayrollEndpoints.cs, PayrollService.cs) - GET/PUT reuse the existing PayslipPrintDto shape, PUT lets every earning/deduction line item and remarks be corrected, DELETE soft-deletes the SalaryPaySlip row only and deliberately never touches SalaryPayments - a payslip is a calculation snapshot, payments already made and posted to accounting against it stay intact regardless of what happens to the payslip afterward. Also closed three pre-existing !Deleted gaps found while touching this code: GetRecentPayslipsAsync, GeneratePayslipsAsync existing-payslip lookup, and CalculateCarryForwardDueAsync previous-payslip lookup all queried SalaryPaySlips with no soft-delete filter, so a deleted payslip would still show in lists or count toward next months carry-forward due. Frontend: apps/hr/pages/payroll.vue Payslips tab gained View (a read-only earnings/deductions/due detail modal) and Edit (a form for the same fields the payslip stores, matching the existing Salary Structure edit forms shape) and Delete (guarded confirm, matching the existing Structures/Payments tabs pattern) buttons, alongside the existing Pay/PDF/Email/WhatsApp actions. Validated: dotnet build (0 errors, same 7 pre-existing unrelated warnings), full backend test suite (72 passed, 3 pre-existing Postgres-only skipped, zero regressions), clean hr-web production build, validate-structure.mjs, and a dev-server console-error check on /payroll (zero errors, correctly redirects to Login - HR is login-gated with no test credentials in this environment).'
} as const
