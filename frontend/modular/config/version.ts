export const garmetixModularVersion = {
  version: '6.1.0',
  stage: 'Stage 14Q Purchase Module Port (Phase 1)',
  label: 'Version6 Stage 14Q Purchase Module Port (Phase 1)',
  summary: 'Ported the legacy Purchase menu group (frontend/legacy/garmetix-web, live reference garmetix.aadwikafashion.in/purchase) into modular, which previously had only stub/placeholder pages and two read-only Books review pages. Added a new Vendors CRUD page (main app - GSTIN lookup, create/edit/delete popups, filter+pagination; this page did not exist before). Rebuilt the Purchase Register (/purchase) from a read-only recent-list into a full paginated/filtered register with View/Edit/Pay/Cancel/Delete popups. Built a dedicated New Inward page (/purchase/new) replacing its placeholder stub - vendor + barcode/product item entry + payment, posts to the existing purchase/inward endpoint. Built the Purchase Return page (/purchase-return) replacing its placeholder stub - invoice search, per-item returnable-quantity entry, and a return register with view detail. Upgraded Vendor Payments and Vendor Settlements (books app) from "Read only" to full write workflows: payments gained create (against invoice or as vendor advance)/edit/delete plus server pagination; settlements gained a Debit-Notes-Available-For-Settlement register and a Settle popup with invoice allocation and refund handling. Fixed a dangling-route sidebar bug matching the earlier HR/Books pattern - vendor-payments and vendor-settlements had routes but no ModularAppShell sidebar entry at all. Zero backend/DB changes - every endpoint used already existed. Deferred to a later stage: the Scan/Import Supplier Invoice OCR workflow and its two satellite QA/learning pages (large, backed by an existing but unexposed 4060-line backend service), plus two independent read-only QA dashboards (Vendor Payable Reconciliation, Purchase Return Advanced Settlement) - see .claude/todo.md and .claude/roadmap.md for the itemized list.'
} as const
