export const garmetixModularVersion = {
  version: '6.3.0',
  stage: 'Stage 14T: Purchase Return barcode entry, current stock, transport/freight',
  label: 'Version6 Stage 14T: Purchase Return barcode entry, current stock, transport/freight',
  summary: 'Amit asked for a richer Purchase Return entry form: barcode scanning, current-stock visibility, a Goods Return print showing the original invoice row number/invoice number/date/days-old, transport details, and freight charges that either bill back to the vendor (added to the auto-created debit note) or book as an in-house Expense voucher depending on who bears the cost. Confirmed both design decisions with Amit before building (current-stock display purpose, freight vendor-vs-in-house mechanism). This is a genuine schema change plus real accounting-postings logic, not just a frontend addition. Backend: added TransportDetails/FreightAmount/FreightBearer/FreightExpenseVoucherId/FreightExpenseVoucherNumber to the PurchaseReturn entity (migration 20260711090000_AddPurchaseReturnFreightFields.cs + model snapshot + DatabaseSchemaRepairService.cs ALTER TABLE entries, since the SRP host does not apply normal EF migrations - see learnings.md). GET purchase/invoices/{id}/returnable now returns a stable RowNumber (ordered by original invoice-line CreatedAt) and live CurrentStockQuantity per line (previously computed only at submit time, never surfaced). POST .../partial-return accepts TransportDetails/FreightAmount/FreightBearer/FreightEmployeeId: when FreightBearer=Vendor, the freight amount is added to the auto-created debit note (Amount+TaxableAmount) and posted as an extra vendor-debit / freight-ledger-credit journal pair (mirroring the exact inward-time freight ledger, "Transport & Freight Charges"); when FreightBearer=InHouse, an Expense voucher is auto-created in the same DB transaction via the existing (previously only used internally) SaveVoucherInCurrentTransactionAsync, requiring an explicit "issued by" employee since there is no current-user-to-employee mapping in this codebase. GET purchase/returns/{id} and the return PDF (PurchaseReturnPdfDocument.cs) now show the row number prefix per line, "days old" next to the purchase date, and a transport/freight summary line. Frontend (purchase-return/index.vue): added a barcode scan field that jumps to and highlights the matching returnable line, a Current Stock column, Transport Details/Freight Amount/Freight Borne By fields with a conditional Issued-By employee picker (lazy-loads GET employees only when In-house is selected, filtered to active employees via the existing isActiveEmployee helper), and surfaced row number/invoice date/days-old/transport/freight in the return detail slideover. Validated via a clean dotnet build + full backend test suite (75 passed, 3 pre-existing Postgres-only tests skipped - includes an updated/expanded PurchaseReturnDocumentTests.cs asserting the new PDF content), a clean main-web production build (/purchase-return prerenders with real content), validate-structure.mjs, and a dev-server console-error check. No live click-through possible (Purchase is login-gated, no test credentials in this environment) - this is a materially larger untested financial-posting surface than most stages this session (new debit-note amount adjustment, new auto-voucher creation path, new journal lines) built from a careful direct read of every touched DTO/entity/posting method rather than live testing; flagged explicitly for extra scrutiny on first real use.'
} as const
