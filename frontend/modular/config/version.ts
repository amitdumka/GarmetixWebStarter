export const garmetixModularVersion = {
  version: '6.9.20',
  stage: 'Swalekha PersonalFin_14 - Dashboard & Reports',
  label: 'Version6: Swalekha\'s Dashboard now shows real Net Worth aggregated across every module',
  summary: 'The Dashboard is rebuilt into the real "unified home" the design asked for. GET /api/swalekha/dashboard computes a genuine Net Worth by live-aggregating every entity built across PersonalFin_02-13 - Accounts (CurrentBalance), Fixed/Recurring Deposits, Mutual Funds and Shares (both at current market value when a NAV/price has been set, falling back to invested amount otherwise), Other Assets, and Contacts receivables as assets; Loans Outstanding and Contacts payables as liabilities. FD/RD have no stored "current accrued value" field, so this is a deliberate, disclosed scope decision rather than a fabricated number: FDs count at PrincipalAmount and RDs at MonthlyInstallment x InstallmentsPaid (amount invested so far), both labelled as such in the breakdown. A new rolling 30-day Upcoming Dues feed generalizes PersonalFin_13\'s Calendar due-date computation (which only projects one calendar month at a time) to correctly span a month boundary - reusing the same Recurring Bill/FD-RD-maturity/Insurance-premium sources. Also added Today\'s Appointments and a CSV Net Worth &amp; Investments export (GET /api/swalekha/dashboard/export, an explicit scope decision to build a plain CSV rather than reinvent this codebase\'s one existing but write-only .xlsx OOXML builder for a new module). The Dashboard page now shows real Net Worth/Assets/Liabilities breakdowns, Upcoming Dues, Today\'s Appointments, and the existing Expense Breakdown (reusing PersonalFin_04\'s /expenses/summary endpoint as-is rather than duplicating its aggregation logic). Live-verified end to end through the actual UI against real data accumulated across every prior stage this session: Net Worth came back as exactly Rs 5,64,270.00 (Rs 6,64,270 assets - Rs 1,00,000 liabilities), with every single line item - Accounts Rs 5,09,750, Recurring Deposits Rs 500, Mutual Funds Rs 3,300, Shares Rs 720, Other Assets Rs 1,50,000, Loans Outstanding Rs 1,00,000 - matching the real test data from PersonalFin_08 through 13 exactly; Upcoming Dues correctly merged a real Recurring Bill and a real Appointment onto the same day; the CSV export downloaded a valid, correctly-quoted file with matching totals.'
} as const
