export const garmetixModularVersion = {
  version: '6.1.1',
  stage: 'Stage 14Q.1 Purchase Module QA Dashboards (Phase 2a)',
  label: 'Version6 Stage 14Q.1 Purchase Module QA Dashboards (Phase 2a)',
  summary: 'Ported the two independent read-only QA dashboards left over from the Stage 14Q Purchase module port: Vendor Payable Reconciliation (/purchase/vendor-payable-reconciliation) and Purchase Return Advanced Settlement QA (/purchase-return/advanced-settlement), both in the main app. Both call pre-existing, fully-built backend reconciliation endpoints (VendorPayableReconciliationEndpoints.cs, PurchaseReturnAdvancedSettlementEndpoints.cs) that already computed metrics/issues/evidence rows/checklists - zero backend changes, purely a new frontend surface mirroring the established modular QA-dashboard pattern (accounting-gst-validation.vue). Both pages support a date range filter, paginated evidence tables, and CSV evidence export. Along the way, fixed a page-routing hazard while adding the settlement QA route: the existing apps/main/pages/purchase-return.vue was a top-level file with the same name as the new purchase-return/ directory it now needed to share - Nuxt requires the parent-named file to act as a layout wrapper for anything nested under it, so left as-is the new nested route would have silently failed to render. Moved it to purchase-return/index.vue (matching the codebase\'s existing convention for every other page with siblings, e.g. credit-notes/index.vue, debit-notes/index.vue) so /purchase-return and /purchase-return/advanced-settlement are true sibling routes. Still deferred: the Scan/Import Supplier Invoice OCR workflow and its two satellite pages (Import Acceptance QA, Import Learning) - see .claude/todo.md.'
} as const
