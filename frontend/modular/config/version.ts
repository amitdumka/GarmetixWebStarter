export const garmetixModularVersion = {
  version: '6.9.40',
  stage: 'Sale: Vyapar Import payment mapping - Cash/EDC/Card bifurcation + return detection',
  label: 'Vyapar Sale Import: Cash can map to a bank account, Debit/Credit Card force POS-EDC settlement, UPI chooses Bank or POS-EDC, Sale Return/Credit Note rows are detected and excluded from auto-import',
  summary: 'Closes gaps Amit found in the Vyapar Sale Import payment-mapping step (Step 3). (1) Cash sources are no longer force-skipped ("Cash - no mapping needed") - they get an optional bank-account selector; if left blank, behavior is unchanged (posts to Cash In Hand); if mapped, NormalizeConfirmPayments now routes that payment through the mapped account (PaymentMode switches from Cash to NEFT only in that case, since PaymentMode.Cash always resolves to the Cash-In-Hand ledger regardless of BankAccountId downstream). (2) New AccountType.PosMachine enum value (backward compatible, no migration - enums are int columns) lets a business tag a Bank Account as a POS/EDC Machine settlement account from Books > Banking (accountTypeOptions in books-api.ts). (3) Each payment source now carries a guessed, purely descriptive PaymentKindLabel (Cash/Credit Card/Debit Card/UPI/Cheque/Bank Transfer/Other, computed by GuessPaymentKindLabel from the Vyapar source name+description) - the UI splits Card into Credit/Debit and forces both to the POS/EDC account list only; UPI gets a Bank Account vs POS/EDC Machine toggle (defaults to Bank); Cheque/Other stay Bank-account-only. The underlying PaymentMode enum (Cash/Card/UPI/Cheque) and accounting posting logic are unchanged for Card/UPI - only which BankAccountId list the operator picks from changes. (4) Sale Return/Credit Note detection: Vyapar rows whose Transaction Type contains "Return" or "Credit Note" were previously either silently dropped (excluded by the old exact "Contains Sale" filter) or silently imported as ordinary positive sales - both parsers (Sale Report, Item Details) now explicitly allow these rows through, flag them via a new IsReturnOrAdjustment DTO field, add a header warning, and force them out of both the Fully Matched and Ready For Import buckets so they can never be auto-posted as a normal sale; the UI shows a "Return / Adjustment" summary card, invoice filter, and a per-line badge. Full per-invoice return matching against the original invoice is deliberately deferred, per Amit\'s own "later will [be] matched for each inv after import" framing - this stage is detection and exclusion, not reconciliation. Validated: dotnet build (0 errors), full backend test suite (367 passed, 0 regressions), clean main-web and books-web production builds, validate-structure.mjs.'
} as const
