export const garmetixModularVersion = {
  version: '6.8.0',
  stage: 'Stage GST-2: GST API Setup UI',
  label: 'Version6 Stage GST-2: GST API Setup UI',
  summary: 'Second stage of the new GST & Taxes module (see Stage GST-1 for the schema/foundation entry): full GST API provider CRUD, encrypted-credential management, feature/priority/fallback mapping, and test tools, all from the UI - no environment variables. Backend: extended GstTaxEndpoints.cs with GET/POST/PUT/DELETE /api/gst/providers, enable/disable, a GET /api/gst/providers/catalog (provider types/environments/feature codes/credential keys, so the frontend never hardcodes these lists separately), GET/PUT /api/gst/providers/{id}/credentials (encrypts on save via GstCredentialProtector, returns only masked values and a hasValue flag, decrypted secrets are never sent to the frontend), and test-health/test-gstin/test-hsn endpoints that work fully for the seeded Local Master provider (checking local HSN/GSTIN-cache tables) and return an honest "arrives in a later stage" message for every other provider type rather than pretending to call a live API that has no adapter yet - every test attempt is logged to GstApiCallLogs. Mutating provider/credential endpoints are gated at GarmetixPolicies.Admin (Admin/Owner/SuperAdmin only), matching the spec\'s "only admin can manage credentials" requirement. Frontend: new apps/books/pages/gst-tax-setup.vue (provider table with Edit/Enable-Disable/Delete - delete requires typing DELETE PROVIDER, matching this codebase\'s guarded-write convention - and a create/edit modal covering provider fields, feature-mapping checkboxes, priority/fallback, a credentials panel with per-key masked-value display and Save/Clear, and inline GSTIN/HSN/health test buttons with results shown live). Registered as route /gst-tax-setup (Owner/Admin roles only) and added to the Books "GST & Taxes" sidebar group and the dashboard\'s quick actions. Also added a "GST API Configuration" cross-app link card to the Admin app\'s existing Company Setup page (/setup), per the spec\'s instruction to surface this from the existing app-setup page - uses the same appUrls runtime-config cross-app-link pattern already established for the app switcher. Caught and fixed a real bug before shipping: the shared BooksMasterTable component only supports flat string columns plus one generic "actions" slot (no per-cell slot overrides like #cell-isEnabled), so the first draft\'s badge/action slots would have silently rendered nothing - reworked the provider table to pass pre-formatted display strings and use the single #actions slot correctly. Validated: dotnet build (0 errors), full backend test suite (72 passed, 0 regressions), clean books-web and admin-web production builds (both new/changed routes confirmed via compiled-bundle grep), validate-structure.mjs, books-accounting-contract-check.mjs, and a dev-server console-error check on /gst-tax-setup and /gst-tax-dashboard (zero errors, both correctly redirect to Login - Books remains login-gated with no test credentials in this environment, so full interactive click-through of the new CRUD flows was not possible, consistent with every prior Books stage).'
} as const
