export const garmetixModularVersion = {
  version: '6.4.0',
  stage: 'Stage 14U: Purchase Return Edit/Delete with real reversal',
  label: 'Version6 Stage 14U: Purchase Return Edit/Delete with real reversal',
  summary: 'Amit asked for the Purchase Return register to have Add/Edit/Delete like a normal record list - Add already existed (search invoice, then Return Items), but Edit and Delete did not exist anywhere in this codebase, not even in legacy - the register only ever offered View/Print/Settle. Confirmed this was not an oversight to just patch in: a posted return already has live consequences (stock moved, a debit note created, GST ITC reversed, and possibly a freight journal/voucher posted), so a naive delete would corrupt the books. Checked with Amit twice on exactly what Edit and Delete should do given that risk, and both were confirmed as a genuine full reversal, not a shortcut. Backend: added POST purchase/returns/{id}/reverse, a new reversal engine that undoes a posted returns stock movement (a new offsetting StockMovement, MovementType PurchaseReturnReversalIn, following this codebase existing SalesCancellationIn/SalesReturnIn naming convention), removes its PurchaseReturnItems and PurchaseReturnItcReversals rows (required for correctness since the returnable-quantity lookup does not filter by return status), hard-deletes its JournalEntry/JournalLines, removes its debit note (CommercialNote), restores the vendor balance, and - if freight was in-house - reverses its auto-created Expense voucher and journal too. Blocked entirely if the debit note has already been used in a vendor settlement (SettledAmount > 0 or CommercialNote.IsAdjusted), since there is no unsettle mechanism to safely undo that first. Recomputes the purchase invoice status afterward (back to Paid/PartiallyPaid/Pending if nothing remains returned, or PartiallyRefunded otherwise). A hardDelete flag additionally soft-deletes the PurchaseReturn header itself so it disappears from the register; without it, the return is marked Cancelled and kept for audit. Frontend (purchase-return/index.vue): the register gained a Status column plus Edit and Delete buttons (shown only for Posted, unsettled returns). Delete requires typing DELETE RETURN to confirm, matching this codebase established guarded-write pattern for serious destructive actions. Edit reverses the old return immediately (after an explicit confirm describing exactly what that means), then reopens the same Return Items form pre-filled with the old quantities/reason/transport/freight so the operator can correct and resubmit as a fresh return via the existing, unmodified partial-return endpoint - deliberately two separate API calls rather than one shared transaction, so a failure re-entering the corrected return does not also roll back the already-completed reversal. Validated via a clean dotnet build plus the full backend test suite (72 passed, 3 pre-existing Postgres-only tests skipped, zero regressions), and a clean main-web production build with a compiled-bundle grep confirming no dangling references (the Chrome extension needed for a live console-error check was disconnected in this environment).'
} as const
