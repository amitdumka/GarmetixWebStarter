export const garmetixModularVersion = {
  version: '6.8.8',
  stage: 'Stage GST-7: Sale GST Review',
  label: 'Version6 Stage GST-7: Sale GST Review',
  summary: 'Seventh stage of the GST & Taxes module (see Stage GST-1 through GST-6 for schema/foundation, provider setup, GSTIN verification, HSN/SAC master, rate master, and audit engine). Implemented autonomously via the scheduled GST continuation task. Adds "Sale GST Review": line-level GST mismatch detection for Sale invoices incl. the garment threshold, merged/linked with the existing Sale Review tool. Research found the existing apparel-threshold "Sale Review" (backend-only SaleReviewEndpoints.cs at /api/sale-review, hardcoded 5% up to Rs.2,499 / 18% above, with a tested Option-B correction+accounting-posting workflow) had zero modular frontend - only a legacy-only page existed. Backend: new GstSaleReviewEndpoints.cs (/api/gst/sale-review, read-only) resolves each Sale invoice lines GST rate live against the configurable Rate Master (Stage GST-5, HSN/category/price-threshold incl. garment slabs) rather than a hardcoded number, comparing it to the stored line tax rate/amount, and cross-links each invoice to its count of open GST Audit Engine findings (Stage GST-6) via GstAuditFindings.InvoiceId - added InvoiceId/PurchaseInvoiceId to GstAuditFindingDto and an optional invoiceId/purchaseInvoiceId filter to GET /api/gst/audit/findings as part of this (small, additive, reusable by Stage GST-8 too). Capped at 2000 lines per request, mirroring the Stage GST-6 audit engine cap, since each line resolves live. New page apps/books/pages/gst-tax-sale-review.vue: one page, two sections - "Apparel Threshold Review" ports the legacy sale-review.vue UI onto the existing, untouched /api/sale-review endpoints (filters, summary cards, invoice table with Apply Option B, item table with pagination, CSV export, copy issue summary - this closes the "no modular UI at all" gap for the existing tool, the literal "merge/link with existing Sale Review"), and "GST Rate Master Line Check" is the new configurable-rate-driven view (summary cards, invoice table with an Open Audit Findings link to /gst-tax-audit, line-level resolved-vs-stored table with a Garment Threshold badge). Deliberate scope decision: the new section is read-only by design - the existing, already-tested Option-B posting logic is reused as-is (same endpoint, same backend code) rather than building a second, redundant financial-mutation path for the new rate-resolver view. Registered route /gst-tax-sale-review, added to Books GST & Taxes sidebar menu and dashboard quick actions. Validated: dotnet build (0 errors), full backend test suite (72 passed, 3 pre-existing Postgres-only skipped, 0 regressions), clean books-web production build (new route confirmed via compiled-bundle grep), validate-structure.mjs, and a dev-server console-error check on /gst-tax-sale-review (zero errors, redirects to Login).'
} as const
