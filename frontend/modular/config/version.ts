export const garmetixModularVersion = {
  version: '6.2.7',
  stage: 'Stage 14S: Petty Cash full CRUD parity + Shift working/break hours',
  label: 'Version6 Stage 14S: Petty Cash full CRUD parity + Shift working/break hours',
  summary: 'Two more direct requests from Amit. (1) Petty Cash (books/pages/petty-cash.vue): the modular page was read-only (view saved-vs-calculated comparison only, no create/edit/delete/print). Rebuilt to full legacy parity: New Sheet button opens a create form that auto-pre-fills all 10 cash fields from the existing GET petty-cash-sheets/prepare endpoint (with a Recalculate button and a live Total-in/Total-out/Calculated-cash summary), Edit and Delete actions per row, and an in-app Print modal (income/payment breakdown, cash-in-hand, reconciliation-mismatch alert on save) with Download PDF and Print A5 (opens the PDF in a new tab via a new openBlob helper added to books-api.ts) actions - mirroring legacy petty-cash/index.vue feature-for-feature. Kept the existing modular-only saved-vs-calculated review/mismatch panel since it has no legacy equivalent and is genuinely useful. Every endpoint used (list/get/prepare/POST/PUT/DELETE/pdf) already existed - zero backend changes. (2) Shift working/break hours (hr/pages/attendance/shifts.vue): investigated Amit\'s "2 shifts = full day" request and found the underlying session-count mechanic (AttendanceShift.RequiredSessionsForFullDay/RequiredSessionsForHalfDay) already fully implemented end-to-end in both legacy and modular, wired through AttendanceRuleEngine into payroll - confirmed with Amit and scoped down to the real gap: shift times were only ever shown as clock times (start/end/break-start/break-end), with no "X hours working / Y hours break" computed anywhere. Added a live computed Working-hours/Break-hours summary to the shift create/edit form (updates as start/end/break minutes change) and to each shift\'s row-card summary, derived client-side from the existing minute-of-day fields (span minus break duration, unless countBreakAsWork is set) - no schema or backend change. Verified via clean books-web and hr-web production builds (both /petty-cash and /attendance/shifts prerender with real content), dev-server console-error checks on both routes, and all Books validation scripts passing (validate-structure.mjs, books-parity-baseline.mjs, books-accounting-contract-check.mjs).'
} as const
