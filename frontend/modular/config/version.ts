export const garmetixModularVersion = {
  version: '6.9.4',
  stage: 'Final Accounts report fix, part 3 - materialize then LINQ-to-objects',
  label: 'Version6: Final Accounts reports fixed for real (client-side aggregation)',
  summary: 'The ValueTuple fix (part 2) solved GroupBy but Amit immediately hit a third variant one level deeper: OrderBy(item => item.Entry.OnDate), which surfaced as new ValueTuple(...).Item1.OnDate left unflattened. With Amit logged in live, this proved the underlying EF Core version simply cannot reliably flatten member access through ANY explicitly-constructed compound projection - tuple or record - for arbitrary downstream operators; Where and Contains happened to work, GroupBy and OrderBy did not, with no reliable way to predict which operator would fail next without hitting it live. Rather than keep patching operators one at a time, restructured ReportLines() to push every genuinely translatable filter (scope, status, accountIds, and now an onOrAfter/onOrBefore date range) directly into the single join query, then every one of the 6 call sites materializes the result via ToListAsync() immediately - no further Where/GroupBy/OrderBy/Select ever composes against the IQueryable again. All remaining grouping, ordering and DTO projection now happens as plain LINQ-to-Objects on the materialized List<(Entry, Line)>, which has no EF translation involved at all and therefore no way to hit this class of bug again. Verified this time by logging into the live site with credentials Amit provided and opening Reports directly, rather than relying on unit tests alone (the 138 FinalAccounts unit tests exercise rule logic only, not live SQL translation, which is why none of these three bugs were caught before reaching production). Validated: dotnet build (0 errors), full backend test suite (219 passed, 3 pre-existing Postgres-only skipped, zero regressions).'
} as const
