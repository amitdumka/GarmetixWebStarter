export const garmetixModularVersion = {
  version: '6.0.69',
  stage: 'Stage 14P Inventory App Adaptation And Deploy',
  label: 'Version6 Stage 14P Inventory App Adaptation And Deploy',
  summary: 'Adapted the copied-in Inventory app (originally built by antigravity ai against an older Nuxt UI API and pointed at nonexistent /masters/* backend routes) into a real, working modular app and wired it into the SRP deploy pipeline. Rewrote all four CRUD pages (categories, brands, barcodes, stock) to call the real backend contracts (/api/product-categories, /api/brands, /api/price-tags/search+prepare with the correct Rows/thermalCommands shape, /api/inventory/stock-operations/* with StockId-based adjustment/transfer requests) and converted every page from old-style UFormGroup/:options/UTable slots to this codebase\'s Nuxt UI 4 conventions (garmetix-page-stack layout, USelectMenu/USelect :items, UModal forms, shared AdminMasterTable). Fixed the same defineProps<{...}> runtime-registration bug in AdminMasterTable.vue that broke BooksMasterTable earlier this session, before it could ship broken. Registered inventory as a real app id across config/apps.ts, shared-types, shared-ui appCopy/localMenus/appDefaultBase, added its routes, and wired the SRP whole-site deploy script end to end (base path, URL env vars, nginx location block, build_app call, build:inventory script) - verified via a full 8-app local build before shipping.'
} as const
