export const garmetixModularVersion = {
  version: '6.9.26',
  stage: 'Communication & Mail CM-09 - Webhooks and Suppression',
  label: 'Communication & Mail: authenticated Brevo delivery webhook, automatic suppression, and a Queue/Log + Suppression UI',
  summary: 'CM-09 of the Garmetix Communication & Mail module (branch Mail-Com, not yet merged). Researched Brevo\'s actual current webhook documentation (developers.brevo.com/docs/how-to-use-webhooks, help.brevo.com/hc/en-us/articles/15127404548498) rather than assuming a Stripe/GitHub-style HMAC signature exists - it does not, for transactional webhooks Brevo\'s own documented model is Basic Auth credentials embedded in the notify URL and/or a custom header token configured in the Brevo dashboard, plus an IP allow-list (Brevo\'s published outbound range is 1.179.112.0/20). BrevoWebhookAuthenticator implements exactly that (Basic Auth OR header-token match, both via CryptographicOperations.FixedTimeEquals, plus an optional CIDR allow-list via the new System.Net.IPNetwork) and fails closed: BrevoWebhook:Enabled=false or no credentials configured rejects every call rather than accepting anything, so a fresh install\'s webhook endpoint is inert by default the same way every other optional integration in this module already is. POST /api/communication/webhooks/brevo (unauthenticated by JWT - the caller is Brevo\'s server, not a logged-in user) accepts a single event or Brevo\'s batched-array payload, deduplicates on a deterministic ProviderEventId (message-id+event+email+timestamp - Brevo\'s payload has no single guaranteed unique event id field, confirmed by the same research pass), matches the event\'s message-id against EmailQueueItem.ProviderMessageId, applies the matching legal EmailQueueStateMachine transition (opens/clicks never touch queue status - "opens are not proof of human reading" per the master prompt), and auto-creates a scoped EmailSuppressionEntry on confirmed hard-bounce/invalid/complaint. EmailSuppressionEndpoints.cs adds the audited admin surface (list scoped by WorkspaceScope, manual add, remove-with-required-reason). EmailQueueEndpoints.cs adds the CM-04-deferred queue/log visibility (list with status/module/correlation-id/provider-message-id/search filters, detail with the full attempt + delivery-event timeline, retry/cancel/reschedule/restore-dead-letter) - restoring a DeadLetter item required adding DeadLetter->Pending as a new legal EmailQueueStateMachine transition (alongside the existing DeadLetter->Processing), since the CM-02 design had only anticipated a direct-to-Processing restore. New Queue & Log and Suppression pages in the Communication & Mail app. Validated: dotnet build 0 errors/0 new warnings, backend suite 346 passed/0 failed/11 skipped (8 new BrevoWebhookAuthenticator tests covering fail-closed defaults, Basic Auth, header token, and CIDR allow-list accept/reject, all passing for real - not gated behind Postgres), clean communication-web build (all 8 routes prerender with real content), validate-structure.mjs passing.'
} as const
