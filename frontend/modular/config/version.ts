export const garmetixModularVersion = {
  version: '6.9.37',
  stage: 'POS: Basic Rate Calculator popup tool',
  label: 'New POS helper tool: Basic Rate Calculator (MRP to basic/tax, Fabric or Apparel, discount % or amount)',
  summary: 'Closes the long-standing "Basic Rate Calculator toolkit function flagged as needed but never built" gap (Inventory.cs:108), built as a POS helper popup rather than a backend static function since it is a pure client-side calculation with no persistence need. New RateCalculatorModal.vue (frontend/modular/apps/pos/components/): enter MRP and pick Apparel or Fabric. Apparel reuses the exact same threshold constants backend/Garmetix.Api/Billing/SaleReviewEndpoints.cs already applies (5% GST when basic/taxable value is up to Rs 2,499, 18% above) - rate is derived from MRP by trying the lower band first and falling through to the higher band if the resulting basic price would not fit under the threshold, since the threshold is defined on basic value, not MRP, and this reverse-derivation is inherently approximate right at the boundary (disclosed via an on-screen note). Fabric has no fixed slab anywhere in this codebase, so its tax rate is a plain editable field (defaults to 5%). Discount % and Discount Amount are two-way synced (editing either updates the other against the basic price) and reduce the basic price before recomputing tax - the panel shows basic price, tax amount, discount, and net selling price both before and after the discount. Wired into the main POS billing screen (/sale, header toolbar) and the POS counter dashboard (/, header toolbar) via Nuxt auto-imported component, no explicit import needed. Zero backend changes - purely client-side math, no API calls. Validated: clean pos-web production build (all routes prerender), validate-structure.mjs, hand-verified the threshold/discount math against four manual test cases (mid-band, above-threshold, exact boundary, and a discounted line) before deploying.'
} as const
