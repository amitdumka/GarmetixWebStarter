export const garmetixModularVersion = {
  version: '6.9.27',
  stage: 'Communication & Mail CM-10 - Optional Postfix Relay',
  label: 'Communication & Mail: isolated, disabled-by-default Postfix-to-Brevo relay for operators who want a local SMTP hop',
  summary: 'CM-10 of the Garmetix Communication & Mail module (branch Mail-Com, not yet merged) - the smallest, ops-only stage. New frontend/modular/deploy/postfix-relay/ (docker-compose.yml, .env.example, postfix/main.cf.template, healthcheck.sh, README.md), entirely isolated: not referenced by srp-whole-site-deploy.sh, srp-swalekha-deploy.sh, or any other deploy script - `docker compose up` in that directory has zero effect on the main application, and most deployments never need it at all since the module\'s SMTP/Brevo-API providers already talk to Brevo directly. Configuration follows this repo\'s established .env/.env.example/.gitignore convention (no new gitignore entry needed - the existing root `.env`/`.env.*`/`!.env.example` patterns already cover it). Relay-only Postfix config: mynetworks restricted to an operator-supplied internal CIDR (never 0.0.0.0/0), smtpd_sender_restrictions requiring a default-deny sender_access allow-list, mandatory TLS upstream to Brevo\'s SMTP relay with modern-protocols-only enforced, Docker json-file log rotation (10MB x 5 files) plus Postfix\'s own maillog, and a standalone healthcheck.sh safe to run from cron/monitoring without touching the main app. README.md documents the full DNS/deliverability sequence (SPF merge-not-conflict, DKIM CNAME/TXT pair, staged DMARC p=none->quarantine->reject, Reply-To mailbox setup) - this section applies whether or not the relay is used, since it\'s about the Brevo sending domain itself. Also published the real docs/communication-mail-deployment.md (replacing the implementation pack\'s generic template) with concrete commands: the actual SRP backup/deploy/verify sequence, a rollback plan noting all 16 CM-02 tables are additive (old release code simply never queries them), and a live-disable path (EmailQueue:Enabled=false or disabling every Provider) that needs no redeploy. Validated: docker-compose.yml is hand-reviewed for YAML correctness (docker/PyYAML were not available in this sandbox to run `docker compose config`, disclosed rather than silently skipped); no backend/frontend code changed this stage, so the existing dotnet build/test and communication-web build results from CM-09 still hold unchanged. No merge, push, or deploy performed.'
} as const
