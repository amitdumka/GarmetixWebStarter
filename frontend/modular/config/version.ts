export const garmetixModularVersion = {
  version: '6.9.21',
  stage: 'Swalekha PersonalFin_15 - Document Vault & Hardening',
  label: 'Version6: Swalekha closes out its 15-stage roadmap with a Document Vault and a live isolation self-check',
  summary: 'The final stage of the Swalekha module. New SwalekhaDocument entity (Owner-scoped like every other Swalekha table) + /api/swalekha/documents (list/upload/download/soft-delete), a genuine multipart file-upload endpoint - the first one in Swalekha, researched from PurchaseInvoiceImportService.cs\'s existing pattern - storing files per-Owner on disk (extension/size-validated, 15MB cap, PDF/PNG/JPG/WEBP) with a StoredFilePath row pointing at them, so a scanned FD certificate, loan agreement, or insurance policy PDF can be attached against any entityType/entityId. New /api/swalekha/security/self-check - a live runtime assertion (not a static claim) that PersonalFin_06\'s Owner-isolation query filter is doing its job: connects to swalekha_db, resolves the Owner from the JWT, compares a normally-filtered SwalekhaAccounts count against an explicit IgnoreQueryFilters count scoped to that Owner (must match), and confirms other Owners\' rows genuinely exist elsewhere in the same table but are correctly invisible - proving isolation against real data, not just true-by-absence. New /documents page (upload form, filterable list, download/delete) and a Self-Check panel added to /about. The backup protocol was also extended: srp-backup-database.sh now dumps swalekha_db alongside the main database on every stage backup, gracefully skipping if the connection string isn\'t present on a given host (extension is code-complete/reviewed but not yet exercised against a real deployed SRP host, since Swalekha has never been deployed). Live-verified end to end against the real local stack: uploaded a real PDF (201, correctly stored and listed with type/size/notes), downloaded it through the authenticated blob-fetch flow (200), deleted it (204, correctly disappeared - soft delete), and ran the self-check through the actual UI button - all 4 checks passed, including "5 account row(s) belonging to other Owners exist and are correctly invisible", proving the isolation check ran against real other-Owner data accumulated from earlier stages this session, not an empty table. All 15 PersonalFin stages of the original roadmap (docs/personal-finance-module-design.md) are now complete.'
} as const
