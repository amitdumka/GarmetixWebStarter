export const garmetixModularVersion = {
  version: '6.8.4',
  stage: 'Stage 14L.3: Notes Modal Parity And Admin Dot Matrix',
  label: 'Version6 Stage 14L.3: Notes Modal Parity And Admin Dot Matrix',
  summary: 'Frontend-only parity polish after Stage 14L.2. Debit Notes and Credit Notes now use register-first flows with New/Edit forms in slide-over panels and detail views in slide-over panels, while preserving existing direct routes. Added the legacy Dot Matrix Print workflow to the modular Admin app at /dot-matrix-print with store settings, bridge/test-print modal, queue stats, retry/skip/reprint/reset actions and printable text preview. No backend, database, pull, push or deploy change was made.'
} as const
