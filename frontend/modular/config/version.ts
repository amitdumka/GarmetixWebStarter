export const garmetixModularVersion = {
  version: '6.2.1',
  stage: 'Stage 14Q.3 Import Satellite Pages + Tailoring & Alteration Port',
  label: 'Version6 Stage 14Q.3 Import Satellite Pages + Tailoring & Alteration Port',
  summary: 'Ported the two remaining Purchase Import satellite pages plus the Tailoring & Alteration module, closing out the rest of the Purchase Import backlog and adding an entirely new sales module. Import Acceptance (/purchase/import-acceptance) - final QA dashboard (acceptance summary, final-closure checklist, parser QA by template, backup/restore proof checklist, recent-imports register with Pass/Retest/Correction actions and CSV/audit-JSON export), calling the pre-existing acceptance-summary/parser-qa-summary/final-closure-status/backup-restore-checklist/posting-report/correction-plan endpoints. Import Learning (/purchase/import-profiles) - vendor OCR learning profile master/detail (ignored line-pattern and product-alias management, preferred parser template selector - this is where "Tally style" parsing is already surfaced, it is not a separate feature). Tailoring & Alteration (/tailoring) - full rewrite of what was an 11-line placeholder stub, ported field-for-field from the legacy 716-line page against the already-fully-built /api/tailoring/* backend (929-line TailoringEndpoints.cs, zero backend changes for this part): separate Stitching and Alteration order-entry workflows with customer mobile lookup, multi-line service items, vendor cost tracking, a delivery board, an orders register with status/type filter and pagination, service master, and vendor + vendor-rate management, using popups for all secondary create actions per this codebase\'s established convention. Verified via clean production build (all three routes prerender with real content) and dev-server console-error checks.'
} as const
