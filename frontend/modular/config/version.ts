export const garmetixModularVersion = {
  version: '6.2.0',
  stage: 'Stage 14Q.2 Purchase Import Supplier Invoice OCR Workflow (Phase 3)',
  label: 'Version6 Stage 14Q.2 Purchase Import Supplier Invoice OCR Workflow (Phase 3)',
  summary: 'Ported the core Scan/Import Supplier Invoice workflow (/purchase/import) - the largest remaining piece of the Purchase module port and the first modular frontend for the existing but previously unexposed 4060-line PurchaseInvoiceImportService.cs backend (34 endpoints). Full upload-review-post pipeline: upload a PDF/image/pasted-text supplier invoice against a selected store; review the OCR-parsed header (vendor match-or-manual, GSTIN, invoice number/date, freight/discount/round-off/bill/paid amounts, payment mode/bank); review a live posting-readiness panel (active/ignored line counts, new-vs-matched product counts, missing barcode/tax counts, bill-difference reconciliation, checklist); edit every line inline (product name/barcode/HSN/category/subcategory/unit/qty/MRP/cost/discount/tax/price-mode, ignore toggle); per-line "Find Similar Product" search-and-match and "Split By Size" (garment size-label splitting); bulk "Generate Missing Barcodes" and "Distribute Header Discount Across Lines"; paste-and-reparse OCR text with a replace-vs-append toggle; duplicate-invoice detection with Override (reasoned) or Recheck actions; View Proof (opens the original uploaded file) and Load Extracted Text (raw OCR text viewer); and Save Draft / Post Purchase Inward / Reject / Delete lifecycle actions. Added postForm (multipart upload), openBlob (view a fetched blob in a new tab) and getText (plain-text response) helpers to apps/main/utils/main-api.ts since none of the existing per-app API clients had multipart or non-JSON response support yet. Zero backend changes - every one of the ~20 endpoints wired already existed and was fully implemented; this is purely a new frontend surface. Deferred: the two satellite pages (Import Acceptance QA dashboard, Import Learning vendor-profile management) and several secondary backend features not wired into this pass (audit-json/lines-csv export, cleanup-history, cross-batch acceptance-summary/parser-qa-summary/final-closure-status/backup-restore-checklist reports) - see .claude/todo.md. Verified via a clean production build (route prerenders with real content) and a dev-server console-error check; full interactive click-through of the upload/OCR/post pipeline was not possible in this environment (login-gated, no test credentials, and no sample invoice file to upload against a live backend) - this is a materially larger untested surface than prior stages and should get a real click-through pass before being treated as fully verified.'
} as const
