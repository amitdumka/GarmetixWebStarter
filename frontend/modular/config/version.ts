export const garmetixModularVersion = {
  version: '6.9.28',
  stage: 'Communication & Mail CM-11 - QA and Hardening',
  label: 'Communication & Mail: security hardening pass - webhook rate limiting, secret-masking/tenant-isolation tests, and a real test/acceptance report',
  summary: 'CM-11 of the Garmetix Communication & Mail module (branch Mail-Com, not yet merged). Added the one gap a security review against docs/communication-mail-security.md\'s checklist actually found missing: the Brevo webhook endpoint had a body-size cap (CM-09) but no request-rate limit. Added a dedicated ASP.NET Core AddFixedWindowLimiter("brevo-webhook", 120 req/min) scoped only to that one route via RequireRateLimiting - deliberately not a global app-wide limiter, so it can never throttle normal user-facing API traffic. New pure-logic tests for the two security-critical surfaces that had none yet: EmailCredentialProtectorTests (round-trip, tamper detection via CryptographicException, masking never exposes more than the last 4 characters, and - the actual mechanism CM-03\'s module-isolation claim rests on - two different Data Protection purpose strings genuinely cannot decrypt each other\'s ciphertext) and CommunicationAttachmentStorageServiceTests (extension allow-list case-insensitivity, size-cap boundaries, zero/negative rejection). Performed and documented a full manual code-review pass against every new endpoint for credential exposure, header injection, tenant/cross-tenant isolation, HR/Payroll confidentiality, ID enumeration, attachment abuse, SQL injection, and log secret-leakage - written up as concrete file/method references, not just checked boxes. Replaced the implementation pack\'s generic testing-plan template with docs/communication-mail-testing.md, an honest accounting of what 367 passing automated tests actually proved, what the Postgres-gated integration tests would prove if run against a real database (not available in this sandbox), what the manual code review covered, and what genuinely was not exercised (live Brevo send/webhook round-trip, live browser click-through, provider-outage failure injection at runtime) - disclosed explicitly rather than silently assumed passing. Validated: dotnet build 0 errors/0 new warnings, backend suite 367 passed/0 failed/11 skipped (21 new tests this stage, all passing for real), validate-structure.mjs passing (no frontend changes this stage). No merge, push, or deploy performed.'
} as const
