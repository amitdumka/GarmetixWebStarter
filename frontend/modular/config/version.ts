export const garmetixModularVersion = {
  version: '6.9.10',
  stage: 'Swalekha - first real local click-through, two bugs found and fixed',
  label: 'Version6: Swalekha verified live against a real database and Owner login',
  summary: 'Amit asked to run the Swalekha module locally for testing. This was the first time any Swalekha page was exercised against a real Postgres database and a real Owner login (every prior stage validated with build/console checks only, no test credentials available). Two genuine bugs surfaced immediately and were fixed: (1) wrapping a UButton in a UTooltip inside a UTable cell slot throws at render time in this Nuxt UI version, silently killing that row\'s rendering - the table showed its header but zero data rows despite the underlying array being populated. Fixed across all 10 Swalekha page files by replacing UTooltip-wrapped buttons with plain UButton + a native title attribute. (2) SwalekhaTripPayload.StartDate/EndDate are DateTime? on the backend, but the frontend\'s date <UInput type="date"> fields default to an empty string, which System.Text.Json cannot deserialize into DateTime? (only a real date or JSON null) - every "New Trip" submission with an unset date failed with a 500. Fixed by converting empty-string dates to null before posting. With both fixes applied, every PersonalFin_02-05 feature was genuinely exercised end to end: account creation/deposit/transfer, contact loan-given/settle, expense-sheet entries including the Hidden-expense visibility toggle, income entries, recurring-bill mark-paid, and trip creation/expense-entry/close (confirming the Travel roll-up design needs no data copy - the expense summary\'s bySheetType total picked up the trip\'s spend automatically). The same UTooltip-in-UTable-cell pattern was flagged as a likely pre-existing issue in other modular apps (e.g. final-accounts) that have never been live-tested either, spawned as a separate follow-up task.'
} as const
