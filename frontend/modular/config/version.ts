export const garmetixModularVersion = {
  version: '6.3.1',
  stage: 'Stage 14T.1: hotfix - Purchase Return page broken by dangling ref',
  label: 'Version6 Stage 14T.1: hotfix - Purchase Return page broken by dangling ref',
  summary: 'Amit reported the Purchase Return page (just shipped in Stage 14T) was completely broken live - neither the invoice-search/register section nor the new Return Items entry form was rendering. Root cause: when the barcode-jump feature was built, a helper function (setQtyInputRef) was removed from the script during a mid-build simplification, but a template `:ref` binding on the quantity input still referenced it - a dangling reference that throws a ReferenceError on every row render and breaks the whole component. Fixed by removing the stray `:ref` binding (the working `setLineRef`/barcode-highlight mechanism was untouched and did not need it). Verified via a clean main-web production build and a direct grep of the compiled output confirming no trace of the removed reference remains. Also checked the legacy frontend to rule out a second, separate "old form" concept Amit's report might have meant - confirmed legacy has the same single purchase-return/index.vue entry form (plus the same advanced-settlement.vue QA page) as modular, so this was purely the one rendering bug, not a missing second form. Browser-based live click-through verification was not possible this pass (Chrome extension was disconnected in this environment); relying on the clean build plus compiled-bundle grep as the verification bar for this hotfix.'
} as const
