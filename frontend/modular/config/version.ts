export const garmetixModularVersion = {
  version: '6.2.3',
  stage: 'Stage 14Q.4 Vendor Type Distinction (schema-repair fix)',
  label: 'Version6 Stage 14Q.4 Vendor Type Distinction (schema-repair fix)',
  summary: 'Added a nullable VendorType column to the Vendor entity so tailoring/alteration vendors can be genuinely distinguished from purchase vendors, reusing a VendorType enum that already existed (with a Tailoring literal) but had never been wired to anything. Added EF migration 20260710120000_AddVendorType.cs and updated the model snapshot by hand. Live verification on the SRP server after deploying and restarting the API uncovered that the formal migration file alone is NOT sufficient on this deployment: the server is configured with Database:SchemaBootstrapMode=FreshBaseline (confirmed by reading the exact startup log output), which makes ApplyDatabaseStartupMigrationsAsync in Program.cs call EnsureCreatedAsync + mark a baseline migration row and return early, never reaching db.Database.MigrateAsync() - so individual dated migration files are not actually applied on this box. The real mechanism keeping this server\'s live schema in sync is a separate, hand-maintained idempotent DatabaseSchemaRepairService.cs (RepairKnownSchemaDriftAsync, called unconditionally on every startup) containing explicit "ALTER TABLE ... ADD COLUMN IF NOT EXISTS" statements per column - confirmed via the actual post-restart journalctl output, which showed dozens of prior columns being added this way but nothing for VendorType. Added the missing "ALTER TABLE \"Vendors\" ADD COLUMN IF NOT EXISTS \"VendorType\" integer NULL;" statement to that repair service so the column genuinely gets created on next restart. TailoringEndpoints.cs/GET /api/vendors/Purchase Vendors page changes from the prior version are unchanged. Verified via a clean dotnet build (0 errors); live column creation to be confirmed via a follow-up log check after the next API restart.'
} as const
