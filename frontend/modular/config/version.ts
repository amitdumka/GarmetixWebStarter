export const garmetixModularVersion = {
  version: '6.9.2',
  stage: 'Final Accounts report fix - LINQ translation error on account-scoped queries',
  label: 'Version6: Final Accounts reports fixed (LINQ Contains translation)',
  summary: 'Amit reported every Final Accounts report unavailable with an EF Core error: a LINQ expression using accountIds.Contains(item.Line.AccountId) could not be translated. Root cause: FinalAccountsReportService.ReportLines() projects the joined journal entry and journal line into a private positional record (ReportLineQueryRow), and six call sites across General Ledger, Trial Balance, Profit and Loss category values, and closing-balance loading then filtered the resulting IQueryable with accountIds.Contains(item.Line.AccountId) - Contains() on a HashSet parameter needs to translate directly against a column reference, and EF Core could not flatten that member access back through the record constructor to the underlying line.AccountId column, so every one of those reports failed at query time. Fixed by adding an optional accountIds parameter directly to ReportLines() and applying the Contains filter inside the query itself, against the raw line.AccountId property, before it is wrapped into the record - fully translatable, and updated all six call sites to pass accountIds into ReportLines() instead of filtering after the fact. Validated: dotnet build (0 errors), full backend test suite (219 passed, 3 pre-existing Postgres-only skipped, zero regressions) - the 138 FinalAccounts unit tests all passed both before and after this fix since they exercise rule logic rather than a live EF Core-to-SQL translation, which is exactly why this bug was not caught before reaching production.'
} as const
