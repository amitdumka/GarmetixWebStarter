export const garmetixModularVersion = {
  version: '6.2.4',
  stage: 'Stage 14Q.5 Security: factory reset now genuinely SuperAdmin-only',
  label: 'Version6 Stage 14Q.5 Security: factory reset now genuinely SuperAdmin-only',
  summary: 'Fixed a previously-flagged security gap: POST /api/factory-reset was route-level gated behind GarmetixPolicies.Admin, which (via AccessPermissionMatrix.CanAccessPolicy\'s blanket IsAdminOrOwner short-circuit) actually admitted SuperAdmin, Admin, and Owner accounts alike, not SuperAdmin only. Added a new GarmetixPolicies.SuperAdmin constant and registered it directly in Program.cs as a standalone ASP.NET Core policy (bypassing the shared AddMatrixPolicy/CanAccessPolicy mechanism, since that mechanism structurally cannot express "stricter than Admin/Owner"), asserting on the same superAdmin JWT claim already used elsewhere in this codebase. Exposed AccessPermissionMatrix.IsSuperAdmin as public (was private) so Program.cs can reuse it directly instead of duplicating the claim check. FactoryResetEndpoints.cs now requires this new policy at the route level; its existing handler-level DB-backed SuperAdmin check (lines ~51-54) was left untouched as defense-in-depth - it already only allowed genuine SuperAdmins through, so this fix closes the outer/inner mismatch rather than changing runtime behavior for any already-blocked caller. No modular frontend UI exists for factory-reset today (only the legacy app has one), so no frontend changes were needed. Verified via a clean dotnet build (0 errors, only pre-existing unrelated warnings).'
} as const
