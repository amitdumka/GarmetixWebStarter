export const garmetixModularVersion = {
  version: '6.8.14',
  stage: 'Stage 14F.6 - Garmetix Assistant: pluggable model provider (Anthropic + Gemini)',
  label: 'Version6: Garmetix Assistant multi-provider (Anthropic + Gemini)',
  summary: 'Amit asked for the Garmetix Assistant to support two model providers - Anthropic (current) and Google Gemini - selectable via config, so he can run on Gemini free Flash tier for testing and switch to Claude for production quality. Extracted an IAssistantModelClient abstraction (AssistantModelClient.cs): one SendAsync(AssistantConversationState, toolSchemas, systemPrompt, ct) method returning an AssistantModelTurn (WantsToolCalls + ToolCalls, or FinalText) - both provider-neutral, with AssistantConversationState/AssistantConversationTurn/AssistantToolCall/AssistantToolResult replacing the old raw JsonArray messages the tool loop used to pass around directly. AssistantAnthropicClient now implements this interface as a thin wrapper - its existing tool_use/tool_result JsonNode request/response logic is unchanged, just reshaped to translate to/from the new neutral types instead of being called directly by AssistantChatService. Added AssistantGeminiClient.cs calling Gemini generateContent, translating the same tool schemas (name/description/input_schema) into Gemini functionDeclarations (name/description/parameters - the JSON Schema body is reused as-is) and handling functionCall/functionResponse parts; Gemini has no call-id concept so the client synthesizes one locally, pairing calls to results by function name (via a new ToolName field on AssistantToolResult) rather than id. Added Assistant:Provider config ("anthropic" default or "gemini") and a Gemini options section (Enabled/ApiKey/BaseUrl/Model default gemini-2.5-flash/MaxOutputTokens/TimeoutSeconds) mirroring AssistantOptions. Both clients are registered in Program.cs; AssistantChatService no longer depends on AssistantAnthropicClient directly - it resolves the active IAssistantModelClient via a new AssistantModelClientFactory keyed off Assistant:Provider (which also refuses to resolve Gemini if Gemini:Enabled is false, a deliberate safety gate independent of the Provider switch). AssistantToolCatalog.cs was left completely untouched, as required - the same six tools work unmodified under either provider. Replaced the Anthropic-only AssistantAnthropicException with a shared AssistantModelException(providerName, statusCode, body) both clients throw, updating the one catch site in AssistantEndpoints.cs accordingly. Added docs/garmetix-assistant.md documenting the provider switch, the config reference, and how to get a free Gemini API key from aistudio.google.com (explicitly noting this AI Studio key is unrelated to any Gemini Pro consumer subscription). Validated: dotnet build (0 errors, 0 warnings), full backend test suite (81 passed - 72 pre-existing plus 9 new, 3 pre-existing Postgres-only skipped, zero regressions). Added a new test file (AssistantModelClientTests.cs) exercising both AssistantAnthropicClient and AssistantGeminiClient end-to-end through a fake HttpMessageHandler (tool-call round trip, plain text reply, and error-to-AssistantModelException mapping for each provider, plus AssistantModelClientFactory provider-selection/safety-gate behavior) - this is the closest available substitute for a live Gemini/Anthropic call in an environment with no real API keys configured, since a true sandboxed live call was not possible here.'
} as const
