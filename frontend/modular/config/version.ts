export const garmetixModularVersion = {
  version: '6.5.0',
  stage: 'Stage 14V: Purchase Return rebuilt to mirror legacy exactly',
  label: 'Version6 Stage 14V: Purchase Return rebuilt to mirror legacy exactly',
  summary: 'Amit said what he was seeing on Purchase Return was not working, and asked for a from-scratch redesign that implements the page exactly as legacy frontend has it, without removing any backend/API/database code, UI first and other things to follow as separate asks. Rebuilt purchase-return/index.vue from the ground up as a structural mirror of frontend/legacy/garmetix-web/pages/purchase-return/index.vue: a Purchase Return Register (all posted returns, print-status filter, search, Settle/Print/View row actions, ITC/Print/Settlement status badges) sitting above a Returnable Purchase Invoices register that lists every returnable invoice directly (GET purchase/invoices/recent, matching legacys own endpoint choice, not the search-only GET purchase/invoices used before) with a Return Items action per row - this direct full-list-with-action-button is the actual Add affordance legacy uses, which is why the previous search-box-first design and even the follow-up New Return button never read as a working Add to Amit. The Create Partial Purchase Return modal and Purchase Return Details modal (full reconciliation checks, item snapshot table, ITC reversal table, print/download footer) also mirror legacy field-for-field. Deliberately dropped for this pass, per explicit instruction to bring the UI first and ask before layering more back on: the barcode-scan line-jump, Current Stock column, Transport/Freight fields, and the Edit/Delete register actions added in Stages 14T/14U - none of those exist in legacy either, and nothing about them was removed from the backend (the freight fields on PurchaseReturn, the reversal engine at POST purchase/returns/{id}/reverse, and the barcode/current-stock fields on the returnable-items endpoint all still exist and still work, just are not wired into this UI pass). Validated via a clean main-web production build (/purchase-return prerenders with real content), validate-structure.mjs, and a compiled-bundle grep confirming every new section renders and no dangling ref bindings exist (this rewrite uses zero template ref bindings at all, unlike the two prior stages that each shipped one). Browser-based console-error verification was not possible - Chrome extension disconnected in this environment.'
} as const
