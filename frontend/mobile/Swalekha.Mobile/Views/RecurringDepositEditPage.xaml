<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:buttons="clr-namespace:Syncfusion.Maui.Toolkit.Buttons;assembly=Syncfusion.Maui.Toolkit"
             xmlns:inputLayout="clr-namespace:Syncfusion.Maui.Toolkit.TextInputLayout;assembly=Syncfusion.Maui.Toolkit"
             xmlns:viewModels="clr-namespace:Swalekha.Mobile.ViewModels"
             x:Class="Swalekha.Mobile.Views.RecurringDepositEditPage"
             x:DataType="viewModels:RecurringDepositEditViewModel"
             Title="{Binding Title}"
             BackgroundColor="{AppThemeBinding Light={StaticResource SwalekhaBackground}, Dark={StaticResource SwalekhaBackgroundDark}}">

    <ScrollView>
        <VerticalStackLayout Padding="16" Spacing="14">

            <Label Text="{Binding ErrorMessage}"
                   IsVisible="{Binding HasError}"
                   TextColor="{StaticResource SwalekhaDanger}"
                   BackgroundColor="{StaticResource SwalekhaDangerBg}"
                   Padding="10" FontSize="13" />

            <inputLayout:SfTextInputLayout Hint="Bank name" ContainerType="Outlined">
                <Entry Text="{Binding BankName}" />
            </inputLayout:SfTextInputLayout>

            <inputLayout:SfTextInputLayout Hint="RD number (optional)" ContainerType="Outlined">
                <Entry Text="{Binding RdNumber}" />
            </inputLayout:SfTextInputLayout>

            <VerticalStackLayout Spacing="4">
                <Label Text="Linked account (optional)" FontSize="12" TextColor="{StaticResource SwalekhaMuted}" />
                <Picker ItemsSource="{Binding Accounts}" ItemDisplayBinding="{Binding Name}" SelectedItem="{Binding LinkedAccount}" />
            </VerticalStackLayout>

            <inputLayout:SfTextInputLayout Hint="Monthly installment" ContainerType="Outlined">
                <Entry Text="{Binding MonthlyInstallmentText}" Keyboard="Numeric" />
            </inputLayout:SfTextInputLayout>

            <Grid ColumnDefinitions="*,*" ColumnSpacing="12">
                <inputLayout:SfTextInputLayout Grid.Column="0" Hint="Interest rate %" ContainerType="Outlined">
                    <Entry Text="{Binding InterestRateText}" Keyboard="Numeric" />
                </inputLayout:SfTextInputLayout>
                <inputLayout:SfTextInputLayout Grid.Column="1" Hint="Tenure (months)" ContainerType="Outlined">
                    <Entry Text="{Binding TenureMonthsText}" Keyboard="Numeric" />
                </inputLayout:SfTextInputLayout>
            </Grid>

            <Grid ColumnDefinitions="*,*" ColumnSpacing="12">
                <inputLayout:SfTextInputLayout Grid.Column="0" Hint="Start date (YYYY-MM-DD)" ContainerType="Outlined">
                    <Entry Text="{Binding StartDateText}" />
                </inputLayout:SfTextInputLayout>
                <inputLayout:SfTextInputLayout Grid.Column="1" Hint="Maturity date (YYYY-MM-DD)" ContainerType="Outlined">
                    <Entry Text="{Binding MaturityDateText}" />
                </inputLayout:SfTextInputLayout>
            </Grid>

            <inputLayout:SfTextInputLayout Hint="Expected maturity amount (optional)" ContainerType="Outlined">
                <Entry Text="{Binding MaturityAmountText}" Keyboard="Numeric" />
            </inputLayout:SfTextInputLayout>

            <inputLayout:SfTextInputLayout Hint="Notes (optional)" ContainerType="Outlined">
                <Entry Text="{Binding Notes}" />
            </inputLayout:SfTextInputLayout>

            <buttons:SfButton Text="Save"
                               Command="{Binding SaveCommand}"
                               HeightRequest="48" CornerRadius="8"
                               Background="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource Secondary}}"
                               TextColor="{StaticResource White}" />

            <ActivityIndicator IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}" Color="{StaticResource Secondary}" />

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
