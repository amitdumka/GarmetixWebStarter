# Shared UI Package

Purpose: shared UI contracts and later reusable Vue/Nuxt UI components.

Planned extraction source:

- `legacy/frontend/garmetix-web/components/ui/*`
- reusable dashboard display components
- shell/menu metadata contracts

Responsibilities:

- Keep app shells visually consistent.
- Share common empty states, toolbar patterns, confirmation dialogs, and page headers.
- Avoid moving complex page-specific forms too early.

No Vue component is extracted in Stage 12A.2. This package starts as a safe placeholder.

