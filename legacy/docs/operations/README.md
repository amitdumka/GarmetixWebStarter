# Operations

- `deployment/` - Windows, Linux, Mac mini, and production-environment deployment.
- `release/` - production release and go-live checklists.
- `validation/` - developer build and runtime validation procedures.

The public copies in `frontend/garmetix-web/public/docs/` remain in place because the running application links to them.

