# Next Part After v4.12.06

Next planned stage:

## Stage 11D-92 — Vyapar Import Batch Undo + Bulk Barcode Mapping Upload

Why this is next:

- Current Vyapar import can preview/review/confirm invoices.
- Runtime validation now checks imported invoices after posting.
- The next safety requirement is the ability to manage whole import batches and undo/reverse a wrongly imported historical batch.

Planned work:

1. Import batch ID/history.
2. Imported invoice batch list.
3. Admin-only batch undo/reversal.
4. Bulk barcode mapping upload from Excel/CSV.
5. Export mismatch workbook directly from import page.
6. Final approval screen before posting large batches.
