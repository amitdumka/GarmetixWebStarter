# Next Part After v4.12.07

## Stage 11D-93 — Vyapar Import Batch Undo + Bulk Barcode Mapping Upload

Recommended scope:

1. Add an import batch id/reference to every imported Vyapar sale invoice.
2. Add imported batch list with counts, totals, user, date/time, and source file name.
3. Add admin-only undo/reversal for one imported batch.
4. Add bulk barcode mapping upload so the exported mismatch Excel/CSV can be corrected and re-uploaded.
5. Add mismatch workbook export directly from the import page.
6. Add final confirmation screen before importing large ready batches.
7. Add stronger audit log for every batch confirm/undo.
