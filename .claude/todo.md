# Active Todo (Claude View)

Last updated: 2026-07-07. Check items off as completed; move detail/history into `changelog.md` once done. Codex's own module-lane TODOs live in `.codex/*_TODO.md` and `frontend/modular/docs/MODULAR_TODO.md` - this list is Claude-facing and bug/analysis driven.

## Bugs / Issues Found (2026-07-07 analysis pass)

- [x] **Security (fixed 2026-07-10, v6.2.4)**: `backend/Garmetix.Api/Backup/FactoryResetEndpoints.cs:17` was gating factory reset behind `GarmetixPolicies.Admin`, which - via `AccessPermissionMatrix.CanAccessPolicy`'s blanket `IsAdminOrOwner` short-circuit - actually admitted SuperAdmin, Admin, and Owner alike, not SuperAdmin only. Added a standalone `GarmetixPolicies.SuperAdmin` policy registered directly in `Program.cs` (bypassing the shared matrix mechanism, which can't express "stricter than Admin/Owner"), asserting on the existing `superAdmin` JWT claim via a newly-public `AccessPermissionMatrix.IsSuperAdmin`. The route now requires that policy; the existing handler-level DB-backed SuperAdmin check was left in place as defense-in-depth. No modular UI exists for factory-reset (only legacy has one), so no frontend change was needed.
- [ ] **Data modeling**: `backend/Garmetix.Domain/Generated/Models/Inventory/Invoicing.cs:289-290` - fields are stored in DB that a `//TODO` comment says should instead be calculated, plus missing `JsonIgnore`. Worth a follow-up to confirm this isn't causing drift between stored and true values on invoices.
- [ ] **Incomplete domain model**: `backend/Garmetix.Domain/Generated/Models/Inventory/Inventory.cs:39` - Fabric/UOM (unit of measurement) support is not implemented; relevant if fabric-based (as opposed to piece-based) inventory is ever needed.
- [ ] **Incomplete domain model**: `backend/Garmetix.Domain/Generated/Models/Inventory/Inventory.cs:108` - a "Basic Rate Calculator" static toolkit function referenced as needed but not yet created; check whether rate calculation logic is duplicated ad hoc elsewhere in the codebase as a result.
- [ ] **Incomplete enum**: `backend/Garmetix.Domain/Generated/Enums/BharatEnums.cs:58` - card-type enum is explicitly marked incomplete.
- [x] **Repo hygiene (already fixed, confirmed 2026-07-10)**: `.gitattributes` already has `* text=auto` on line 1 (added 2026-07-07 in commit `9259403`, alongside a factory-reset handler tightening). This item was stale in the list - no action needed.
- [x] **CRM bug (found 2026-07-07, live on SRP, confirmed fixed 2026-07-08)**: `/crm/customers` build-collided with its own `customers/new`/`customers/dues-reconciliation` sub-routes during static prerendering. Spot-checked live after the 2026-07-08 full-site redeploy: `/crm/customers` now serves real content directly (2911 bytes, matches every other app-shell page size), and the trailing-slash variant does a single clean redirect to the no-slash URL rather than looping. Resolved via the separately-spawned background fix and/or the 14F.6B deep-route trailing-slash normalization - not something this session touched directly.
- [x] **Nitro flaky cold-build bug (found + mitigated 2026-07-07)**: a from-scratch `nuxt generate` non-deterministically crashes or silently emits a 16-byte `"Redirecting..."` stub instead of real page content for every route of every modular app (confirmed unrelated to GST source changes). Mitigated with a retry+content-size-verification safety net in `frontend/modular/deploy/srp-whole-site-deploy.sh`'s `build_app()`. Underlying Nitro/Nuxt root cause not fixed upstream, just worked around.
- [x] **npm workspace install bug (found + fixed 2026-07-07)**: every app/shared-package `package.json` pinned internal `@garmetix/shared-*` deps at a stale exact version (`"6.0.0"`) not matching the real package version, causing npm 11.13 to 404 against the public registry for these private package names and fail the whole install. Fixed by relaxing all pins to `"*"`.
- [x] **Deploy script `dotnet publish` Git-Bash path bug (found 2026-07-07, fixed 2026-07-08)**: `srp-whole-site-deploy.sh`'s dotnet publish step failed with a doubled drive-letter path (`C:\c\AIArea\...`) under Git Bash. Root cause: Git Bash/MSYS's own implicit argv-to-Windows-path conversion for the native `dotnet` binary (not `wslpath`, which an earlier fix attempt wrongly targeted). Fixed via explicit `cygpath -w` in `dotnet_path_arg()`, verified against a real `--build-only` deploy run. Commit `99a835d`.

## Non-Bugs Confirmed Clean (checked, no action needed)

- [x] No `rejectUnauthorized: false` / disabled TLS verification anywhere in frontend or backend.
- [x] No hardcoded production domains found inside modular app source (`frontend/modular/apps`, `frontend/modular/packages`).
- [x] No stray `TODO`/`FIXME`/`console.log` left in modular `apps`/`packages` Vue/TS source.
- [x] `node frontend/modular/scripts/validate-structure.mjs` passes.

## Follow-Up Work (from roadmap, actionable slice)

- [x] Books Voucher Register filters (done 2026-07-12, v6.8.1): added date range, month/year, voucher type, ledger type/group, exact ledger and text-search filters to modular Books `vouchers.vue`. Frontend-only; no API/DB/deploy/commit performed so Claude can keep handling Books module coordination.
- [x] Books Day Book port (done 2026-07-12, v6.8.3): added modular `/day-book` from legacy Accounting menu with filters, pagination, CSV export, print/PDF evidence, detail slideover, quick-create actions and modular source-link mapping. Frontend-only; no API/DB/GST-module/deploy/commit performed.
- [x] Books Debit/Credit Notes modal parity + Admin Dot Matrix Print (done 2026-07-13, v6.8.4): modular Debit Notes and Credit Notes existed, but their registers still linked to separate New/Detail pages. Added a shared `CommercialNoteRegister` so New/Edit forms and details open in slide-over panels while direct routes remain usable. Ported legacy `/dot-matrix-print` into modular Admin with settings, test print, queue stats/actions and printable text preview. Frontend-only; no API/DB/GST-module/deploy/commit performed.
- [x] Books Stage 14C.5 (GST/accounting report finalization, financial-year lock acceptance, final Books closure) - closed 2026-07-07. See `.claude/changelog.md` for detail. Books modular parity lane is done pending live-token/manual evidence.
- [x] Books Stage 14G (full legacy GST menu parity: GSTR-1/3B builder, CA share, accounting-gst-validation, gst-final-acceptance) - closed 2026-07-07, deployed to `.127` SRP. The first deploy attempt's acceptance check falsely showed all-green (status-code-only); Amit caught the real bug from his browser, which led to fixing two real infra bugs (Nitro flaky build, npm workspace pin) and a genuinely-verified redeploy. See `.claude/changelog.md` for the full trail.
- [x] Start Stage 14F.7 MCP wrapper for the AI Sense read-only tool catalog once Assistant is confirmed stable. Done 2026-07-08: `POST /api/mcp`, gated behind `Assistant:McpEnabled` (default off), additive wrapper around `AssistantToolCatalog`, no live MCP-client test yet (no test creds/DB in this environment).
- [ ] Ask Amit whether to prioritize clearing the "pending live evidence" backlog (POS/HR/Books/CRM/Admin) over new feature work.
- [ ] Begin AntiGravity (`Version6.A`) port review starting with Priority 0 (file-list diff) from `AntiGarvity2CodeTODO.md` - do not read Antigravity workspace files beyond what's needed for a named comparison without asking first.

## Purchase Import satellite pages + Tailoring & Alteration (done 2026-07-10, v6.2.1-6.2.2)

Amit asked for this work immediately instead of waiting for the scheduled overnight run (which was cancelled - see `.claude/roadmap.md`). All four planned parts shipped:

- [x] **v6.2.1 - Import Acceptance QA dashboard** (`/purchase/import-acceptance`) - ported from legacy (530 lines). Zero backend changes.
- [x] **v6.2.1 - Import Learning (vendor OCR profiles)** (`/purchase/import-profiles`) - ported from legacy (294 lines), includes the parser-template selector ("Tally style" parsing was already covered by the existing `tally-prime`/`tally-prime-mrp` parser templates - not a separate feature). Zero backend changes.
- [x] **v6.2.1 - Tailoring & Alteration module port** (`/tailoring`) - full rewrite of the 11-line placeholder stub, ported field-for-field from legacy (716 lines) against the pre-existing `/api/tailoring/*` backend. Zero backend changes for this part.
- [x] **v6.2.2 - Vendor type distinction (schema change)** - added a nullable `VendorType?` column to the `Vendor` entity (`backend/Garmetix.Domain/Generated/Models/Inventory/Invoicing.cs`), reusing the previously-orphaned `VendorType` enum (`BharatEnums.cs`, has a `Tailoring` literal). EF migration `20260710120000_AddVendorType.cs` + model snapshot updated by hand (this repo's migrations are hand-written, not `dotnet ef`-generated - confirmed via `git log` on the Migrations folder). `TailoringEndpoints.cs`'s vendor list/create now use the real enum instead of the old hardcoded-label workaround; `GET /api/vendors` excludes `VendorType.Tailoring` by default. **Backward-compat note**: pre-migration vendor rows have `VendorType == null` and are treated as visible-in-both-lists (can't retroactively know which pool they belonged to) - only vendors created after this change are cleanly separated. Removed the now-obsolete `forbid('item.VendorType')` regression-guard assertions in `legacy/scripts/validation/stage8e-package7-compile-hotfix-static-checks.py` (they checked for the exact old workaround text this change intentionally replaced). `dotnet build` on `Garmetix.Api` passes clean. Migrations apply automatically on API startup (`db.Database.MigrateAsync()` in `Program.cs`) - no manual production DB step needed, but this part deploys without `--skip-api` since the API binary changed.

## Purchase Module Port (started 2026-07-10, v6.1.0)

Amit asked to port the legacy Purchase menu group (`frontend/legacy/garmetix-web`, live reference `https://garmetix.aadwikafashion.in/purchase`) into modular, since it was previously stub/read-only. Full legacy inventory (11 pages, all backend endpoints) is documented in the session record; summarized here for future pickup.

**Shipped in this stage (Phase 1 - full CRUD, matches legacy feature set for the core workflow)**:
- [x] `frontend/modular/apps/main/pages/vendors.vue` - new Vendors CRUD page (create/edit/delete popups, GSTIN lookup, filter + pagination). Was entirely missing before.
- [x] `frontend/modular/apps/main/pages/purchase/index.vue` - Purchase Register rebuilt from read-only recent-list to full paginated/filtered register with View/Edit/Pay/Cancel/Delete popups.
- [x] `frontend/modular/apps/main/pages/purchase/new.vue` - New Inward dedicated full page (was a placeholder stub), vendor + item entry + payment, posts to `POST /api/purchase/inward`.
- [x] `frontend/modular/apps/main/pages/purchase-return.vue` - Purchase Return register + return-items workflow (was a placeholder stub).
- [x] `frontend/modular/apps/books/pages/vendor-payments.vue` - upgraded from "Read only" to full CRUD (create against invoice or as vendor advance, edit, delete) with server pagination.
- [x] `frontend/modular/apps/books/pages/vendor-settlements.vue` - added the "Debit Notes Available For Settlement" register + Settle popup (was "Read only", view-only).
- [x] Fixed a dangling-route sidebar bug: `vendor-payments`/`vendor-settlements` had routes but no `ModularAppShell.vue` `localMenus` entry (same pattern as the earlier HR/Books menu bugs), plus added `vendors` to the Purchase menu group.

**Deferred to a future session (Phase 2 - not started, too large for one pass)**:
- [x] **Import/Scan Supplier Invoice core workflow** (`/purchase/import`) - done 2026-07-10, v6.2.0. `frontend/modular/apps/main/pages/purchase/import.vue` - full upload/review/edit/post pipeline against ~20 of the 34 `PurchaseInvoiceImportEndpoints.cs` endpoints (uploads, batch get/list/update, match-product, split-by-size, generate-missing-barcodes, override/recheck-duplicate, distribute-discount, reparse-text, post, reject, delete, proof, extracted-text, product-matches). **Not yet click-through tested** - login-gated environment with no test credentials and no sample invoice file to actually exercise the OCR pipeline; only build + no-console-error verified. Treat as needing a real first-use pass before relying on it.
- [x] **Import Acceptance QA dashboard** (`/purchase/import-acceptance`) - done 2026-07-10, v6.2.1. Also picked up most of the "secondary features" originally deferred here: audit-json/lines-csv per-batch export, acceptance status update (Pass/Fail/NeedsRetest), and correction-plan/request-correction are all wired on this page.
- [x] **Import Learning profiles** (`/purchase/import-profiles`) - done 2026-07-10, v6.2.1. Vendor OCR alias/ignored-pattern management, parser-template selector, `vendor-profiles` CRUD.
- [x] **Import features now wired (done 2026-07-10, v6.2.5)**: `cleanup-history` - new Cleanup History panel on `import-acceptance.vue` (older-than-days + status checkboxes + delete-files toggle, confirm dialog). `correction-safety` - the row-level correction action on `import-acceptance.vue` now opens a Correction Safety Check panel first (canDeleteDraft/canRejectDraft/directUndoAvailable/safe actions/warnings) before the operator proceeds to the existing `request-correction` call. Multi-file listing/download - `import.vue` gained a Files button/modal (`GET batches/{id}/files`) with per-file download (`GET batches/{id}/files/{fileId}/download`), previously only the single proof file was reachable. All three backend endpoints already existed fully implemented; this was pure frontend wiring, zero backend changes.
- [x] **Vendor Payable Reconciliation** (`/purchase/vendor-payable-reconciliation`) - done 2026-07-10, v6.1.1. Read-only QA dashboard calling the pre-existing `VendorPayableReconciliationEndpoints.cs`, mirrors the `accounting-gst-validation.vue` pattern (date filter, metrics, issues, paginated evidence table, CSV export).
- [x] **Purchase Return Advanced Settlement QA** (`/purchase-return/advanced-settlement`) - done 2026-07-10, v6.1.1. Same pattern, calls the pre-existing `PurchaseReturnAdvancedSettlementEndpoints.cs`. Required moving `apps/main/pages/purchase-return.vue` to `purchase-return/index.vue` first (a top-level file with the same name as a directory it needs to share breaks Nuxt nested routing unless it's set up as a layout wrapper - fixed to match the codebase's established `index.vue`-in-a-directory convention).

## GST & Taxes Module (started 2026-07-12, v6.7.0, Stage GST-1 done)

Amit gave a full 22-section spec for a new "GST & Taxes" module (multi-provider GST API architecture with UI-configurable encrypted credentials, HSN/rate master, GST audit engine, Sale/Purchase GST review, GST returns/CA export, E-Invoice/E-Way Bill placeholders). This is a large, explicitly staged build (the spec itself defines Stage GST-1 through GST-10) - being delivered stage-by-stage like every other multi-stage module in this project.

- [x] **Stage GST-1 (Foundation)** - done 2026-07-12, v6.7.0. 11 new backend tables (`gst_api_providers`/`features`/`credentials`, `gst_api_call_logs`, `gstin_verification_cache`, `gst_hsn_master`, `gst_tax_rate_rules`, `gst_state_codes`, `gst_uqc_codes`, `gst_audit_rules`, `gst_audit_findings`), first-ever backend credential-encryption service (`GstCredentialProtector`, ASP.NET Core Data Protection), `GET /api/gst/dashboard` + `GET /api/gst/providers`, new `GarmetixPolicies.Gst` policy (also fixed a pre-existing gap: the `"GST"` module string was listed in `AccessPermissionMatrix.AllModules`/profile lists but never actually wired into `ModuleRoles`/`CanAccessPolicy` - it would have always evaluated false), startup seed data (Indian GST state codes, UQC codes, 17-rule audit catalog, default garment 5%/18% price-threshold rate rule at Rs.1000, "Local Master" provider). Frontend: renamed Books' "GST" sidebar group to "GST & Taxes", added `gst-tax-dashboard.vue` as its landing page with empty/zeroed cards for metrics later stages will populate. Confirmed via research this app already has `/api/gst-returns` (GSTR-1/3B/HSN summary/CA share) and `/api/gstin` (single env-configured GSTIN lookup) - Stage 1 builds the new multi-provider/audit/master-data layer and links to what exists rather than duplicating it. Also fixed 3 pre-existing brittle version-string assertions (`books-stage14c5-closure.mjs`, `books-stage14g-closure.mjs`, `books-parity-baseline.mjs`) that hard-pinned `6.0.x`/`Stage 14`, which would fail forever past that version regardless of this stage's changes - same durability-bug class already fixed once before in this project's history. Validated: `dotnet build` (0 errors), full backend test suite (72 passed, 3 pre-existing Postgres-only skipped), clean `books-web` build, `validate-structure.mjs`.
- [x] **Stage GST-2 (API Setup UI)** - done 2026-07-12, v6.8.0. Full provider CRUD (`GET/POST/PUT/DELETE /api/gst/providers`, enable/disable), a `GET /api/gst/providers/catalog` (provider types/environments/feature codes/credential keys, one source of truth for both backend validation and frontend dropdowns), encrypted credential save/mask/clear (`GET/PUT /api/gst/providers/{id}/credentials`, values are never sent back decrypted), and test-health/test-gstin/test-hsn endpoints (fully functional for the seeded Local Master provider against local HSN/GSTIN-cache tables, honest "arrives in a later stage" message for every other provider type, every attempt logged to `GstApiCallLogs`). Mutating endpoints gated at `GarmetixPolicies.Admin`. New page `apps/books/pages/gst-tax-setup.vue` (provider table, guarded delete requiring typing `DELETE PROVIDER`, create/edit modal with feature checkboxes + priority/fallback + credentials panel + inline test buttons), route `/gst-tax-setup` (Owner/Admin only), added to the Books "GST & Taxes" menu and dashboard quick actions, plus a "Configure GST API" cross-app link card added to the Admin app's existing Company Setup page (`/setup`). Caught a real bug pre-ship: `BooksMasterTable` only supports flat string columns + one generic `actions` slot, not per-cell slots like `#cell-isEnabled` - reworked the table to match its actual contract. Validated: `dotnet build` (0 errors), full backend test suite (72 passed, 0 regressions), clean `books-web`/`admin-web` builds (compiled-bundle grep confirmed), `validate-structure.mjs`, `books-accounting-contract-check.mjs`, and a dev-server console-error check on both new/changed routes (zero errors, both correctly redirect to Login).
- [x] **Stage GST-3 (GSTIN Verification)** - done 2026-07-13, v6.8.2. `GstinResolutionService.cs`: local-cache-first (unless force-refresh) -> enabled providers by priority for feature `GSTIN_LOOKUP` (first real adapter, `GenericRestProvider`, does an actual HTTP GET with API_KEY/AUTH_TOKEN credential injection and flexible JSON parsing; every other provider type stays honest "later stage" per Stage GST-2's precedent) -> next provider only if `FallbackEnabled` -> local format/state-code-only answer. Every attempt logged to `GstApiCallLogs`, every success cached to `GstinVerificationCaches`. New endpoints `POST /api/gst/gstin/verify`, `GET /api/gst/gstin/cache` (+`/{gstin}`), `POST /api/gst/gstin/cache/manual`. New page `apps/books/pages/gst-tax-gstin-verify.vue` (verify + force-refresh, result panel, Copy Details, recent-cache table, Update Vendor/Update Customer search-and-apply). **Deliberately left the existing "Check GSTIN" buttons on Vendor Master (main app) and Customer Master (CRM `CustomerForm.vue`) untouched** - they already work against the older single-provider `/api/gstin` module; replacing working data-entry functionality wasn't worth the regression risk for this stage, the new GST & Taxes page is additive. Route `/gst-tax-gstin-verify`, added to Books menu + dashboard quick actions. Validated: `dotnet build` (0 errors), full backend test suite (72 passed, 0 regressions), clean `books-web` build (compiled-bundle grep confirmed), `validate-structure.mjs`, `books-accounting-contract-check.mjs`, dev-server console-error check (zero errors, redirects to Login).
- [x] **Stage GST-4 (HSN/SAC Master)** - done 2026-07-13, v6.8.5. New `GstHsnEndpoints.cs` (`/api/gst/hsn`): full CRUD, filtered/paginated list with a per-row Product mapping count (grouped-count query, not N+1), `/search` for autocomplete, `/bulk-status` for bulk activate/deactivate, CSV import/export/template. **Deliberate, disclosed scope adaptation**: bulk import accepts CSV not `.xlsx` - this codebase has no Excel-reading library (only a write-only `SimpleXlsxBuilder`), and a hand-rolled OpenXML parser for arbitrary uploaded workbooks is a real correctness risk for a data-load path; CSV covers the same need with far less risk. New page `apps/books/pages/gst-tax-hsn.vue` (filterable/paginated table, checkbox multi-select bulk activate/deactivate, create/edit modal, Import CSV/Download Template). Added `postForm` to `books-api.ts` (mirroring `main-api.ts`'s existing multipart helper, Books never needed one before). Wired a lightweight debounced HSN autocomplete + "Missing HSN" warning into the Inventory app's existing `products.vue` (calls the new `/gst/hsn/search` endpoint), rest of that form untouched. Route `/gst-tax-hsn`, added to Books menu + dashboard quick actions. Validated: `dotnet build` (0 errors, incl. a self-caught CA2024 warning fixed pre-commit), full backend test suite (72 passed, 0 regressions), clean `books-web`/`inventory-web` builds (bundle grep confirmed for books-web), `validate-structure.mjs`, dev-server console-error check on `/gst-tax-hsn` (zero errors; inventory-web has no dev-server launch entry in this environment, verified via clean build only).
- [x] **Stage GST-5 (GST Rate Master)** - done 2026-07-13, v6.8.6. New `GstRateResolutionService.cs` implements the resolve algorithm (spec section 9): HSN match first, then product category, filter by the rule's price threshold (the garment slab), highest-priority match wins, split into CGST/SGST (intra-state) or IGST (inter-state) unless the rule has explicit split rates, fall back to the HSN master's own default rate, then an honest "nothing configured" result. Uses invoice date, never "today". New `GstRateEndpoints.cs` (`/api/gst/rates`): full CRUD + `POST /resolve`. New page `apps/books/pages/gst-tax-rates.vue` (rule table, create/edit modal, live "Resolve A Rate" test panel). Product Master: added a "Resolve" button next to the existing Tax dropdown in Inventory's `products.vue` - since `Product.taxId` references a fixed Tax-slab master not a free-form percentage, shows the resolved rate as a suggestion with an Apply link rather than silently overwriting the field. **Scope note**: wiring "GST Validate" into the live Sale/Purchase invoice entry screens (spec 7.4/7.5) is deliberately deferred to Stage GST-7/8 (Sale/Purchase GST Review), which are already scoped for exactly that line-level integration - avoids a second, redundant wiring pass into revenue-critical live billing screens. Validated: `dotnet build` (0 errors), full backend test suite (72 passed, 0 regressions), clean `books-web`/`inventory-web` builds (bundle grep confirmed for books-web), `validate-structure.mjs`, dev-server console-error check on `/gst-tax-rates` (zero errors, redirects to Login).
- [x] **Stage GST-6 (Audit Engine)** - done 2026-07-13, v6.8.7. New `GstAuditEngineService.cs` runs the 17-rule catalog: Product checks (HSN missing/invalid, GST rate missing, UQC missing, GST_RATE_MISMATCH via the Stage GST-5 resolver), Vendor/Customer checks (GSTIN format/inactive, plus GSTIN_STATE_MISMATCH for Customer only since only Customer has a separate `State` field - Vendor doesn't, confirmed by reading the entity), and - after a dedicated schema-research pass confirmed exact `Invoice`/`InvoiceItem`/`PurchaseInvoice` field names - real Sale/Purchase checks: `CGST_SGST_NOT_EQUAL`/`INTRASTATE_IGST_USED`/`INTERSTATE_CGST_SGST_USED` (header `InterState` + CGST/SGST/IGST amounts), `B2B_GSTIN_REQUIRED`, `SALE_LINE_TAX_MISMATCH`/`PURCHASE_LINE_TAX_MISMATCH` (recomputed expected tax from each line's stored MRP/DiscountAmount/TaxPercentage vs the stored `TaxAmount` - these are genuinely separate persisted columns that can drift, confirmed via research, not a computed-property comparison that could never fail), `GST_RATE_MISMATCH`/`GARMENT_THRESHOLD_RATE_MISMATCH` per line, and `INVOICE_TOTAL_TAX_MISMATCH`. Capped at 2000 lines/module per run. New `GstAuditEndpoints.cs` (`/api/gst/audit`): rules CRUD (Admin-gated), `POST /run` (Edit-gated), findings list + ignore/mark-fixed/accept. New page `apps/books/pages/gst-tax-audit.vue` (run panel, findings list, rules table). Validated: `dotnet build` (0 errors on first attempt), full backend test suite (72 passed, 0 regressions), clean `books-web` build (bundle grep confirmed), `validate-structure.mjs`, dev-server console-error check (zero errors, redirects to Login).
- [x] **Stage GST-7 (Sale GST Review)** - done 2026-07-13, v6.8.8, implemented autonomously via the scheduled GST continuation task. Research found the existing apparel-threshold "Sale Review" (`SaleReviewEndpoints.cs`, `/api/sale-review`, hardcoded 5% up to Rs.2,499/18% above, with a tested Option-B correction+posting workflow) had **zero modular frontend** - only a legacy-only page existed at `frontend/legacy/garmetix-web/pages/billing/sale-review.vue`. New `GstSaleReviewEndpoints.cs` (`/api/gst/sale-review`, read-only): resolves each Sale line's GST rate live against the configurable Rate Master (Stage GST-5, incl. garment slabs) instead of a hardcoded threshold, compares to the stored line rate, and cross-links each invoice to its open GST Audit Engine finding count (Stage GST-6) via `GstAuditFindings.InvoiceId` - added `InvoiceId`/`PurchaseInvoiceId` to `GstAuditFindingDto` and an optional filter to `GET /api/gst/audit/findings` as a small reusable addition (useful for Stage GST-8 too). Capped at 2000 lines/request (mirrors the Stage GST-6 cap, since each line resolves live). New page `apps/books/pages/gst-tax-sale-review.vue`: one page, two sections - "Apparel Threshold Review" ports the legacy UI onto the existing, untouched `/api/sale-review` endpoints (this closes the "no modular UI" gap and is the literal "merge/link with existing Sale Review"), and "GST Rate Master Line Check" is the new configurable-rate view with an Open Audit Findings link to `/gst-tax-audit`. **Deliberate scope decision**: kept the new section read-only - reused the existing, already-tested Option-B posting endpoint as-is rather than building a second financial-mutation path for the rate-resolver view. Route `/gst-tax-sale-review`, added to Books menu + dashboard quick actions. Validated: `dotnet build` (0 errors), full backend test suite (72 passed, 0 regressions), clean `books-web` build (bundle grep confirmed), `validate-structure.mjs`, dev-server console-error check (zero errors, redirects to Login).
- [x] **Stage GST-8 (Purchase GST Review)** - done 2026-07-13, v6.8.9, implemented autonomously via the scheduled GST continuation task. Confirmed via research there is no existing hardcoded-threshold "Purchase Review" tool to merge with (unlike Sale Review's `SaleReviewEndpoints.cs`) - this is the first Purchase GST review surface in the codebase. New `GstPurchaseReviewEndpoints.cs` (`/api/gst/purchase-review`, read-only): resolves each Purchase line's GST rate live against the Rate Master (Stage GST-5) exactly like Sale GST Review (Stage GST-7), cross-links each invoice to its open GST Audit Engine finding count (Stage GST-6, `ModuleArea == "Purchase"` via `GstAuditFindings.PurchaseInvoiceId`, already exposed by Stage GST-7's findings filter), and adds a per-invoice **ITC eligibility heuristic** (documented as a heuristic, not a formal ledger calc - this codebase has no ITC ledger table): "Not Eligible" when the purchase invoice carries no vendor GSTIN, "At Risk" when the vendor master's GST registration status is on record as not Active, "Unverified" when a GSTIN is present but never confirmed via GSTIN Verification (Stage GST-3), "Eligible" otherwise. Capped at 2000 lines/request (same cap as Sale GST Review). New page `apps/books/pages/gst-tax-purchase-review.vue`: filters (date range, store, vendor, search, only-issues), a rate-mismatch summary row plus a dedicated ITC summary row, an invoice table (stored vs resolved tax, Open Findings link to `/gst-tax-audit`, ITC status badge + note), and a line table (HSN/category, stored vs resolved %, matched rule, Garment Threshold badge) with pagination - a single section (unlike Sale GST Review's two), since there's no legacy apparel-threshold Purchase tool to port alongside it. **Purchase-import (OCR) scope note**: covered automatically once a batch is posted, since posting writes into the same `PurchaseInvoices`/`PurchaseInvoiceItems` tables this reads from; unposted import drafts are intentionally out of scope (not yet real financial invoices). Route `/gst-tax-purchase-review`, added to Books menu + dashboard quick actions. Validated: `dotnet build` (0 errors), full backend test suite (72 passed, 0 regressions), clean `books-web` build (bundle grep confirmed), `validate-structure.mjs`, dev-server console-error check (zero errors, redirects to Login).
- [x] **Stage GST-9 (GST Returns & CA Export)** - done 2026-07-13, v6.8.10. Research first: confirmed the pre-existing GST Returns module (`/api/gst-returns`, `GstReturnEndpoints.cs`/`GstReturnExportService.cs`) already fully covers GSTR-1/GSTR-3B/HSN summary/tax-summary/invoice-register with CSV and CA-ready Excel export - the one genuine gap is that the GSTR-3B builder's "4 ITC" section is entirely manual entry (its own export literally notes "Manual/separate GST module export. Not linked to Billing/Purchase yet."). This stage closes that gap: a real **ITC Register** auto-computed from posted Purchase invoices. Extracted Stage GST-8's ITC eligibility heuristic out of `GstPurchaseReviewEndpoints.cs` into a new shared `GstItcEligibilityService.cs` so Purchase GST Review and the new register can never disagree (refactored the former to call the latter). New `GstItcRegisterEndpoints.cs` (`/api/gst/itc-register`, read-only, `GarmetixPolicies.Gst`): paginated JSON, `/csv`, and `/excel` - the Excel export reuses the existing `SimpleXlsxBuilder`/`XlsxSheet` OOXML writer from `GstReturnExportService.cs` (building on existing infrastructure per the todo's own suggestion, not duplicating an Excel writer). Each row is a Purchase invoice with taxable value/CGST/SGST/IGST/tax (same `NetAmount`/CGST-SGST-IGST-fallback pattern as the existing GST Returns invoice-register report), its ITC status, and an "eligible ITC amount" (full tax when Eligible/Unverified, zero when At Risk/Not Eligible). Capped at 2000 rows/request. New page `apps/books/pages/gst-tax-itc-register.vue`: filters, totals + status-breakdown summary rows, a paginated register table, and Export CSV / CA Excel Export buttons wired through the existing shared `download()` helper in `books-api.ts` (reused, not reimplemented). **Deliberately does not touch the existing manual GSTR-3B ITC block** - the new register is documented on the page as a source to cross-check those manually-entered numbers against, not a replacement. Route `/gst-tax-itc-register`, added to Books menu + dashboard quick actions. Validated: `dotnet build` (0 errors, incl. after the eligibility-service refactor), full backend test suite (72 passed, 0 regressions), clean `books-web` build (bundle grep confirmed), `validate-structure.mjs`, dev-server console-error check (zero errors, redirects to Login).
- [ ] **Stage GST-10 (E-Invoice/E-Way Bill placeholders)** - `gst_einvoice_irn_records`/`gst_ewaybill_records` tables (not yet created), provider-ready structure, "not configured" messaging when no provider is set.

## Process Reminders

- [ ] Before any backend/API/DB change: confirm with Amit if it can affect `frontend/legacy/garmetix-web`.
- [ ] After any code change: update `.claude/changelog.md` and root `CLAUDE.md`.
