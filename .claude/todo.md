# Active Todo (Claude View)

Last updated: 2026-07-07. Check items off as completed; move detail/history into `changelog.md` once done. Codex's own module-lane TODOs live in `.codex/*_TODO.md` and `frontend/modular/docs/MODULAR_TODO.md` - this list is Claude-facing and bug/analysis driven.

## Bugs / Issues Found (2026-07-07 analysis pass)

- [x] **Security (fixed 2026-07-10, v6.2.4)**: `backend/Garmetix.Api/Backup/FactoryResetEndpoints.cs:17` was gating factory reset behind `GarmetixPolicies.Admin`, which - via `AccessPermissionMatrix.CanAccessPolicy`'s blanket `IsAdminOrOwner` short-circuit - actually admitted SuperAdmin, Admin, and Owner alike, not SuperAdmin only. Added a standalone `GarmetixPolicies.SuperAdmin` policy registered directly in `Program.cs` (bypassing the shared matrix mechanism, which can't express "stricter than Admin/Owner"), asserting on the existing `superAdmin` JWT claim via a newly-public `AccessPermissionMatrix.IsSuperAdmin`. The route now requires that policy; the existing handler-level DB-backed SuperAdmin check was left in place as defense-in-depth. No modular UI exists for factory-reset (only legacy has one), so no frontend change was needed.
- [ ] **Data modeling**: `backend/Garmetix.Domain/Generated/Models/Inventory/Invoicing.cs:289-290` - fields are stored in DB that a `//TODO` comment says should instead be calculated, plus missing `JsonIgnore`. Worth a follow-up to confirm this isn't causing drift between stored and true values on invoices.
- [ ] **Incomplete domain model**: `backend/Garmetix.Domain/Generated/Models/Inventory/Inventory.cs:39` - Fabric/UOM (unit of measurement) support is not implemented; relevant if fabric-based (as opposed to piece-based) inventory is ever needed.
- [ ] **Incomplete domain model**: `backend/Garmetix.Domain/Generated/Models/Inventory/Inventory.cs:108` - a "Basic Rate Calculator" static toolkit function referenced as needed but not yet created; check whether rate calculation logic is duplicated ad hoc elsewhere in the codebase as a result.
- [ ] **Incomplete enum**: `backend/Garmetix.Domain/Generated/Enums/BharatEnums.cs:58` - card-type enum is explicitly marked incomplete.
- [x] **Repo hygiene (already fixed, confirmed 2026-07-10)**: `.gitattributes` already has `* text=auto` on line 1 (added 2026-07-07 in commit `9259403`, alongside a factory-reset handler tightening). This item was stale in the list - no action needed.
- [x] **CRM bug (found 2026-07-07, live on SRP, confirmed fixed 2026-07-08)**: `/crm/customers` build-collided with its own `customers/new`/`customers/dues-reconciliation` sub-routes during static prerendering. Spot-checked live after the 2026-07-08 full-site redeploy: `/crm/customers` now serves real content directly (2911 bytes, matches every other app-shell page size), and the trailing-slash variant does a single clean redirect to the no-slash URL rather than looping. Resolved via the separately-spawned background fix and/or the 14F.6B deep-route trailing-slash normalization - not something this session touched directly.
- [x] **Nitro flaky cold-build bug (found + mitigated 2026-07-07)**: a from-scratch `nuxt generate` non-deterministically crashes or silently emits a 16-byte `"Redirecting..."` stub instead of real page content for every route of every modular app (confirmed unrelated to GST source changes). Mitigated with a retry+content-size-verification safety net in `frontend/modular/deploy/srp-whole-site-deploy.sh`'s `build_app()`. Underlying Nitro/Nuxt root cause not fixed upstream, just worked around.
- [x] **npm workspace install bug (found + fixed 2026-07-07)**: every app/shared-package `package.json` pinned internal `@garmetix/shared-*` deps at a stale exact version (`"6.0.0"`) not matching the real package version, causing npm 11.13 to 404 against the public registry for these private package names and fail the whole install. Fixed by relaxing all pins to `"*"`.
- [x] **Deploy script `dotnet publish` Git-Bash path bug (found 2026-07-07, fixed 2026-07-08)**: `srp-whole-site-deploy.sh`'s dotnet publish step failed with a doubled drive-letter path (`C:\c\AIArea\...`) under Git Bash. Root cause: Git Bash/MSYS's own implicit argv-to-Windows-path conversion for the native `dotnet` binary (not `wslpath`, which an earlier fix attempt wrongly targeted). Fixed via explicit `cygpath -w` in `dotnet_path_arg()`, verified against a real `--build-only` deploy run. Commit `99a835d`.

## Non-Bugs Confirmed Clean (checked, no action needed)

- [x] No `rejectUnauthorized: false` / disabled TLS verification anywhere in frontend or backend.
- [x] No hardcoded production domains found inside modular app source (`frontend/modular/apps`, `frontend/modular/packages`).
- [x] No stray `TODO`/`FIXME`/`console.log` left in modular `apps`/`packages` Vue/TS source.
- [x] `node frontend/modular/scripts/validate-structure.mjs` passes.

## Follow-Up Work (from roadmap, actionable slice)

- [x] **Stage-aware database backup protocol** (done 2026-07-14): `srp-backup-database.sh` now requires `--stage=<StageName>`, stores dumps/checksums/history under `/opt/garmetix/backup/database/`, and `srp-whole-site-deploy.sh` runs the backup before real upload/install deploys.
- [~] **BS-16 Accounting Master Audit**: local read-only audit tooling is implemented on `version6`. Before SRP deploy or live audit, run `npm --prefix frontend/modular run deploy:srp:backup -- --stage=BS16AccountingMasterAudit` on the deployed host, then deploy/pull and capture `GET /api/final-accounts/audit/accounting-master`. Do not start BS-17/18/19 mutation until that backup/history row and audit evidence exist.
- [~] **BS-17 Indian/Tally-compatible COA normalization**: local read-only preview tooling is implemented on `version6`. Before SRP deploy/live review, run `npm --prefix frontend/modular run deploy:srp:backup -- --stage=BS17IndianCOANormalization` on the deployed host, then capture `GET /api/final-accounts/audit/coa-normalization`. Do not mutate ledger groups or relink ledgers until Amit/CA approves the preview.
- [~] **BS-18 Party/ledger unification**: local read-only preview tooling is implemented on `version6`. Before SRP deploy/live review, run `npm --prefix frontend/modular run deploy:srp:backup -- --stage=BS18PartyLedgerUnification` on the deployed host, then capture `GET /api/final-accounts/audit/party-ledger-unification`. Do not relink PartyId fields or merge ledgers until Amit/CA approves the preview.
- [~] **BS-19 Transaction backfill and reconciliation**: local read-only evidence tooling is implemented on `version6`. Before SRP deploy/live review, run `npm --prefix frontend/modular run deploy:srp:backup -- --stage=BS19TransactionBackfillReconciliation` on the deployed host, then capture `GET /api/final-accounts/audit/transaction-backfill-reconciliation`. Do not run live backfill until Amit/CA approves the evidence.
- [~] **BS-20 Final Accounts direct ledger integration**: local read-only direct-ledger evidence tooling is implemented on `version6`. Before SRP deploy/live review, run `npm --prefix frontend/modular run deploy:srp:backup -- --stage=BS20FinalAccountsLedgerIntegration` on the deployed host, then capture `GET /api/final-accounts/audit/direct-ledger-integration`. Do not switch Final Accounts report sources until Amit/CA approves the evidence.
- [~] **BS-21 Restore drill and production safety**: local restore-drill tooling is implemented on `version6`. Before any production accounting migration, run `npm --prefix frontend/modular run deploy:srp:restore-drill -- --confirm-non-production-restore --stage=BS21RestoreDrillProductionSafety` on the deployed host and capture `/opt/garmetix/backup/database/RestoreDrillHistory.md`. Do not run live restore/migration/backfill until Amit approves the evidence.
- [x] Books Voucher Register filters (done 2026-07-12, v6.8.1): added date range, month/year, voucher type, ledger type/group, exact ledger and text-search filters to modular Books `vouchers.vue`. Frontend-only; no API/DB/deploy/commit performed so Claude can keep handling Books module coordination.
- [x] Books Day Book port (done 2026-07-12, v6.8.3): added modular `/day-book` from legacy Accounting menu with filters, pagination, CSV export, print/PDF evidence, detail slideover, quick-create actions and modular source-link mapping. Frontend-only; no API/DB/GST-module/deploy/commit performed.
- [x] Books Debit/Credit Notes modal parity + Admin Dot Matrix Print (done 2026-07-13, v6.8.4): modular Debit Notes and Credit Notes existed, but their registers still linked to separate New/Detail pages. Added a shared `CommercialNoteRegister` so New/Edit forms and details open in slide-over panels while direct routes remain usable. Ported legacy `/dot-matrix-print` into modular Admin with settings, test print, queue stats/actions and printable text preview. Frontend-only; no API/DB/GST-module/deploy/commit performed.
- [x] Books Stage 14C.5 (GST/accounting report finalization, financial-year lock acceptance, final Books closure) - closed 2026-07-07. See `.claude/changelog.md` for detail. Books modular parity lane is done pending live-token/manual evidence.
- [x] Books Stage 14G (full legacy GST menu parity: GSTR-1/3B builder, CA share, accounting-gst-validation, gst-final-acceptance) - closed 2026-07-07, deployed to `.127` SRP. The first deploy attempt's acceptance check falsely showed all-green (status-code-only); Amit caught the real bug from his browser, which led to fixing two real infra bugs (Nitro flaky build, npm workspace pin) and a genuinely-verified redeploy. See `.claude/changelog.md` for the full trail.
- [x] Start Stage 14F.7 MCP wrapper for the AI Sense read-only tool catalog once Assistant is confirmed stable. Done 2026-07-08: `POST /api/mcp`, gated behind `Assistant:McpEnabled` (default off), additive wrapper around `AssistantToolCatalog`, no live MCP-client test yet (no test creds/DB in this environment).
- [ ] Ask Amit whether to prioritize clearing the "pending live evidence" backlog (POS/HR/Books/CRM/Admin) over new feature work.
- [ ] Begin AntiGravity (`Version6.A`) port review starting with Priority 0 (file-list diff) from `AntiGarvity2CodeTODO.md` - do not read Antigravity workspace files beyond what's needed for a named comparison without asking first.

## Purchase Import satellite pages + Tailoring & Alteration (done 2026-07-10, v6.2.1-6.2.2)

Amit asked for this work immediately instead of waiting for the scheduled overnight run (which was cancelled - see `.claude/roadmap.md`). All four planned parts shipped:

- [x] **v6.2.1 - Import Acceptance QA dashboard** (`/purchase/import-acceptance`) - ported from legacy (530 lines). Zero backend changes.
- [x] **v6.2.1 - Import Learning (vendor OCR profiles)** (`/purchase/import-profiles`) - ported from legacy (294 lines), includes the parser-template selector ("Tally style" parsing was already covered by the existing `tally-prime`/`tally-prime-mrp` parser templates - not a separate feature). Zero backend changes.
- [x] **v6.2.1 - Tailoring & Alteration module port** (`/tailoring`) - full rewrite of the 11-line placeholder stub, ported field-for-field from legacy (716 lines) against the pre-existing `/api/tailoring/*` backend. Zero backend changes for this part.
- [x] **v6.2.2 - Vendor type distinction (schema change)** - added a nullable `VendorType?` column to the `Vendor` entity (`backend/Garmetix.Domain/Generated/Models/Inventory/Invoicing.cs`), reusing the previously-orphaned `VendorType` enum (`BharatEnums.cs`, has a `Tailoring` literal). EF migration `20260710120000_AddVendorType.cs` + model snapshot updated by hand (this repo's migrations are hand-written, not `dotnet ef`-generated - confirmed via `git log` on the Migrations folder). `TailoringEndpoints.cs`'s vendor list/create now use the real enum instead of the old hardcoded-label workaround; `GET /api/vendors` excludes `VendorType.Tailoring` by default. **Backward-compat note**: pre-migration vendor rows have `VendorType == null` and are treated as visible-in-both-lists (can't retroactively know which pool they belonged to) - only vendors created after this change are cleanly separated. Removed the now-obsolete `forbid('item.VendorType')` regression-guard assertions in `legacy/scripts/validation/stage8e-package7-compile-hotfix-static-checks.py` (they checked for the exact old workaround text this change intentionally replaced). `dotnet build` on `Garmetix.Api` passes clean. Migrations apply automatically on API startup (`db.Database.MigrateAsync()` in `Program.cs`) - no manual production DB step needed, but this part deploys without `--skip-api` since the API binary changed.

## Purchase Module Port (started 2026-07-10, v6.1.0)

Amit asked to port the legacy Purchase menu group (`frontend/legacy/garmetix-web`, live reference `https://garmetix.aadwikafashion.in/purchase`) into modular, since it was previously stub/read-only. Full legacy inventory (11 pages, all backend endpoints) is documented in the session record; summarized here for future pickup.

**Shipped in this stage (Phase 1 - full CRUD, matches legacy feature set for the core workflow)**:
- [x] `frontend/modular/apps/main/pages/vendors.vue` - new Vendors CRUD page (create/edit/delete popups, GSTIN lookup, filter + pagination). Was entirely missing before.
- [x] `frontend/modular/apps/main/pages/purchase/index.vue` - Purchase Register rebuilt from read-only recent-list to full paginated/filtered register with View/Edit/Pay/Cancel/Delete popups.
- [x] `frontend/modular/apps/main/pages/purchase/new.vue` - New Inward dedicated full page (was a placeholder stub), vendor + item entry + payment, posts to `POST /api/purchase/inward`.
- [x] `frontend/modular/apps/main/pages/purchase-return.vue` - Purchase Return register + return-items workflow (was a placeholder stub).
- [x] `frontend/modular/apps/books/pages/vendor-payments.vue` - upgraded from "Read only" to full CRUD (create against invoice or as vendor advance, edit, delete) with server pagination.
- [x] `frontend/modular/apps/books/pages/vendor-settlements.vue` - added the "Debit Notes Available For Settlement" register + Settle popup (was "Read only", view-only).
- [x] Fixed a dangling-route sidebar bug: `vendor-payments`/`vendor-settlements` had routes but no `ModularAppShell.vue` `localMenus` entry (same pattern as the earlier HR/Books menu bugs), plus added `vendors` to the Purchase menu group.

**Deferred to a future session (Phase 2 - not started, too large for one pass)**:
- [x] **Import/Scan Supplier Invoice core workflow** (`/purchase/import`) - done 2026-07-10, v6.2.0. `frontend/modular/apps/main/pages/purchase/import.vue` - full upload/review/edit/post pipeline against ~20 of the 34 `PurchaseInvoiceImportEndpoints.cs` endpoints (uploads, batch get/list/update, match-product, split-by-size, generate-missing-barcodes, override/recheck-duplicate, distribute-discount, reparse-text, post, reject, delete, proof, extracted-text, product-matches). **Not yet click-through tested** - login-gated environment with no test credentials and no sample invoice file to actually exercise the OCR pipeline; only build + no-console-error verified. Treat as needing a real first-use pass before relying on it.
- [x] **Import Acceptance QA dashboard** (`/purchase/import-acceptance`) - done 2026-07-10, v6.2.1. Also picked up most of the "secondary features" originally deferred here: audit-json/lines-csv per-batch export, acceptance status update (Pass/Fail/NeedsRetest), and correction-plan/request-correction are all wired on this page.
- [x] **Import Learning profiles** (`/purchase/import-profiles`) - done 2026-07-10, v6.2.1. Vendor OCR alias/ignored-pattern management, parser-template selector, `vendor-profiles` CRUD.
- [x] **Import features now wired (done 2026-07-10, v6.2.5)**: `cleanup-history` - new Cleanup History panel on `import-acceptance.vue` (older-than-days + status checkboxes + delete-files toggle, confirm dialog). `correction-safety` - the row-level correction action on `import-acceptance.vue` now opens a Correction Safety Check panel first (canDeleteDraft/canRejectDraft/directUndoAvailable/safe actions/warnings) before the operator proceeds to the existing `request-correction` call. Multi-file listing/download - `import.vue` gained a Files button/modal (`GET batches/{id}/files`) with per-file download (`GET batches/{id}/files/{fileId}/download`), previously only the single proof file was reachable. All three backend endpoints already existed fully implemented; this was pure frontend wiring, zero backend changes.
- [x] **Vendor Payable Reconciliation** (`/purchase/vendor-payable-reconciliation`) - done 2026-07-10, v6.1.1. Read-only QA dashboard calling the pre-existing `VendorPayableReconciliationEndpoints.cs`, mirrors the `accounting-gst-validation.vue` pattern (date filter, metrics, issues, paginated evidence table, CSV export).
- [x] **Purchase Return Advanced Settlement QA** (`/purchase-return/advanced-settlement`) - done 2026-07-10, v6.1.1. Same pattern, calls the pre-existing `PurchaseReturnAdvancedSettlementEndpoints.cs`. Required moving `apps/main/pages/purchase-return.vue` to `purchase-return/index.vue` first (a top-level file with the same name as a directory it needs to share breaks Nuxt nested routing unless it's set up as a layout wrapper - fixed to match the codebase's established `index.vue`-in-a-directory convention).

## GST & Taxes Module (started 2026-07-12, v6.7.0, Stage GST-1 done)

Amit gave a full 22-section spec for a new "GST & Taxes" module (multi-provider GST API architecture with UI-configurable encrypted credentials, HSN/rate master, GST audit engine, Sale/Purchase GST review, GST returns/CA export, E-Invoice/E-Way Bill placeholders). This is a large, explicitly staged build (the spec itself defines Stage GST-1 through GST-10) - being delivered stage-by-stage like every other multi-stage module in this project.

- [x] **Stage GST-1 (Foundation)** - done 2026-07-12, v6.7.0. 11 new backend tables (`gst_api_providers`/`features`/`credentials`, `gst_api_call_logs`, `gstin_verification_cache`, `gst_hsn_master`, `gst_tax_rate_rules`, `gst_state_codes`, `gst_uqc_codes`, `gst_audit_rules`, `gst_audit_findings`), first-ever backend credential-encryption service (`GstCredentialProtector`, ASP.NET Core Data Protection), `GET /api/gst/dashboard` + `GET /api/gst/providers`, new `GarmetixPolicies.Gst` policy (also fixed a pre-existing gap: the `"GST"` module string was listed in `AccessPermissionMatrix.AllModules`/profile lists but never actually wired into `ModuleRoles`/`CanAccessPolicy` - it would have always evaluated false), startup seed data (Indian GST state codes, UQC codes, 17-rule audit catalog, default garment 5%/18% price-threshold rate rule at Rs.1000, "Local Master" provider). Frontend: renamed Books' "GST" sidebar group to "GST & Taxes", added `gst-tax-dashboard.vue` as its landing page with empty/zeroed cards for metrics later stages will populate. Confirmed via research this app already has `/api/gst-returns` (GSTR-1/3B/HSN summary/CA share) and `/api/gstin` (single env-configured GSTIN lookup) - Stage 1 builds the new multi-provider/audit/master-data layer and links to what exists rather than duplicating it. Also fixed 3 pre-existing brittle version-string assertions (`books-stage14c5-closure.mjs`, `books-stage14g-closure.mjs`, `books-parity-baseline.mjs`) that hard-pinned `6.0.x`/`Stage 14`, which would fail forever past that version regardless of this stage's changes - same durability-bug class already fixed once before in this project's history. Validated: `dotnet build` (0 errors), full backend test suite (72 passed, 3 pre-existing Postgres-only skipped), clean `books-web` build, `validate-structure.mjs`.
- [x] **Stage GST-2 (API Setup UI)** - done 2026-07-12, v6.8.0. Full provider CRUD (`GET/POST/PUT/DELETE /api/gst/providers`, enable/disable), a `GET /api/gst/providers/catalog` (provider types/environments/feature codes/credential keys, one source of truth for both backend validation and frontend dropdowns), encrypted credential save/mask/clear (`GET/PUT /api/gst/providers/{id}/credentials`, values are never sent back decrypted), and test-health/test-gstin/test-hsn endpoints (fully functional for the seeded Local Master provider against local HSN/GSTIN-cache tables, honest "arrives in a later stage" message for every other provider type, every attempt logged to `GstApiCallLogs`). Mutating endpoints gated at `GarmetixPolicies.Admin`. New page `apps/books/pages/gst-tax-setup.vue` (provider table, guarded delete requiring typing `DELETE PROVIDER`, create/edit modal with feature checkboxes + priority/fallback + credentials panel + inline test buttons), route `/gst-tax-setup` (Owner/Admin only), added to the Books "GST & Taxes" menu and dashboard quick actions, plus a "Configure GST API" cross-app link card added to the Admin app's existing Company Setup page (`/setup`). Caught a real bug pre-ship: `BooksMasterTable` only supports flat string columns + one generic `actions` slot, not per-cell slots like `#cell-isEnabled` - reworked the table to match its actual contract. Validated: `dotnet build` (0 errors), full backend test suite (72 passed, 0 regressions), clean `books-web`/`admin-web` builds (compiled-bundle grep confirmed), `validate-structure.mjs`, `books-accounting-contract-check.mjs`, and a dev-server console-error check on both new/changed routes (zero errors, both correctly redirect to Login).
- [x] **Stage GST-3 (GSTIN Verification)** - done 2026-07-13, v6.8.2. `GstinResolutionService.cs`: local-cache-first (unless force-refresh) -> enabled providers by priority for feature `GSTIN_LOOKUP` (first real adapter, `GenericRestProvider`, does an actual HTTP GET with API_KEY/AUTH_TOKEN credential injection and flexible JSON parsing; every other provider type stays honest "later stage" per Stage GST-2's precedent) -> next provider only if `FallbackEnabled` -> local format/state-code-only answer. Every attempt logged to `GstApiCallLogs`, every success cached to `GstinVerificationCaches`. New endpoints `POST /api/gst/gstin/verify`, `GET /api/gst/gstin/cache` (+`/{gstin}`), `POST /api/gst/gstin/cache/manual`. New page `apps/books/pages/gst-tax-gstin-verify.vue` (verify + force-refresh, result panel, Copy Details, recent-cache table, Update Vendor/Update Customer search-and-apply). **Deliberately left the existing "Check GSTIN" buttons on Vendor Master (main app) and Customer Master (CRM `CustomerForm.vue`) untouched** - they already work against the older single-provider `/api/gstin` module; replacing working data-entry functionality wasn't worth the regression risk for this stage, the new GST & Taxes page is additive. Route `/gst-tax-gstin-verify`, added to Books menu + dashboard quick actions. Validated: `dotnet build` (0 errors), full backend test suite (72 passed, 0 regressions), clean `books-web` build (compiled-bundle grep confirmed), `validate-structure.mjs`, `books-accounting-contract-check.mjs`, dev-server console-error check (zero errors, redirects to Login).
- [x] **Stage GST-4 (HSN/SAC Master)** - done 2026-07-13, v6.8.5. New `GstHsnEndpoints.cs` (`/api/gst/hsn`): full CRUD, filtered/paginated list with a per-row Product mapping count (grouped-count query, not N+1), `/search` for autocomplete, `/bulk-status` for bulk activate/deactivate, CSV import/export/template. **Deliberate, disclosed scope adaptation**: bulk import accepts CSV not `.xlsx` - this codebase has no Excel-reading library (only a write-only `SimpleXlsxBuilder`), and a hand-rolled OpenXML parser for arbitrary uploaded workbooks is a real correctness risk for a data-load path; CSV covers the same need with far less risk. New page `apps/books/pages/gst-tax-hsn.vue` (filterable/paginated table, checkbox multi-select bulk activate/deactivate, create/edit modal, Import CSV/Download Template). Added `postForm` to `books-api.ts` (mirroring `main-api.ts`'s existing multipart helper, Books never needed one before). Wired a lightweight debounced HSN autocomplete + "Missing HSN" warning into the Inventory app's existing `products.vue` (calls the new `/gst/hsn/search` endpoint), rest of that form untouched. Route `/gst-tax-hsn`, added to Books menu + dashboard quick actions. Validated: `dotnet build` (0 errors, incl. a self-caught CA2024 warning fixed pre-commit), full backend test suite (72 passed, 0 regressions), clean `books-web`/`inventory-web` builds (bundle grep confirmed for books-web), `validate-structure.mjs`, dev-server console-error check on `/gst-tax-hsn` (zero errors; inventory-web has no dev-server launch entry in this environment, verified via clean build only).
- [x] **Stage GST-5 (GST Rate Master)** - done 2026-07-13, v6.8.6. New `GstRateResolutionService.cs` implements the resolve algorithm (spec section 9): HSN match first, then product category, filter by the rule's price threshold (the garment slab), highest-priority match wins, split into CGST/SGST (intra-state) or IGST (inter-state) unless the rule has explicit split rates, fall back to the HSN master's own default rate, then an honest "nothing configured" result. Uses invoice date, never "today". New `GstRateEndpoints.cs` (`/api/gst/rates`): full CRUD + `POST /resolve`. New page `apps/books/pages/gst-tax-rates.vue` (rule table, create/edit modal, live "Resolve A Rate" test panel). Product Master: added a "Resolve" button next to the existing Tax dropdown in Inventory's `products.vue` - since `Product.taxId` references a fixed Tax-slab master not a free-form percentage, shows the resolved rate as a suggestion with an Apply link rather than silently overwriting the field. **Scope note**: wiring "GST Validate" into the live Sale/Purchase invoice entry screens (spec 7.4/7.5) is deliberately deferred to Stage GST-7/8 (Sale/Purchase GST Review), which are already scoped for exactly that line-level integration - avoids a second, redundant wiring pass into revenue-critical live billing screens. Validated: `dotnet build` (0 errors), full backend test suite (72 passed, 0 regressions), clean `books-web`/`inventory-web` builds (bundle grep confirmed for books-web), `validate-structure.mjs`, dev-server console-error check on `/gst-tax-rates` (zero errors, redirects to Login).
- [x] **Stage GST-6 (Audit Engine)** - done 2026-07-13, v6.8.7. New `GstAuditEngineService.cs` runs the 17-rule catalog: Product checks (HSN missing/invalid, GST rate missing, UQC missing, GST_RATE_MISMATCH via the Stage GST-5 resolver), Vendor/Customer checks (GSTIN format/inactive, plus GSTIN_STATE_MISMATCH for Customer only since only Customer has a separate `State` field - Vendor doesn't, confirmed by reading the entity), and - after a dedicated schema-research pass confirmed exact `Invoice`/`InvoiceItem`/`PurchaseInvoice` field names - real Sale/Purchase checks: `CGST_SGST_NOT_EQUAL`/`INTRASTATE_IGST_USED`/`INTERSTATE_CGST_SGST_USED` (header `InterState` + CGST/SGST/IGST amounts), `B2B_GSTIN_REQUIRED`, `SALE_LINE_TAX_MISMATCH`/`PURCHASE_LINE_TAX_MISMATCH` (recomputed expected tax from each line's stored MRP/DiscountAmount/TaxPercentage vs the stored `TaxAmount` - these are genuinely separate persisted columns that can drift, confirmed via research, not a computed-property comparison that could never fail), `GST_RATE_MISMATCH`/`GARMENT_THRESHOLD_RATE_MISMATCH` per line, and `INVOICE_TOTAL_TAX_MISMATCH`. Capped at 2000 lines/module per run. New `GstAuditEndpoints.cs` (`/api/gst/audit`): rules CRUD (Admin-gated), `POST /run` (Edit-gated), findings list + ignore/mark-fixed/accept. New page `apps/books/pages/gst-tax-audit.vue` (run panel, findings list, rules table). Validated: `dotnet build` (0 errors on first attempt), full backend test suite (72 passed, 0 regressions), clean `books-web` build (bundle grep confirmed), `validate-structure.mjs`, dev-server console-error check (zero errors, redirects to Login).
- [x] **Stage GST-7 (Sale GST Review)** - done 2026-07-13, v6.8.8, implemented autonomously via the scheduled GST continuation task. Research found the existing apparel-threshold "Sale Review" (`SaleReviewEndpoints.cs`, `/api/sale-review`, hardcoded 5% up to Rs.2,499/18% above, with a tested Option-B correction+posting workflow) had **zero modular frontend** - only a legacy-only page existed at `frontend/legacy/garmetix-web/pages/billing/sale-review.vue`. New `GstSaleReviewEndpoints.cs` (`/api/gst/sale-review`, read-only): resolves each Sale line's GST rate live against the configurable Rate Master (Stage GST-5, incl. garment slabs) instead of a hardcoded threshold, compares to the stored line rate, and cross-links each invoice to its open GST Audit Engine finding count (Stage GST-6) via `GstAuditFindings.InvoiceId` - added `InvoiceId`/`PurchaseInvoiceId` to `GstAuditFindingDto` and an optional filter to `GET /api/gst/audit/findings` as a small reusable addition (useful for Stage GST-8 too). Capped at 2000 lines/request (mirrors the Stage GST-6 cap, since each line resolves live). New page `apps/books/pages/gst-tax-sale-review.vue`: one page, two sections - "Apparel Threshold Review" ports the legacy UI onto the existing, untouched `/api/sale-review` endpoints (this closes the "no modular UI" gap and is the literal "merge/link with existing Sale Review"), and "GST Rate Master Line Check" is the new configurable-rate view with an Open Audit Findings link to `/gst-tax-audit`. **Deliberate scope decision**: kept the new section read-only - reused the existing, already-tested Option-B posting endpoint as-is rather than building a second financial-mutation path for the rate-resolver view. Route `/gst-tax-sale-review`, added to Books menu + dashboard quick actions. Validated: `dotnet build` (0 errors), full backend test suite (72 passed, 0 regressions), clean `books-web` build (bundle grep confirmed), `validate-structure.mjs`, dev-server console-error check (zero errors, redirects to Login).
- [x] **Stage GST-8 (Purchase GST Review)** - done 2026-07-13, v6.8.9, implemented autonomously via the scheduled GST continuation task. Confirmed via research there is no existing hardcoded-threshold "Purchase Review" tool to merge with (unlike Sale Review's `SaleReviewEndpoints.cs`) - this is the first Purchase GST review surface in the codebase. New `GstPurchaseReviewEndpoints.cs` (`/api/gst/purchase-review`, read-only): resolves each Purchase line's GST rate live against the Rate Master (Stage GST-5) exactly like Sale GST Review (Stage GST-7), cross-links each invoice to its open GST Audit Engine finding count (Stage GST-6, `ModuleArea == "Purchase"` via `GstAuditFindings.PurchaseInvoiceId`, already exposed by Stage GST-7's findings filter), and adds a per-invoice **ITC eligibility heuristic** (documented as a heuristic, not a formal ledger calc - this codebase has no ITC ledger table): "Not Eligible" when the purchase invoice carries no vendor GSTIN, "At Risk" when the vendor master's GST registration status is on record as not Active, "Unverified" when a GSTIN is present but never confirmed via GSTIN Verification (Stage GST-3), "Eligible" otherwise. Capped at 2000 lines/request (same cap as Sale GST Review). New page `apps/books/pages/gst-tax-purchase-review.vue`: filters (date range, store, vendor, search, only-issues), a rate-mismatch summary row plus a dedicated ITC summary row, an invoice table (stored vs resolved tax, Open Findings link to `/gst-tax-audit`, ITC status badge + note), and a line table (HSN/category, stored vs resolved %, matched rule, Garment Threshold badge) with pagination - a single section (unlike Sale GST Review's two), since there's no legacy apparel-threshold Purchase tool to port alongside it. **Purchase-import (OCR) scope note**: covered automatically once a batch is posted, since posting writes into the same `PurchaseInvoices`/`PurchaseInvoiceItems` tables this reads from; unposted import drafts are intentionally out of scope (not yet real financial invoices). Route `/gst-tax-purchase-review`, added to Books menu + dashboard quick actions. Validated: `dotnet build` (0 errors), full backend test suite (72 passed, 0 regressions), clean `books-web` build (bundle grep confirmed), `validate-structure.mjs`, dev-server console-error check (zero errors, redirects to Login).
- [x] **Stage GST-9 (GST Returns & CA Export)** - done 2026-07-13, v6.8.10. Research first: confirmed the pre-existing GST Returns module (`/api/gst-returns`, `GstReturnEndpoints.cs`/`GstReturnExportService.cs`) already fully covers GSTR-1/GSTR-3B/HSN summary/tax-summary/invoice-register with CSV and CA-ready Excel export - the one genuine gap is that the GSTR-3B builder's "4 ITC" section is entirely manual entry (its own export literally notes "Manual/separate GST module export. Not linked to Billing/Purchase yet."). This stage closes that gap: a real **ITC Register** auto-computed from posted Purchase invoices. Extracted Stage GST-8's ITC eligibility heuristic out of `GstPurchaseReviewEndpoints.cs` into a new shared `GstItcEligibilityService.cs` so Purchase GST Review and the new register can never disagree (refactored the former to call the latter). New `GstItcRegisterEndpoints.cs` (`/api/gst/itc-register`, read-only, `GarmetixPolicies.Gst`): paginated JSON, `/csv`, and `/excel` - the Excel export reuses the existing `SimpleXlsxBuilder`/`XlsxSheet` OOXML writer from `GstReturnExportService.cs` (building on existing infrastructure per the todo's own suggestion, not duplicating an Excel writer). Each row is a Purchase invoice with taxable value/CGST/SGST/IGST/tax (same `NetAmount`/CGST-SGST-IGST-fallback pattern as the existing GST Returns invoice-register report), its ITC status, and an "eligible ITC amount" (full tax when Eligible/Unverified, zero when At Risk/Not Eligible). Capped at 2000 rows/request. New page `apps/books/pages/gst-tax-itc-register.vue`: filters, totals + status-breakdown summary rows, a paginated register table, and Export CSV / CA Excel Export buttons wired through the existing shared `download()` helper in `books-api.ts` (reused, not reimplemented). **Deliberately does not touch the existing manual GSTR-3B ITC block** - the new register is documented on the page as a source to cross-check those manually-entered numbers against, not a replacement. Route `/gst-tax-itc-register`, added to Books menu + dashboard quick actions. Validated: `dotnet build` (0 errors, incl. after the eligibility-service refactor), full backend test suite (72 passed, 0 regressions), clean `books-web` build (bundle grep confirmed), `validate-structure.mjs`, dev-server console-error check (zero errors, redirects to Login).
- [x] **Stage GST-10 (E-Invoice/E-Way Bill placeholders)** - done 2026-07-13, v6.8.11. Explicitly a placeholder stage per the spec. Confirmed the Stage GST-1 provider registry was already provider-ready for this (`GstTaxCatalog.ProviderTypes` already lists `OfficialEInvoiceNIC`/`OfficialEWayBill`, `FeatureCodes` already lists the full `EINVOICE_*`/`EWAYBILL_*` set) - but no adapter calling the real government NIC/e-way bill portals exists, and building one on guessed API shapes would be worse than not building it. New schema: `GstEinvoiceIrnRecords` (per Sale invoice IRN lifecycle) and `GstEwaybillRecords` (nullable `InvoiceId`/`PurchaseInvoiceId`, mirroring `GstAuditFinding`'s dual-link pattern, since e-way bills apply to both Sale and Purchase) - migration + hand-updated model snapshot + the `DatabaseSchemaRepairService` idempotent repair (the mechanism that matters on the SRP `FreshBaseline` host). New `GstEinvoiceEwaybillEndpoints.cs` (`/api/gst/einvoice`, `/api/gst/ewaybill`): `GET` list (B2B Sale invoices for e-invoice; Sale+Purchase for e-way bill) and `POST generate`, which checks the provider registry and returns an honest "no provider configured" or "provider configured but live generation isn't implemented yet" message - mirroring the exact precedent Stage GST-2/3 set for every provider type without a live adapter. **Deliberately no Cancel endpoint** - it would be untestable dead code until live generation exists. Wired the Stage GST-1 dashboard's last two explicit `0` placeholders (`EInvoicePendingCount`/`EWayBillPendingCount`) to real current-month counts. New page `apps/books/pages/gst-tax-einvoice-ewaybill.vue`: two sections (E-Invoice, E-Way Bill), filters, summary cards, status table, Generate button per row. Route `/gst-tax-einvoice-ewaybill`, added to Books menu + dashboard quick actions. Validated: `dotnet build` (0 errors), full backend test suite (72 passed, 0 regressions), clean `books-web` build (bundle grep confirmed), `validate-structure.mjs`, dev-server console-error check (zero errors, redirects to Login).
- [x] **GST & Taxes module status: Stages GST-1 through GST-10 are all complete and deployed live.** The module (multi-provider GST API architecture, encrypted credentials, local-first HSN/rate masters, configurable audit engine, Sale/Purchase GST review, ITC register, GST returns/CA export, e-invoice/e-way bill placeholders) is code-complete and shipped. Deployed 2026-07-13 (Amit's explicit "deploy" instruction, confirmed with target `SRP, 192.168.11.127` after the platform's own safety check required a named-target confirmation for a first-time production push of this scale). Full whole-site deploy (`srp-whole-site-deploy.sh --install-remote`, all 8 frontend apps + the .NET API). Verified live via direct content checks (not just HTTP status, per this project's own hard-learned rule): `/books/` and `/books/gst-tax-audit` both serve real ~2973-byte app-shell HTML (not a flaky-build stub), the compiled bundle contains the new Stage GST-5/9 sidebar menu strings, `/api/gst/dashboard` returns `401` (reachable, correctly auth-gated, not `404`/`502`), and every other app (pos/hr/admin/crm/inventory/ai-sense/main) still serves real content with no regression. The public `https://srp.aadwikafashion.in` HTTPS check failed locally with a Windows curl/schannel certificate-revocation error unrelated to server health (direct LAN IP:8088 checks all passed) - flagged to Amit to spot-check the public URL himself since it wasn't independently confirmed from this environment.

## Swalekha (Personal & Personal Finance) Module - proposed 2026-07-17, pending build approval

Amit asked for a brand-new, completely isolated "Personal & Personal Finance" module - usable only by the Owner login, not part of the app switcher/sidebar, with its own separate database (`swalekha_db`) run by the same shared API process, same domain/login. Full design (naming, isolation architecture, feature list, staged plan) is in `docs/personal-finance-module-design.md`. Design was reviewed and approved via plan mode on 2026-07-17; no application code, migrations, DbContext, policies, or frontend scaffolding exist yet - only this todo entry, the roadmap pointer, and the design doc were written this pass.

- App name: **Swalekha**. URL: `srp.aadwikafashion.in/swalekha`. Access: Owner userType only (SuperAdmin/Admin explicitly refused). Database: separate `swalekha_db` Postgres database, own `SwalekhaDbContext`, own `GarmetixPolicies.SwalekhaOwner` policy (registered directly in `Program.cs`, bypassing the shared matrix, same pattern as the Stage 14Q.5 SuperAdmin-only fix). Discoverability: direct URL only, plus one small Owner-only link in the existing apps' shared profile/account dropdown - never in the app switcher or any sidebar.
- [x] **`PersonalFin_01` Foundation** - done 2026-07-17, v6.9.5, on branch `swalekha`. `swalekha_db` + `SwalekhaDbContext` (`backend/Garmetix.Infrastructure/Data/SwalekhaDbContext.cs`, own `ConnectionStrings:Swalekha`, own `IDesignTimeDbContextFactory`, zero DbSets yet - real domain models arrive in PersonalFin_02), `GarmetixPolicies.SwalekhaOwner` (registered directly in `Program.cs`'s `AddAuthorization`, bypassing `AddMatrixPolicy`, asserting `AccessPermissionMatrix.IsOwner` which was widened from `private` to `public` for this - same standalone pattern as the Stage 14Q.5 SuperAdmin-only fix), a startup `EnsureCreatedAsync` bootstrap wrapped in try/catch so a Swalekha DB outage can never block the shared business API from starting, and `GET /api/swalekha/health` (Owner-gated). New isolated `frontend/modular/apps/swalekha` Nuxt app (port 3109, path `/swalekha/`, own minimal top bar in `app.vue` - not `ModularAppShell.vue` - own `login.vue`/`access-denied.vue`/`index.vue`, `middleware/auth.global.ts` enforcing Owner-only client-side, deliberately absent from `config/apps.ts`/`routes.ts`/every other app's `appUrls`). One small Owner-role-conditional link added to `ModularAppShell.vue`'s profile dropdown (a new one-off `swalekhaUrl` runtime-config key on the 9 existing apps' `nuxt.config.ts`, separate from the `appUrls` object the switcher itself reads). Deploy wiring added to `srp-whole-site-deploy.sh` (`build_app`, Nginx `location /swalekha/`, `--apps=` allow-list, `patch_static_runtime_config` patch for the new `swalekhaUrl` key) - not executed, no deploy performed. `docs/database-stage-backup-protocol.md` updated to flag that `swalekha_db` is not yet covered by the existing single-database backup command; extending it is required before any stage that writes real Swalekha data. Validated: `dotnet build` (0 errors, same 7 pre-existing warnings), full backend test suite (281 passed, 3 pre-existing Postgres-only skipped, zero regressions), clean `swalekha-web` and `books-web` production builds (compiled-bundle grep confirmed both the new app's content and the profile-menu link string), `validate-structure.mjs`. Proves isolation end-to-end (separate DB, Owner-only auth, invisible switcher, one small entry point) before any real personal-finance feature is built.
- [x] **`PersonalFin_02` Accounts Hub Core** - done 2026-07-17, v6.9.6, on branch `swalekha`. New `SwalekhaAccount`/`SwalekhaAccountTransaction` domain models (`backend/Garmetix.Domain/Generated/Models/Swalekha/SwalekhaAccounts.cs`, plain `BaseEntity`), `SwalekhaDbContext` extended with `OnModelCreating` (soft-delete query filter, decimal(18,2), DateTime-kind normalization for Postgres `timestamp without time zone` - the same three global conventions `GarmetixDbContext` applies, reused rather than re-derived), and a new idempotent `SwalekhaSchemaRepairService` (mirrors `DatabaseSchemaRepairService`'s `CREATE TABLE IF NOT EXISTS`/`ADD COLUMN IF NOT EXISTS` pattern - needed because startup `EnsureCreatedAsync` only creates tables the first time `swalekha_db` has none, it won't add new tables/columns on later deploys). Full accounts API (`backend/Garmetix.Api/Swalekha/SwalekhaAccountsEndpoints.cs`, all under `GarmetixPolicies.SwalekhaOwner`): account CRUD (soft delete), a paginated per-account transaction ledger, deposit/withdrawal entry, and a transfer endpoint that posts a linked `TransferOut`/`TransferIn` pair (shared `TransferGroupId`) so both legs reverse together if either is deleted. `CurrentBalance` on the account is the live source of truth, updated transactionally (`Database.BeginTransactionAsync`) alongside every transaction insert/delete - never recomputed from history on read. Frontend: new `pages/accounts/index.vue` (net worth + per-type summary cards, account table, create/edit `USlideover`, transfer `USlideover`) and `pages/accounts/[id].vue` (account header, add-transaction form, paginated ledger table with delete) in the `swalekha` app; the dashboard's `PersonalFin_02` "coming soon" card was replaced with a real "Accounts Hub" button. Validated: `dotnet build` (0 errors, same 7 pre-existing warnings), full backend test suite (281 passed, 0 regressions), clean `swalekha-web` production build (compiled-bundle grep confirmed "Accounts Hub"), `validate-structure.mjs`, and a dev-server pass confirming zero console errors and the Owner-only middleware correctly redirects an unauthenticated `/accounts` request to `/login` - no test credentials in this environment, so the actual account CRUD/ledger/transfer click-through was not exercised live, consistent with every prior Books/HR/Purchase stage's documented limitation in this project.
- [x] **`PersonalFin_03` Contacts + Person Ledger** - done 2026-07-17, v6.9.7, on branch `swalekha`. New `SwalekhaContact`/`SwalekhaPersonLedgerEntry` domain models (`backend/Garmetix.Domain/Generated/Models/Swalekha/SwalekhaContacts.cs`) - a personal contact directory separate from the CRM business customer list, and a per-contact ledger for money lent/borrowed outside formal loan accounts. `Balance` is signed from the Owner's point of view (positive = contact owes Owner, negative = Owner owes contact), updated transactionally alongside every ledger insert/delete, same pattern `PersonalFin_02` established for account balances. New `backend/Garmetix.Api/Swalekha/SwalekhaContactsEndpoints.cs` (`GarmetixPolicies.SwalekhaOwner`): contact CRUD (soft delete), paginated ledger, `LoanGiven`/`LoanTaken`/`RepaymentReceived`/`RepaymentPaid` entries, delete-entry with balance reversal, and a `POST .../settle` action that auto-computes and posts whichever repayment direction brings the balance to exactly zero (no manual amount/direction math needed). `SwalekhaSchemaRepairService` extended with `SwalekhaContacts`/`SwalekhaPersonLedgerEntries` idempotent table definitions. Frontend: new `pages/contacts/index.vue` (owed-to-you / you-owe / net summary cards, contact table, create/edit `USlideover`) and `pages/contacts/[id].vue` (contact header, add-ledger-entry form, Settle button, paginated ledger table with delete); dashboard's `PersonalFin_03` placeholder card replaced with a real "Contacts" nav button. Validated: `dotnet build` (0 errors, same 7 pre-existing warnings), full backend test suite (281 passed, 0 regressions), clean `swalekha-web` production build (`/contacts` prerenders, compiled-bundle grep confirmed "Person Ledger"), `validate-structure.mjs`, dev-server pass (zero console errors, unauthenticated `/contacts` correctly redirects to `/login`). No live click-through of the contact/ledger/settle flow - no test credentials in this environment, same documented limitation as every prior stage.
- [x] **`PersonalFin_04` Expense & Income** - done 2026-07-17, v6.9.8, on branch `swalekha`. New `SwalekhaExpenseSheet`/`SwalekhaExpenseEntry`/`SwalekhaIncomeEntry`/`SwalekhaRecurringBill` domain models (`backend/Garmetix.Domain/Generated/Models/Swalekha/SwalekhaExpenses.cs`). Sheets are grouped by a free-text `SheetType` (Personal/House/Medical/Gifts/Hidden/custom) - deliberately generic since `PersonalFin_05`'s Travel sheets are a specialization of the same table, not a separate one. Entries can each be individually flagged `IsHidden` (the Hidden Expenses requirement), independent of which sheet they're on. `RecurringBill` is a deliberately lightweight due-day-of-month reminder with a manual mark-paid stamp, not a full recurring-transaction engine. **Scope decision, disclosed**: all four entities are standalone from Accounts Hub in v1 - not linked to `SwalekhaAccount` balances, to avoid a double-entry risk (recording the same money movement twice) and keep this stage tractable; a future stage can add an optional payment-account link if reconciling against Accounts Hub balances is wanted. New `backend/Garmetix.Api/Swalekha/SwalekhaExpenseEndpoints.cs` (sheet + entry CRUD, hidden/visible-aware paginated entry list, a cross-sheet `GET /api/swalekha/expenses/summary` grouped by sheet type and category), `SwalekhaIncomeEndpoints.cs`, `SwalekhaRecurringBillEndpoints.cs` (incl. `mark-paid`). `SwalekhaSchemaRepairService` extended with the four new tables. Frontend: `pages/expenses/index.vue` (visible/hidden spend + per-sheet-type summary cards, sheet table, create/edit `USlideover` with a datalist-backed free-text sheet-type field), `pages/expenses/[id].vue` (add-entry form with a hidden toggle, a show-hidden switch, paginated entry table), `pages/income/index.vue` (inline add form, paginated list with running total), `pages/recurring-bills/index.vue` (bill table with a due-this-month badge and one-click mark-paid); dashboard nav updated with Expenses/Income/Bills buttons. Validated: `dotnet build` (0 errors, same 7 pre-existing warnings), full backend test suite (281 passed, 0 regressions), clean `swalekha-web` production build (`/expenses`, `/income`, `/recurring-bills` all prerender; compiled-bundle grep confirmed "Recurring Bills"), `validate-structure.mjs`, dev-server pass (zero console errors across all three new routes, each correctly redirects to `/login`). No live click-through - no test credentials in this environment, same documented limitation as every prior stage.
- [x] **`PersonalFin_05` Travel Expense Sheets** - done 2026-07-17, v6.9.9, on branch `swalekha`. New `SwalekhaTrip` model (`backend/Garmetix.Domain/Generated/Models/Swalekha/SwalekhaTrips.cs`) - a thin metadata wrapper around a dedicated `SwalekhaExpenseSheet` (`SheetType` "Travel") created automatically on trip creation. Expense entries are added via the **existing** `PersonalFin_04` `SwalekhaExpenseEndpoints` entry routes against the trip's `SheetId` - deliberately not duplicated, built directly on the prior stage's tables as planned. **"Roll up into the main Expense ledger under Travel" needed no data-copy step**: money already lives in a Travel-typed sheet from the moment it's entered, so the existing `GET /api/swalekha/expenses/summary`'s `bySheetType` grouping already aggregates every trip's spend under Travel automatically, while per-trip drill-down stays available via each trip's own `SheetId` - closing a trip (`backend/Garmetix.Api/Swalekha/SwalekhaTripEndpoints.cs`, `POST .../close`) just finalizes it (`IsClosed`/`ClosedAt` on the trip, `IsActive=false` on its linked sheet), with a matching `reopen`. Trip name/budget edits stay synced to the linked sheet; delete soft-deletes both the trip and its sheet together, entries kept intact. `SwalekhaSchemaRepairService` extended with the `SwalekhaTrips` table. Frontend: `pages/trips/index.vue` (trip table - budget-vs-spent colored when over, open/closed badge, close/reopen/edit/delete, create/edit `USlideover`) and `pages/trips/[id].vue` (trip header, an add-expense form hidden once the trip is closed, an explanatory banner on closed trips, paginated entry table reusing the same `expense-sheets/{id}/entries` endpoints from `PersonalFin_04`); dashboard nav updated with a Trips button. Validated: `dotnet build` (0 errors, same 7 pre-existing warnings), full backend test suite (281 passed, 0 regressions), clean `swalekha-web` production build (`/trips` prerenders; compiled-bundle grep confirmed "Travel Expense Sheets"), `validate-structure.mjs`, dev-server pass (zero console errors, unauthenticated `/trips` correctly redirects to `/login`). No live click-through - no test credentials in this environment, same documented limitation as every prior stage.
- [x] **`PersonalFin_06` Multi-Owner Data Isolation** - done 2026-07-17, v6.9.12, on branch `swalekha`. **Amit caught a real design gap after the first live test pass**: Swalekha had no concept of "which Owner" - a company with two Owner logins would have shared every account/contact/expense/etc, since nothing filtered by user. New `SwalekhaOwnedEntity` base class (`Guid OwnerId`, `backend/Garmetix.Domain/Generated/Models/Swalekha/SwalekhaOwnedEntity.cs`) that all 9 existing Swalekha entities now extend instead of plain `BaseEntity`. New scoped `SwalekhaOwnerContext` (`backend/Garmetix.Infrastructure/Data/SwalekhaOwnerContext.cs`) populated per-request by `SwalekhaOwnerMiddleware` (`backend/Garmetix.Api/Swalekha/SwalekhaOwnerMiddleware.cs`) from the JWT's `NameIdentifier` claim - same established pattern as `AuditActorContext`/`AuditActorMiddleware`, not `IHttpContextAccessor` (confirmed via research that's not this codebase's idiom). `SwalekhaDbContext` now (a) auto-stamps `OwnerId` on every insert via a `SaveChanges`/`SaveChangesAsync` override (only when `OwnerId` is still `Guid.Empty` - deliberately not overwriting an already-set value, the one exception `PersonalFin_07`'s family-transfer sync needs to post directly into a *different* owner's data), and (b) applies a combined `!Deleted && OwnerId == CurrentOwnerId` global query filter to every `SwalekhaOwnedEntity` type (built via the same generic expression-tree technique the existing soft-delete filter already used) - so no individual endpoint can forget to scope a query and leak data across owners. `SwalekhaSchemaRepairService` extended with an `OwnerId` column + index on all 9 tables. **Verified live with two real Owner logins** (created a second test Owner, `swalekha_owner2`, via `POST /api/access/users`): confirmed via direct API calls that Owner 2 starts with a completely empty account list, creating an account as Owner 2 is fully invisible to Owner 1's `GET /accounts`, and vice versa - the core privacy guarantee holds. Pre-existing test data from earlier this session (created before this fix existed) is now correctly "orphaned" (`OwnerId` defaulted to all-zeros by the `ALTER TABLE` migration, matching no real owner) - expected and safe, it was only test data. Validated: `dotnet build` (0 errors, same 7 pre-existing warnings), full backend test suite (281 passed, 0 regressions).
- [x] **`PersonalFin_07` Owner Profile + Family Connections + Transaction Sync** - done 2026-07-17, v6.9.13, on branch `swalekha`. New `SwalekhaOwnerProfile` (PAN/Aadhar/Passport/address/spouse name+contact/`LinkedAccountId` pointing at one of the Owner's own `SwalekhaAccount` rows) and `SwalekhaFamilyMember` (name/relationship/mobile/DOB/notes/`LinkedOwnerId`) entities, `backend/Garmetix.Domain/Generated/Models/Swalekha/SwalekhaProfile.cs`. `GET/PUT /api/swalekha/owner-profile` (`SwalekhaProfileEndpoints.cs`) auto-provisions the profile on first access: reads the current Owner's `AppUser.EmployeeId` from `GarmetixDbContext` and, when it resolves to an `EmployeeCategory.Owner` row, prefills PAN/Aadhar/Mobile/Email/spouse name from `Employee`/`EmployeeDetail` and marks `IsAutoProvisioned` - still fully editable afterward. `GET/POST/PUT/DELETE /api/swalekha/family` + `GET /family/linkable-owners` (`SwalekhaFamilyEndpoints.cs`) manage the family list; each member can optionally be linked to a real Owner-type `AppUser.Id` on the platform. `POST /api/swalekha/family/{id}/transfer` is the "one entry syncs both sides" sync: **linking is deliberately mutual-consent** - a transfer only succeeds once *both* Owners have independently linked each other back (checked fresh at transfer time via `IgnoreQueryFilters`, not just at link-creation time, since either side could remove the link later), otherwise it's rejected with a clear message before any money moves. A confirmed transfer debits the sender's chosen account and, in the same DB transaction, credits the recipient's own linked-account (falling back to their best active account, or auto-creating one) - both legs live in the same `swalekha_db`, just under different `OwnerId` values. **Live-verified with three real logins**: `AFOwner` (a genuine `Employee`-linked Owner, `Category=Owner`) auto-provisioned correctly (`fullName: "Amit Kumar"`, `pan: "NA"`, `aadhar: "000000000000"`, `mobile: "9334799099"`, `isAutoProvisioned: true`); linked `AFOwner` <-> `swalekha_owner2` as family members from both sides, confirmed `linkConfirmed` flips to `true` only once both sides exist; sent a real ₹1,500 transfer through the actual UI (USelectMenu account picker, amount/date/narration form, Send button) - sender's balance dropped 5000 -> 3500, recipient's linked Bank account rose 0 -> 1500, confirmed via direct API reads on both sides; and confirmed the negative path (`400`, "The link isn't confirmed yet...") when only one side has linked. Validated: `dotnet build` (0 errors, same 7 pre-existing warnings), full backend test suite (281 passed, 0 regressions), clean `swalekha-web` production build (`/profile` and `/family` both prerender with real content), `validate-structure.mjs`.
- [x] **`PersonalFin_08` Investments I** - done 2026-07-17, v6.9.14, on branch `swalekha`. New `SwalekhaFixedDeposit`/`SwalekhaRecurringDeposit` entities (`backend/Garmetix.Domain/Generated/Models/Swalekha/SwalekhaInvestments.cs`) - bank/number/principal-or-monthly-installment/interest rate/tenure/start+maturity dates/expected maturity amount/TDS(FD only)/auto-renew(FD only), each optionally carrying an `AccountId` linking to one of the Owner's own `SwalekhaAccount` rows. Full CRUD (`SwalekhaInvestmentEndpoints.cs`, `/api/swalekha/fixed-deposits`, `/api/swalekha/recurring-deposits`). **Deliberately not disconnected trackers**: `POST .../mark-matured` (both FD and RD) credits the linked account with the final maturity amount in the same DB transaction (mirroring `PersonalFin_05`'s Trip-close roll-up pattern) and posts a real `SwalekhaAccountTransaction`; RD's `POST .../record-installment` debits the linked account for the monthly installment and increments `InstallmentsPaid` the same way. Both actions work with no linked account too (a plain tracker, no ledger entry) - the UI discloses which mode applies before the action runs. New `/investments` hub page (active FD principal, active RD monthly commitment, a 90-day upcoming-maturities feed merging both types, links into the two detail pages) plus `/investments/fixed-deposits` and `/investments/recurring-deposits` (filterable table, create/edit slideover, Mark Matured slideover, RD's extra Record Installment slideover). Also shipped a small **About Swalekha** page (`/about`, developer credit: AKS Labs (India) - Amit Kumar, reachable from the sidebar footer on every page including login). Validated: `dotnet build` (0 errors, same 7 pre-existing warnings), full backend test suite (281 passed, 0 regressions), clean `swalekha-web` production build (all new routes prerender with real content), `validate-structure.mjs`. **Live-verified end to end through the actual UI** (not just API calls): created a real FD (HDFC, Rs 2,000 principal, linked to an existing account) and RD (SBI, Rs 500/month, linked to the same account), recorded one RD installment via the UI (account correctly debited 5650 -> 5150), marked the FD matured via the UI (account correctly credited 3500 -> 5650) - the Investments hub then correctly reflected 0 active/1 matured FD and 1 active/0 matured RD.
- [x] **`PersonalFin_09` Investments II** - done 2026-07-17, v6.9.15, on branch `swalekha`. New `SwalekhaMutualFund`/`SwalekhaMutualFundTransaction` entities (`backend/Garmetix.Domain/Generated/Models/Swalekha/SwalekhaMutualFunds.cs`) - a folio (scheme/AMC/folio number/investment mode Lumpsum-or-SIP/SIP amount+day-of-month) with running `CurrentUnits`/`TotalInvested` totals, plus per-event transactions (Purchase/SipInstallment/Redemption). Full CRUD + `PUT .../nav` (manual NAV update) + `GET .../returns` (`SwalekhaMutualFundEndpoints.cs`, `/api/swalekha/mutual-funds`). **Genuinely integrated with the Accounts Hub ledger**, same pattern `PersonalFin_08` established for FD/RD: Purchase/SipInstallment debits the linked account (units = amount/NAV), Redemption credits it back (amount = units*NAV) and reduces `TotalInvested` at the fund's average cost per unit - a standard approximation for a personal tracker, not full FIFO/LIFO lot accounting. Deleting a transaction reverses its *exact* effect (including on the account balance) because Redemptions record precisely how much they removed from `TotalInvested` (`InvestedAmountRemoved`) rather than re-deriving a ratio against a `CurrentUnits` value that may have since changed - verified live via the API (redeem 4 of 10 units -> units 6/invested 600/account credited 520, then delete that transaction -> units/invested/account balance all restored exactly to 10/1000/4150).
  - **`SwalekhaXirrCalculator.cs`**: a standalone, unit-tested XIRR (extended IRR) utility - Newton-Raphson with a bisection fallback over the fund's full dated cash-flow history (every transaction plus a final "sell everything today" pseudo-flow using the current NAV). Given the financial-correctness stakes of getting this wrong, shipped with 5 xUnit tests (`Garmetix.Api.Tests/Swalekha/SwalekhaXirrCalculatorTests.cs`) including a hand-verified case (invest 1000, get back 1200 exactly one year later -> must return exactly 20%) before it was wired into any endpoint.
  - SIP funds carry a `SipDueThisMonth` flag (mirroring `PersonalFin_04`'s Recurring Bill `DueThisMonth` pattern) computed from `LastSipInstallmentDate`.
  - Frontend: `/investments/mutual-funds` (list, create/edit, Update NAV, aggregate Total Invested/Current Value/Overall Return/SIPs-Due cards) and `/investments/mutual-funds/[id]` (returns/XIRR panel, Record Transaction form, paginated transaction history with delete-reverses). Investments hub (`/investments`) gained a Mutual Funds card.
  - Validated: `dotnet build` (0 errors, same 7 pre-existing warnings), full backend test suite (**286 passed** - 281 prior + 5 new XIRR tests, 0 regressions), clean `swalekha-web` production build, `validate-structure.mjs`. **Live-verified end to end through the actual UI**: created a real Lumpsum fund linked to an existing account, recorded a Rs 1,000 Purchase at NAV 100 (correctly computed 10 units, account debited 5150->4150), updated NAV to 130 (Current Value Rs 1,300, Absolute Return Rs 300, **XIRR 29.94%** - hand-verified against the exact one-year-round-trip math, which predicts ~30%), and confirmed a SIP fund's `SipDueThisMonth` flag correctly flips false after recording an installment.
- [x] **`PersonalFin_10` Investments III** - done 2026-07-17, v6.9.16, on branch `swalekha`. New `SwalekhaShareHolding`/`SwalekhaShareTransaction` entities (`backend/Garmetix.Domain/Generated/Models/Swalekha/SwalekhaShares.cs`) - symbol/company/exchange/demat/broker, running `CurrentQuantity`/`TotalInvested`/`RealizedPnL`, plus per-event Buy/Sell transactions. Full CRUD + `PUT .../price` (manual price update) (`SwalekhaShareEndpoints.cs`, `/api/swalekha/shares`), using the **exact same integration pattern `PersonalFin_09` established for Mutual Funds**: Buy debits the linked account and grows the position, Sell credits it back and reduces `TotalInvested` at average cost per share while adding the realized gain/loss to `RealizedPnL` - deleting a transaction reverses its exact effect (units, invested amount, realized P&L, *and* the account balance) because Sells record `InvestedAmountRemoved`/`RealizedPnLOnSale` rather than re-deriving a ratio later. `CurrentPrice` drives `CurrentValue`/`UnrealizedPnL`/`UnrealizedPnLPercent`.
  - New `SwalekhaOtherAsset` entity + `SwalekhaOtherAssetEndpoints.cs` (`/api/swalekha/other-assets`) for PPF/EPF/NPS/Gold - deliberately scoped as `CurrentValue`-only snapshots with no transaction history or account integration, matching the design doc's explicit "simple asset entries" framing for these holding types (distinct from Shares/Mutual Funds' full buy-sell tracking).
  - Frontend: `/investments/shares` (list, create/edit, Update Price, aggregate Total Invested/Current Value/Unrealized/Realized P&L cards) and `/investments/shares/[id]` (P&L panel, Record Transaction form, paginated transaction history with delete-reverses); `/investments/other-assets` (simple CRUD list with a Total Value card). Investments hub (`/investments`) gained Shares and Other Assets cards.
  - Validated: `dotnet build` (0 errors, same 7 pre-existing warnings), full backend test suite (286 passed, 0 regressions), clean `swalekha-web` production build, `validate-structure.mjs`. **Live-verified with exact math via direct API checks** (the Browser pane itself degraded mid-session - blank screenshots, unreliable clicks - a tooling issue unrelated to the app, confirmed by every backend call still succeeding correctly): created a holding via the actual UI, then Buy 10 shares at Rs 100 (account correctly debited 2150->1150), Sell 4 at Rs 150 (avg-cost-removed Rs 400, realized P&L Rs 200, quantity 10->6, invested 1000->600, account correctly credited to 1750), then a price update to Rs 120 correctly computed Current Value Rs 720 and Unrealized P&L Rs 120 (20%) - all exactly as expected.
- [x] **`PersonalFin_11` Loans** - done 2026-07-17, v6.9.17, on branch `swalekha`. New `SwalekhaLoan`/`SwalekhaLoanPayment` entities (`backend/Garmetix.Domain/Generated/Models/Swalekha/SwalekhaLoans.cs`) - loan type/lender/loan number/principal/rate/tenure/EMI, running `OutstandingPrincipal`, optionally linked to one of the Owner's own `SwalekhaAccounts`. Full CRUD + `POST .../calculate-emi` + `GET .../amortization-schedule` (`SwalekhaLoanEndpoints.cs`, `/api/swalekha/loans`), using the same money-integration pattern `PersonalFin_08`/`09`/`10` established: every EMI/Prepayment debits the linked account. **`SwalekhaLoanCalculator.cs`**: a standalone, unit-tested utility (`Garmetix.Api.Tests/Swalekha/SwalekhaLoanCalculatorTests.cs`, 5 tests incl. a textbook-verified EMI case) providing the standard reducing-balance EMI formula and a projected month-by-month amortization schedule, following the exact precedent `SwalekhaXirrCalculator` set in `PersonalFin_09` - built and tested before being wired into any endpoint.
  - Each EMI splits into interest (`OutstandingPrincipal x monthlyRate`) and principal, both stored explicitly on the payment row so a later delete reverses the *exact* effect on `OutstandingPrincipal` and the account balance rather than re-deriving a split. A payment that brings `OutstandingPrincipal` to (near) zero - whether the final scheduled EMI or an early full prepayment - auto-closes the loan (`IsClosed`/`ClosedAt`); deleting that payment correctly reopens it.
  - **"Loans Given" deliberately has no new entity** - the design (`docs/personal-finance-module-design.md` A3) explicitly scoped it to reuse `PersonalFin_03`'s existing Person Ledger (a Contact's `LoanGiven`/`RepaymentReceived` entries) rather than duplicating a second ledger. The new `/loans` page's "Loans Given" section links to Contacts and surfaces the total currently owed to the Owner there (reusing the existing `GET /api/swalekha/contacts` data, no new backend).
  - Frontend: `/loans` (list, create/edit with a "Suggest" EMI-calculator button, aggregate Total Outstanding/Monthly EMI Commitment/Active Loans cards) and `/loans/[id]` (Record Payment form, paginated payment history with delete-reverses, projected amortization schedule table).
  - Validated: `dotnet build` (0 errors, same 7 pre-existing warnings), full backend test suite (**291 passed** - 286 prior + 5 new loan-calculator tests, 0 regressions), clean `swalekha-web` production build, `validate-structure.mjs`. **Live-verified end to end through the actual UI**: created a real Rs 1,00,000 loan at 12% for 12 months, Suggest correctly computed EMI Rs 8,884.88 (the exact textbook value), the projected schedule fully amortized to Rs 0.00 in exactly 12 rows, a real EMI payment split exactly into Rs 1,000 interest / Rs 7,884.88 principal (account debited, outstanding reduced to exactly Rs 92,115.12) with an exact reversal on delete, and a full prepayment correctly auto-closed the loan with an exact reopen on delete-reversal.
- [x] **`PersonalFin_12` Insurance** - done 2026-07-17, v6.9.18, on branch `swalekha`. New `SwalekhaInsurancePolicy` entity (`backend/Garmetix.Domain/Generated/Models/Swalekha/SwalekhaInsurance.cs`) - Life/Health/Term/Vehicle/Property/ULIP/Other, insurer/policy number, sum assured, nominee name+relationship, premium amount+frequency (Monthly/Quarterly/HalfYearly/Yearly), maturity date+amount for endowment/ULIP policies. Full CRUD + `POST .../pay-premium` + `POST .../mark-matured` (`SwalekhaInsuranceEndpoints.cs`, `/api/swalekha/insurance-policies`), using the same money-integration pattern `PersonalFin_08` established for FD/RD: `PayPremium` debits the linked account per premium, `MarkMatured` credits it with the maturity payout.
  - `NextPremiumDueDate`/`PremiumDueNow` are computed at read time from `PremiumFrequency` and `LastPremiumPaidDate` (or `StartDate` if never paid), not stored - a frequency-aware generalization of `PersonalFin_04`'s Recurring Bill `DueThisMonth` pattern. Deliberately flags an overdue premium as due too (not just due-today), since `nextDue <= today` rather than an exact-date match.
  - Frontend: `/insurance` (list, create/edit, Pay Premium and Mark Matured actions, aggregate Total Sum Assured/Active Policies/Premiums Due cards).
  - Validated: `dotnet build` (0 errors, same 7 pre-existing warnings), full backend test suite (291 passed, 0 regressions), clean `swalekha-web` production build, `validate-structure.mjs`. **Live-verified end to end through the actual UI**: created a real policy (Rs 5,00,000 sum assured, Rs 12,000/Yearly premium, Rs 5,20,000 expected maturity) linked to a real account - `NextPremiumDueDate` correctly computed as exactly one year after the start date; Pay Premium correctly debited the account (1750 -> -10,250) and advanced the due date to the following year; Mark Matured correctly credited the account with the maturity amount (-10,250 -> 5,09,750) and the policy correctly displayed "Matured" on the list, excluded from the active Sum Assured total.
- [x] **`PersonalFin_13` Personal Organizer** - done 2026-07-17, v6.9.19, on branch `swalekha`. Starts Pillar B. New `SwalekhaJournalEntry`/`SwalekhaPersonalNote`/`SwalekhaAppointment` entities (`backend/Garmetix.Domain/Generated/Models/Swalekha/SwalekhaOrganizer.cs`) + CRUD endpoints (`SwalekhaJournalEndpoints.cs` `/api/swalekha/journal`, `SwalekhaNoteEndpoints.cs` `/api/swalekha/notes` with folder/search filtering, `SwalekhaCalendarEndpoints.cs` `/api/swalekha/appointments`).
  - Journal entries are already private by construction (Owner-only data isolation from `PersonalFin_06`) - no separate visibility flag was added, since one would be inert. Personal Notes are deliberately distinct from Journal - topic-organized (Folder + freeform comma-separated Tags, pin/search) rather than a dated timeline.
  - **`GET /api/swalekha/calendar?year=&month=`** is the "auto-populated finance due-dates from Pillar A" the design asked for: merges real `SwalekhaAppointment` rows with due-dates computed *live* for the requested month from active Recurring Bills (`DueDayOfMonth` projected into that month, clamped to the month's day count), open FD/RD maturities (`MaturityDate` in range), and active Insurance policies (`NextPremiumDueDate`, recomputed the same way `SwalekhaInsuranceEndpoints` does it) - none of this is duplicated into a stored table, matching the computed-not-stored pattern `PersonalFin_04`/`12` already established for `DueThisMonth`/`NextPremiumDueDate`.
  - Frontend: `/journal` (dated entry cards, create/edit), `/notes` (grid with folder/tag badges, pin toggle, search), `/calendar` (month-navigable agenda list, color-coded by event type: Appointment/Bill Due/FD Maturity/RD Maturity/Premium Due, New Appointment action).
  - Validated: `dotnet build` (0 errors, same 7 pre-existing warnings), full backend test suite (291 passed, 0 regressions), clean `swalekha-web` production build, `validate-structure.mjs`. **Live-verified end to end**: created a real journal entry and a real pinned/tagged/foldered note through the actual UI (search by tag `goa` correctly matched 1 of 2 notes, a non-matching term correctly matched 0); added a real appointment via the UI and a real Recurring Bill via the API - the calendar correctly merged both onto the same day (the bill's `DueDayOfMonth` projected exactly onto today's date), sorted correctly, with the right type badge and amount on each.
- [ ] **`PersonalFin_14` Dashboard & Reports** - unified home dashboard (net worth, upcoming dues, today's appointments), portfolio allocation, expense breakdown, Excel/PDF tax-relevant export.
- [ ] **`PersonalFin_15` Document Vault & Hardening** - attach scanned docs (policy/loan/FD proofs) to records; Owner-only access self-check; `swalekha_db` backup/restore drill.

## Process Reminders

- [ ] Before any backend/API/DB change: confirm with Amit if it can affect `frontend/legacy/garmetix-web`.
- [ ] After any code change: update `.claude/changelog.md` and root `CLAUDE.md`.
