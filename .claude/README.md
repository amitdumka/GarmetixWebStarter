# .claude Folder

Working memory and instruction set for Claude (Cowork) sessions in this repo. Codex (the other AI collaborator, working from this same C-drive workspace) and Antigravity (working from `D:\AIArea\GarmetixWebStarter`, branch `Version6.A`) should treat this folder as Claude's scratch/context space, mirroring how `.codex/` is Codex's.

| File | Purpose |
|---|---|
| `profile.md` | Who Claude is in this project and how it should behave here |
| `environment.md` | Workspaces, branches, deploy targets, hosts |
| `instructions.md` | Standing rules Claude must follow when changing code |
| `learnings.md` | Non-obvious codebase facts learned by exploration |
| `changelog.md` | Append-only log of what Claude changed, file by file |
| `roadmap.md` | Forward-looking plan, stage by stage |
| `todo.md` | Active, checkable task list |

Root `CLAUDE.md` stays as the short pointer/summary that Codex reads first; details live here.
